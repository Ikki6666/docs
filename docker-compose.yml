services:
  langchain-docs-zh-static:
    build:
      context: .
      dockerfile: deploy/Dockerfile.zh-static
    image: langchain-docs-zh-static:local
    ports:
      - "33031:80"
    restart: unless-stopped
