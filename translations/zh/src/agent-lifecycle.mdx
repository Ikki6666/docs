---
title: The agent development lifecycle
sidebarTitle: Agent lifecycle
description: 使用 Deep Agents 和 LangSmith 端到端构建、测试、部署、监控并治理一个 agent。
mode: wide
---

这篇导览会带你走完整个 agent 开发生命周期：构建、测试、部署、监控和治理。示例使用 [Deep Agents](/oss/deepagents/overview) 作为框架，但每个阶段都与框架无关。你可以按需引入自己的技术栈，只采用需要的阶段；也可以把 LangSmith 当作一体化平台来使用。

<Note>
本页是一个 scaffold。代码示例只是占位符，在发布前需要补齐并用 `deepagents>=0.6.1` 验证。
</Note>

## 生命周期

{/* TODO: replace with the cyclical lifecycle SVG (Design). Stages: Build → Test → Deploy → Monitor → Govern, with Engine at the center. */}

<CardGroup cols={3}>
  <Card title="构建" icon="hammer" href="/oss/deepagents/overview" />
  <Card title="测试" icon="flask" href="/langsmith/evaluation-concepts" />
  <Card title="部署" icon="rocket" href="/langsmith/deployment-quickstart" />
  <Card title="监控" icon="chart-line" href="/langsmith/observability" />
  <Card title="治理" icon="shield-lock" href="/langsmith/govern" />
  <Card title="Engine" icon="bolt" href="/langsmith/engine" />
</CardGroup>

## 构建

使用 Deep Agents 定义 agent。请参见 [Deep Agents](/oss/deepagents/overview)。

```python
# TODO: scaffold — create a Deep Agent (validate against deepagents>=0.6.1)
```

## 测试

在 LangSmith 中针对数据集评估这个 agent。请参见 [Evaluation](/langsmith/evaluation-concepts)。

```python
# TODO: scaffold — create a dataset and run an evaluation
```

## 部署

把 agent 部署到 LangSmith agent server。请参见 [Agent deployment](/langsmith/agent-server)。

```python
# TODO: scaffold — deploy and invoke the agent
```

## 监控

追踪并监控正在运行的 agent。请参见 [Observability](/langsmith/observability)。

```python
# TODO: scaffold — trace requests and inspect runs
```

## 治理

强制执行模型访问、支出和数据策略。请参见 [Governance](/langsmith/govern)。

```python
# TODO: scaffold — route calls through the LLM Gateway with spend and redaction policies
```

## Engine

{/* TODO: pull in shared Engine snippet once created (connects UI and webhooks across stages). */}

了解 Engine 如何连接整个生命周期。请参见 [Engine](/langsmith/engine)。
