---
title: 迁移到 SmithDB 支持的 SDK 方法
description: 将现有 LangSmith SDK 方法迁移到 SmithDB 支持的等价方法，以获得更快的 agent 可观测性。
noindex: true
---

import SmithdbMigrationRunsQuery from '/snippets/langsmith/smithdb-migration/runs-query.mdx';
import SmithdbMigrationRunsRetrieve from '/snippets/langsmith/smithdb-migration/runs-retrieve.mdx';
import SmithdbMigrationExperimentRunsQuery from '/snippets/langsmith/smithdb-migration/experiment-runs-query.mdx';

## 背景

2026 年 5 月，我们发布了 [SmithDB](https://www.langchain.com/blog/introducing-smithdb?utm_source=docs)，这是一个面向现代 AI agent 构建的新可观测性数据库。SmithDB 在每一类核心可观测性工作负载上都提供行业领先的性能，让 LangSmith 的核心体验显著变快。

符合条件区域中的 SDK 用户现在可以使用 SmithDB 支持的方法。

## 部署支持

| 部署 | 状态 |
|---|---|
| US SaaS | 可用。方法完全由 SmithDB 支持。 |
| EU 和其他 SaaS 区域 | 方法可用，但尚未由 SmithDB 支持。 |
| 自托管 | 从自托管版本 0.16.0 开始支持。无需 feature flag。 |

## 最低 SDK 版本

SmithDB 支持的方法需要满足最低 SDK 版本：

| 语言 | 包 | 最低版本 |
|---|---|---|
| Python | `langsmith` | `>=0.10.2` |
| TypeScript | `langsmith` | `>=0.8.1` |
| Java | `langsmith-java` | `0.1.0-beta.11` |
| Go | `langsmith-go` | `v0.17.0` |

## 用 AI agent 迁移

本指南可以直接由 AI coding agent 获取并应用。将下面的 prompt 复制到你的 agent 中，把代码库迁移到 SmithDB 支持的方法。

```text
Migrate this codebase's LangSmith SDK usage to the new SmithDB-backed methods.

Fetch https://docs.langchain.com/langsmith/smithdb-sdk-migration.md and treat it
as the source of truth for what changed, including which methods and
parameters are affected, what replaces them, and deployment support.

1. Check the installed LangSmith SDK version against the minimum version
   required for the SmithDB-backed methods per the guide, and upgrade the
   dependency if it does not meet that minimum.
2. Identify every call site in this codebase that uses a method the guide
   marks as migrated, in whichever language(s) this codebase uses.
3. For each call site, apply the corresponding before/after change from the
   guide, including any added, removed, or renamed parameters.

If a call site or parameter is not covered by the guide, stop and ask rather
than guessing.
```

## 异常

<Tabs>
  <Tab title="Python">
    SmithDB 支持的方法会抛出新的异常类，而不是旧版 `langsmith.utils` 中的异常类。

    | 之前（`langsmith.utils`） | 之后（`langsmith`） | 说明 |
    |---|---|---|
    | `LangSmithError` | `LangsmithError` | SDK 的基础异常类；大小写有变化 |
    | `LangSmithAPIError` | `InternalServerError` | 5xx |
    | `LangSmithRequestTimeout` | `APITimeoutError` | 请求超时时抛出 |
    | `LangSmithUserError` | *（已移除）* | 没有直接对应项。403 org-scoped-key 场景现在抛出 `PermissionDeniedError`；客户端参数校验现在抛出标准的 `ValueError` 或 `TypeError` |
    | `LangSmithRateLimitError` | `RateLimitError` | 429；名称不变 |
    | `LangSmithAuthError` | `AuthenticationError` | 401 |
    | `LangSmithNotFoundError` | `NotFoundError` | 404；名称不变 |
    | `LangSmithConflictError` | `ConflictError` | 409；名称不变 |
    | `LangSmithConnectionError` | `APIConnectionError` | 客户端无法连接到 API 时抛出 |
    | `LangSmithExceptionGroup` | *（已移除）* | 没有对应项 |
    | *（此前不存在）* | `APIError` | 新增：所有 API 相关错误的基础类，带有 `message`、`request` 和 `body` 属性 |
    | *（此前不存在）* | `APIStatusError` | 新增：所有 4xx/5xx 状态错误的基础类 |
    | *（此前不存在）* | `BadRequestError` | 新增：400 |
    | *（此前不存在）* | `PermissionDeniedError` | 新增：403 |
    | *（此前不存在）* | `UnprocessableEntityError` | 新增：422 |
    | *（此前不存在）* | `APIResponseValidationError` | 新增：响应不符合预期 schema 时抛出 |
  </Tab>
  <Tab title="TypeScript">
    SmithDB 支持的方法会抛出新的异常类，而不是普通的 `Error`。

    | 之前（普通 `Error`） | 之后（`langsmith`） | 说明 |
    |---|---|---|
    | *（此前不存在）* | `LangsmithError` | 所有 SDK 错误的基础类 |
    | *（此前不存在）* | `InternalServerError` | 5xx |
    | *（此前不存在）* | `APIConnectionTimeoutError` | 请求超时时抛出 |
    | *（此前不存在）* | `RateLimitError` | 429 |
    | *（此前不存在）* | `AuthenticationError` | 401 |
    | *（此前不存在）* | `NotFoundError` | 404 |
    | *（此前不存在）* | `ConflictError` | 409 |
    | *（此前不存在）* | `APIConnectionError` | 客户端无法连接到 API 时抛出 |
    | *（此前不存在）* | `APIError` | 所有 API 相关错误的基础类，带有 `status`、`headers` 和 `error` 属性 |
    | *（此前不存在）* | `BadRequestError` | 400 |
    | *（此前不存在）* | `PermissionDeniedError` | 403 |
    | *（此前不存在）* | `UnprocessableEntityError` | 422 |
    | *（此前不存在）* | `APIUserAbortError` | 通过 `AbortController` 中止请求时抛出 |
  </Tab>
  <Tab title="Java">
    无变化。错误处理不受此次迁移影响。
  </Tab>
  <Tab title="Go">
    无变化。错误处理不受此次迁移影响。
  </Tab>
  <Tab title="cURL">
    无变化。错误处理不受此次迁移影响。
  </Tab>
</Tabs>

<SmithdbMigrationRunsQuery />

<SmithdbMigrationRunsRetrieve />

<SmithdbMigrationExperimentRunsQuery />
