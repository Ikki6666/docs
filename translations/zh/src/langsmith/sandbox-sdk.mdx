---
title: Sandbox SDK 用法
sidebarTitle: SDK 用法
description: 使用 Python 或 TypeScript SDK 以编程方式创建和管理 sandboxes。
---

import LangsmithEndpointValues from '/snippets/langsmith/langsmith-endpoint-values.mdx';

[LangSmith SDK](/langsmith/reference) 提供了以编程方式创建 sandbox 并与之交互的接口。

## 安装

<CodeGroup>

```bash Python
# uv
uv add "langsmith[sandbox]"

# pip
pip install "langsmith[sandbox]"
```

```bash TypeScript
npm install langsmith
# or
yarn add langsmith
```

</CodeGroup>

Python 的 `[sandbox]` extra 会安装 `websockets`，从而启用实时流式输出和 `timeout=0`。如果没有安装它，`run()` 会自动回退到 HTTP。对于 TypeScript，需要额外安装可选的 `ws` 包来启用 WebSocket 流式输出：

```bash
npm install ws
```

## 创建并运行 sandbox

client 会从环境中读取 `LANGSMITH_API_KEY` 和 `LANGSMITH_ENDPOINT`，因此在创建 sandbox 之前请先导出这两个变量：

```bash
export LANGSMITH_API_KEY="<LANGSMITH_API_KEY>"
export LANGSMITH_ENDPOINT="<LANGSMITH_ENDPOINT>"
```

<LangsmithEndpointValues />

如果你想从可复用的自定义文件系统镜像启动，可以传入 snapshot ID 或名称；相关流程请参见 [Snapshots](/langsmith/sandbox-snapshots)。

<CodeGroup>

```python Python
from langsmith.sandbox import SandboxClient

# Client uses LANGSMITH_ENDPOINT and LANGSMITH_API_KEY from environment
client = SandboxClient()

# Create a sandbox with the default runtime and run code
with client.sandbox() as sb:
    result = sb.run("python -c 'print(2 + 2)'")
    print(result.stdout)  # "4\n"
    print(result.success)  # True
```

```ts TypeScript
import { SandboxClient } from "langsmith/sandbox";

// Client uses LANGSMITH_ENDPOINT and LANGSMITH_API_KEY from environment
const client = new SandboxClient();

// Create a sandbox with the default runtime and run code
const sandbox = await client.createSandbox();
const result = await sandbox.run("node -e 'console.log(2 + 2)'");
console.log(result.stdout); // "4\n"

// Don't forget to clean up
await sandbox.delete();
```

</CodeGroup>

## 运行命令

每次 `run()` 调用都会返回一个 `ExecutionResult`，其中包含 `stdout`、`stderr`、`exit_code` 和 `success`。

<CodeGroup>

```python Python
with client.sandbox() as sb:
    result = sb.run("echo 'Hello, World!'")

    print(result.stdout)     # "Hello, World!\n"
    print(result.stderr)     # ""
    print(result.exit_code)  # 0
    print(result.success)    # True

    # Commands that fail return non-zero exit codes
    result = sb.run("exit 1")
    print(result.success)    # False
    print(result.exit_code)  # 1
```

```ts TypeScript
const sandbox = await client.createSandbox();
try {
  const result = await sandbox.run("echo 'Hello, World!'");

  console.log(result.stdout);     // "Hello, World!\n"
  console.log(result.stderr);     // ""
  console.log(result.exit_code);  // 0

  // Pass environment variables and working directory
  const envResult = await sandbox.run("echo $MY_VAR", {
    env: { MY_VAR: "test-value" },
    cwd: "/tmp",
  });
} finally {
  await sandbox.delete();
}
```

</CodeGroup>

## 流式输出

对于长时间运行的命令，可以通过回调函数或 `CommandHandle` 实时流式读取输出。

### 使用回调进行流式输出

<CodeGroup>

```python Python
import sys

with client.sandbox() as sb:
    result = sb.run(
        "make build",
        timeout=600,
        on_stdout=lambda s: print(s, end=""),
        on_stderr=lambda s: print(s, end="", file=sys.stderr),
    )
    print(f"\nBuild {'succeeded' if result.success else 'failed'}")
```

```ts TypeScript
const result = await sandbox.run("make build", {
  timeout: 600,
  onStdout: (data) => process.stdout.write(data),
  onStderr: (data) => process.stderr.write(data),
});
console.log(`Exit code: ${result.exit_code}`);
```

</CodeGroup>

### 使用 CommandHandle 进行流式输出

将 `wait=False` 传入后，会得到一个 `CommandHandle`，从而更细粒度地控制输出流。

<CodeGroup>

```python Python
with client.sandbox() as sb:
    handle = sb.run("make build", timeout=600, wait=False)

    print(f"Command ID: {handle.command_id}")

    for chunk in handle:
        prefix = "OUT" if chunk.stream == "stdout" else "ERR"
        print(f"[{prefix}] {chunk.data}", end="")

    result = handle.result
    print(f"\nExit code: {result.exit_code}")
```

```ts TypeScript
const handle = await sandbox.run("python train.py", {
  wait: false,
  timeout: 600,
});

console.log(`Command ID: ${handle.commandId}`);
console.log(`PID: ${handle.pid}`);

for await (const chunk of handle) {
  if (chunk.stream === "stdout") {
    process.stdout.write(chunk.data);
  } else {
    process.stderr.write(chunk.data);
  }
}

const result = await handle.result;
console.log(`Exit code: ${result.exit_code}`);
```

</CodeGroup>

### 发送 stdin 并终止命令

<CodeGroup>

```python Python
with client.sandbox() as sb:
    handle = sb.run(
        "python -c 'name = input(\"Name: \"); print(f\"Hello {name}\")'",
        timeout=30,
        wait=False,
    )

    for chunk in handle:
        if "Name:" in chunk.data:
            handle.send_input("World\n")
        print(chunk.data, end="")

    result = handle.result
```

```ts TypeScript
const handle = await sandbox.run("python -i", { wait: false });

// Send input to stdin
handle.sendInput("print(2 + 2)\n");
handle.sendInput("exit()\n");

for await (const chunk of handle) {
  process.stdout.write(chunk.data);
}
```

</CodeGroup>

终止一个正在运行的命令：

<CodeGroup>

```python Python
with client.sandbox() as sb:
    handle = sb.run("python server.py", timeout=0, wait=False)

    for chunk in handle:
        print(chunk.data, end="")
        if "Ready" in chunk.data:
            break

    handle.kill()
```

```ts TypeScript
const handle = await sandbox.run("sleep 300", { wait: false });
handle.kill();

const result = await handle.result;
console.log(result.exit_code); // non-zero
```

</CodeGroup>

### 重新连接到正在运行的命令

如果客户端断开连接，可以使用 command ID 重新连接：

<CodeGroup>

```python Python
with client.sandbox() as sb:
    handle = sb.run("make build", timeout=600, wait=False)
    command_id = handle.command_id

    # Later, possibly in a different process
    handle = sb.reconnect(command_id)
    for chunk in handle:
        print(chunk.data, end="")
    result = handle.result
```

```ts TypeScript
const handle = await sandbox.run("long-task", { wait: false });
const commandId = handle.commandId;

// Later, or from a different client
const newHandle = await sandbox.reconnect(commandId);
for await (const chunk of newHandle) {
  process.stdout.write(chunk.data);
}
```

</CodeGroup>

## 文件操作

在 sandbox 中读写文件：

<CodeGroup>

```python Python
with client.sandbox() as sb:
    # Write a file
    sb.write("/app/script.py", "print('Hello from file!')")

    # Run the script
    result = sb.run("python /app/script.py")
    print(result.stdout)  # "Hello from file!\n"

    # Read a file (returns bytes)
    content = sb.read("/app/script.py")
    print(content.decode())  # "print('Hello from file!')"

    # Write binary files
    sb.write("/app/data.bin", b"\x00\x01\x02\x03")
```

```ts TypeScript
const sandbox = await client.createSandbox();
try {
  // Write a file (string content)
  await sandbox.write("/app/script.py", "print('Hello from file!')");

  // Run the script
  const result = await sandbox.run("python /app/script.py");
  console.log(result.stdout);  // "Hello from file!\n"

  // Read a file (returns Uint8Array)
  const content = await sandbox.read("/app/script.py");
  console.log(new TextDecoder().decode(content));

  // Write binary files
  await sandbox.write("/app/data.bin", new Uint8Array([0x00, 0x01, 0x02, 0x03]));
} finally {
  await sandbox.delete();
}
```

</CodeGroup>

## 挂载 Context Hub 仓库

挂载一个 [Context Hub](/langsmith/use-the-context-hub) 仓库，可以让 sandbox 中的代码以文件系统方式访问你的 agents 和 skills。该仓库的最新 commit tree 会被镜像到挂载路径，并在 sandbox 的整个生命周期内保持同步，因此新提交无需重启即可出现在正在运行的 sandbox 中。

<Warning>
Context Hub 挂载是**只读**的。同步是单向的，从仓库到 sandbox：agent 写到挂载路径下的文件永远不会被推送回仓库，并且下一次同步会覆盖它们。请将 sandbox 的输出写到挂载路径之外的地方，如果它属于仓库，请[通过 SDK 推送](/langsmith/manage-contexts-sdk)。
</Warning>

通过 `mount_config` 传入挂载。创建 sandbox 所用的 API key 必须对该仓库有访问权限，否则创建会以 `403` 失败。

<CodeGroup>

```python Python
from langsmith.sandbox import AsyncSandboxClient, context_hub_mount, mount_config


async def main():
    async with AsyncSandboxClient() as client:
        async with await client.sandbox(
            name="context-hub-mount-sandbox",
            mount_config=mount_config(
                mounts=[
                    context_hub_mount(
                        id="memories",
                        mount_path="/memories",
                        repo="-/my-agent",
                    )
                ],
            ),
        ) as sb:
            result = await sb.run("ls /memories")
            print(result.stdout)
```

```ts TypeScript
import {
  SandboxClient,
  contextHubMount,
  mountConfig,
} from "langsmith/sandbox";

const client = new SandboxClient();

const sandbox = await client.createSandbox({
  name: "context-hub-mount-sandbox",
  mountConfig: mountConfig({
    mounts: [
      contextHubMount({
        id: "memories",
        mountPath: "/memories",
        repo: "-/my-agent",
      }),
    ],
  }),
});

try {
  const result = await sandbox.run("ls /memories");
  console.log(result.stdout);
} finally {
  await sandbox.delete();
}
```

</CodeGroup>

`repo` 是仓库 handle，可选地以 `owner/repo` 形式限定，其中 `-` 表示当前工作区。

`mount_path` 必须是一个绝对、干净的路径，且不能是文件系统根目录，也不能位于 `/etc` 或 `/usr` 等系统目录下或其下层。任何其他路径都可以。与 bucket 和 Git 挂载不同，Context Hub 挂载不限于 `/mnt/mounts`。

传入 `initial_pull_only` / `initialPullOnly` 可仅在启动时同步一次，而不再轮询仓库更新。

## 为 sandbox 设定规格

在创建时传入 `vcpus`、`mem_bytes` 和 `fs_capacity_bytes`（TypeScript 中为 `vCpus`、`memBytes`、`fsCapacityBytes`）来设定 sandbox 规格。省略它们时，sandbox 会使用以下默认值。

<CodeGroup>

```python Python
sb = client.create_sandbox(
    name="big-vm",
    vcpus=4,
    mem_bytes=16 * 1024**3,        # 16 GiB
    fs_capacity_bytes=32 * 1024**3, # 32 GiB
)
```

```ts TypeScript
const sb = await client.createSandbox({
  name: "big-vm",
  vCpus: 4,
  memBytes: 17_179_869_184,        // 16 GiB
  fsCapacityBytes: 34_359_738_368, // 32 GiB
});
```

</CodeGroup>

| 资源 | 默认值 | 范围 |
|----------|---------|-------|
| CPU | 0.5 vCPU | 0.05 到 16 vCPU。子核请求请使用 `cpu_millicores`（`500` 即 0.5 vCPU）；它优先于 `vcpus`。 |
| 内存 | 每 vCPU 4 GiB | 最多 64 GiB。必须保持在每 vCPU 目标的 50% 范围内，因此一个 1 vCPU 的 sandbox 接受 2 到 6 GiB。只设置内存而不设置 CPU 时，CPU 会按相同比例推导。 |
| 文件系统 | Snapshot 容量 | 最多 64 GiB，且永远不会小于其启动所用的 snapshot。 |

当宿主机有空闲容量时，sandboxes 可以突发到所请求 CPU 的两倍。通过 `update_sandbox` / `updateSandbox` 调整现有 sandbox 的规格会在其下次启动时生效，并且调整时只强制 64 GiB 上限，而不强制每 vCPU 比例。

<Note>
自由格式的 `labels`（每个 sandbox 最多 128 个，每个 key 最多 256 字节，每个 value 最多 4096 字节）可以在创建时通过 REST API 设置。Sandboxes 会继承其 snapshot 的 labels，除非被覆盖。`langsmith.sandbox` 客户端目前尚未暴露该字段。
</Note>

## Sandbox 生命周期与保留策略

Sandbox 采用两阶段保留模型，以**空闲活动**和 **`stopped`** 状态为锚点。

| Field | What it controls | When it fires |
|-------|------------------|---------------|
| `idle_ttl_seconds` | 启动器会在 sandbox 空闲达到该秒数后将其停止。任何命令执行或文件 I/O 都会重置该计时器。`0` 表示禁用空闲停止。 | 未显式设置时，默认值为 `600`（10 分钟）。 |
| `delete_after_stop_seconds` | 一旦 sandbox 进入 `stopped` 状态，该计时器就会开始。到期后，sandbox 记录和文件系统克隆会由服务端清理任务永久删除。`0` 表示禁用基于停止状态的自动删除（需要手动清理）。 | 未显式设置时，服务端会套用其配置的默认值（通常为 14 天）。 |

这两个值都必须是 60 的倍数（按分钟粒度），且 `delete_after_stop_seconds` 的上限为 2592000（30 天）。完整生命周期如下：

```
running ──(idle for idle_ttl_seconds)──▶ stopped ──(delete_after_stop_seconds)──▶ deleted
```

你也可以显式调用 `stop_sandbox` / `stopSandbox`，在空闲超时触发前提前释放资源；这同样会填充 `stopped_at` 并启动删除计时器。此后无需再次启动它：已停止的 sandbox 会在下一次命令、文件操作或 service-URL 请求时自动唤醒。

<CodeGroup>

```python Python
# Default retention (server defaults: 10-min idle stop, 14-day delete)
with client.sandbox() as sb:
    sb.run("echo hello")

# Aggressive: stop after 5 min idle, delete 1 hour after stop
sb = client.create_sandbox(
    idle_ttl_seconds=300,
    delete_after_stop_seconds=3600,
)

# Long-running: never auto-stop, delete 7 days after manual stop
sb = client.create_sandbox(
    idle_ttl_seconds=0,
    delete_after_stop_seconds=604800,
)

# Update retention on an existing sandbox
sb = client.update_sandbox(
    sb.name,
    idle_ttl_seconds=1800,
    delete_after_stop_seconds=2592000,  # 30 days
)
```

```ts TypeScript
// Default retention (server defaults applied)
const sandbox = await client.createSandbox();

// Aggressive: stop after 5 min idle, delete 1 hour after stop
const sb = await client.createSandbox({
  idleTtlSeconds: 300,
  deleteAfterStopSeconds: 3600,
});

// Long-running: never auto-stop, delete 7 days after manual stop
const longRunning = await client.createSandbox({
  idleTtlSeconds: 0,
  deleteAfterStopSeconds: 604800,
});

// Update retention on an existing sandbox
await client.updateSandbox(sb.name, {
  idleTtlSeconds: 1800,
  deleteAfterStopSeconds: 2592000, // 30 days
});
```

</CodeGroup>

## 命令生命周期与 TTL

Sandbox daemon 通过两类超时机制管理命令会话生命周期：

- **Session TTL（已完成命令）**：命令结束后，其会话会在内存中保留一段 TTL 时间（默认 5 分钟）。在此期间你可以重新连接并取回输出。TTL 到期后，会话会被清理。将 `ttl_seconds` 设置为 `-1` 可永久保留会话。
- **Idle timeout（运行中的命令）**：如果运行中的命令没有任何客户端连接，就会在空闲超时后被终止（默认 1 小时）。每当客户端连接时，空闲计时器都会重置。将 `idle_timeout` 设置为 `-1` 可禁用空闲超时。

### 组合使用生命周期选项

<CodeGroup>

```python Python
with client.sandbox() as sb:
    # Long-running task: 30-min idle timeout, 1-hour session TTL
    handle = sb.run(
        "python train.py",
        timeout=0,              # No command timeout
        idle_timeout=1800,      # Kill after 30min with no clients
        ttl_seconds=3600,       # Keep session for 1 hour after exit
        wait=False,
    )

    # Fire-and-forget: no idle timeout, infinite TTL
    handle = sb.run(
        "python background_job.py",
        timeout=0,
        idle_timeout=-1,        # Never kill due to idle
        ttl_seconds=-1,         # Keep session forever
        wait=False,
    )
```

```ts TypeScript
const sandbox = await client.createSandbox();
try {
  // Long-running task: 30-min idle timeout, 1-hour session TTL
  const handle = await sandbox.run("python train.py", {
    timeout: 0,              // No command timeout
    idleTimeout: 1800,       // Kill after 30min with no clients
    ttlSeconds: 3600,        // Keep session for 1 hour after exit
    wait: false,
  });

  // Fire-and-forget: no idle timeout, infinite TTL
  const bg = await sandbox.run("python background_job.py", {
    timeout: 0,
    idleTimeout: -1,         // Never kill due to idle
    ttlSeconds: -1,          // Keep session forever
    wait: false,
  });
} finally {
  await sandbox.delete();
}
```

</CodeGroup>

设置 `kill_on_disconnect=True`（Python）或 `killOnDisconnect: true`（TypeScript）后，当最后一个客户端断开连接时，命令会立刻被杀掉，而不是等到 idle timeout 到来。

## Service URLs（Python）

可以通过带认证的 URL 访问 sandbox 内部运行的 HTTP 服务。你可以在浏览器中打开它、从代码中调用它，或将其分享给团队成员。

```python
with client.sandbox() as sb:
    sb.run("python -m http.server 8000", timeout=0, wait=False)

    svc = sb.service(port=8000)

    # Open in a browser
    print(svc.browser_url)

    # Or make requests with built-in helpers (auth is injected automatically)
    resp = svc.get("/api/data")
    resp = svc.post("/api/data", json={"key": "value"})
```

更多细节，包括使用场景、REST API 访问和完整的 FastAPI 示例，请参见 [Service URLs](/langsmith/sandbox-service-urls)。

## TCP tunnels（Python）

你可以像访问本地服务一样访问运行在 sandbox 中的任意 TCP 服务。隧道会打开一个本地 TCP 端口，并通过 WebSocket 将连接转发到 sandbox 内的目标端口。

```python
import psycopg2

# Snapshot built from the official postgres:16 image
sb = client.create_sandbox(snapshot_id=postgres_snapshot_id)
pg_handle = sb.run(
    "POSTGRES_HOST_AUTH_METHOD=trust docker-entrypoint.sh postgres",
    timeout=0,
    wait=False,
)
import time; time.sleep(6)  # Wait for Postgres to start

try:
    with sb.tunnel(remote_port=5432, local_port=25432) as t:
        conn = psycopg2.connect(
            host="127.0.0.1",
            port=t.local_port,
            user="postgres",
        )
        cursor = conn.cursor()
        cursor.execute("SELECT version()")
        print(cursor.fetchone())
        conn.close()
finally:
    pg_handle.kill()
    client.delete_sandbox(sb.name)
```

隧道适用于任何 TCP 服务（如 Redis、HTTP servers 等），你也可以同时打开多个隧道：

```python
with sb.tunnel(remote_port=5432, local_port=25432) as t1, \
     sb.tunnel(remote_port=6379, local_port=26379) as t2:
    # Use both Postgres and Redis simultaneously
    pass
```

## Async 支持（Python）

Python SDK 提供了完整的异步客户端：

```python
from langsmith.sandbox import AsyncSandboxClient

async def main():
    async with AsyncSandboxClient() as client:
        async with await client.sandbox() as sb:
            result = await sb.run("python -c 'print(1 + 1)'")
            print(result.stdout)  # "2\n"

            await sb.write("/app/test.txt", "async content")
            content = await sb.read("/app/test.txt")
            print(content.decode())

            # Async streaming
            handle = await sb.run("make build", timeout=600, wait=False)
            async for chunk in handle:
                print(chunk.data, end="")
            result = await handle.result

            # Async service URLs
            svc = await sb.service(port=8000)
            resp = await svc.get("/api/data")
            url = await svc.get_service_url()
            token = await svc.get_token()
```

## 追踪 sandbox 内部活动

通过 `run()` 的 `env` 参数把 LangSmith tracing 环境变量传进去，就可以将 sandbox 内部运行代码产生的 traces 发送到 LangSmith。请在进程退出前调用 `flush()`，确保所有 traces 都已发出。

<CodeGroup>

```python Python
from langsmith.sandbox import SandboxClient

client = SandboxClient()

tracing_env = {
    "LANGSMITH_API_KEY": "lsv2_pt_...",
    "LANGSMITH_ENDPOINT": "https://api.smith.langchain.com",
    "LANGSMITH_TRACING": "true",
    "LANGSMITH_PROJECT": "my-sandbox-traces",
}

with client.sandbox() as sandbox:
    sandbox.run("pip install langsmith", timeout=120, env=tracing_env)
    result = sandbox.run("python3 my_agent.py", env=tracing_env)
    print(result.stdout)
```

```ts TypeScript
import { SandboxClient } from "langsmith/sandbox";

const client = new SandboxClient();

const tracingEnv = {
  LANGSMITH_API_KEY: "lsv2_pt_...",
  LANGSMITH_ENDPOINT: "https://api.smith.langchain.com",
  LANGSMITH_TRACING: "true",
  LANGSMITH_PROJECT: "my-sandbox-traces",
};

const sandbox = await client.createSandbox();
try {
  await sandbox.run("pip install langsmith", { timeout: 120, env: tracingEnv });
  const result = await sandbox.run("python3 my_agent.py", { env: tracingEnv });
  console.log(result.stdout);
} finally {
  await sandbox.delete();
}
```

</CodeGroup>

在 sandbox 内部，任何已经接入 LangSmith 的代码（如 `@traceable`、LangChain、LangGraph）都会自动从注入的环境变量中读取 tracing 配置。

<Warning>
在 sandbox 进程退出前，一定要调用 `flush()`，Python 中用 `langsmith.Client().flush()`，TypeScript 中用 `await new Client().flush()`。否则 traces 可能会丢失，因为命令结束时容器会被销毁。
</Warning>

## 错误处理

两个 SDK 都提供了带类型的异常，以便进行更精细的错误处理：

<CodeGroup>

```python Python
from langsmith.sandbox import (
    SandboxClientError,       # Base exception
    ResourceCreationError,    # Provisioning failed
    ResourceNotFoundError,    # Resource doesn't exist
    ResourceTimeoutError,     # Operation timed out
    SandboxNotReadyError,     # Sandbox not ready yet
    SandboxConnectionError,   # Network/WebSocket error
    CommandTimeoutError,      # Command exceeded timeout
    QuotaExceededError,       # Quota limit reached
)

try:
    with client.sandbox() as sb:
        result = sb.run("sleep 999", timeout=10)
except CommandTimeoutError as e:
    print(f"Command timed out: {e}")
except ResourceNotFoundError as e:
    print(f"{e.resource_type} not found: {e}")
except SandboxClientError as e:
    print(f"Error: {e}")
```

```ts TypeScript
import {
  LangSmithSandboxError,
  LangSmithResourceNotFoundError,
  LangSmithResourceTimeoutError,
  LangSmithSandboxConnectionError,
  LangSmithCommandTimeoutError,
  LangSmithQuotaExceededError,
} from "langsmith/sandbox";

try {
  const sandbox = await client.createSandbox("not-a-real-snapshot");
  await sandbox.delete();
} catch (e) {
  if (e instanceof LangSmithResourceNotFoundError) {
    console.log(`${e.resourceType} not found: ${e.message}`);
  } else if (e instanceof LangSmithResourceTimeoutError) {
    console.log(`Timeout waiting for ${e.resourceType}: ${e.message}`);
  } else if (e instanceof LangSmithSandboxError) {
    console.log(`Error: ${e.message}`);
  }
}
```

</CodeGroup>

<Note>
更多细节请参见 GitHub 上的 sandbox SDK reference：[Python](https://github.com/langchain-ai/langsmith-sdk/tree/main/python/langsmith/sandbox) 或 [TypeScript](https://github.com/langchain-ai/langsmith-sdk/tree/main/js/src/sandbox)。
</Note>
