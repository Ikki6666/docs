---
title: 条件式 tracing
sidebarTitle: 条件式 tracing
---

当你在全局设置了环境变量 `LANGSMITH_TRACING=true` 时，traces 会自动发送到 LangSmith。本指南介绍如何为特定请求有选择地关闭 tracing，或自定义 tracing 行为。

在以下场景中，你会需要条件式 tracing：

- **满足数据保留策略**：有些客户出于合规或隐私原因，要求完全零数据保留。
- **处理敏感操作**：对于涉及 PII、凭据或机密数据的操作，关闭 tracing。
- **实现按 tenant 定制配置**：根据不同客户，把 traces 路由到不同项目，或应用不同设置。
- **控制成本**：为低价值请求关闭 tracing，同时保留关键操作的可观测性。
- **配合 feature flags**：只有在特定功能或实验性代码路径启用时才打开 tracing。

<Tip>
如果你想通过只记录一部分 runs 来降低 trace 量，请参阅[为 traces 设置采样率](/langsmith/sample-traces)。
</Tip>

@[`tracing_context`][tracing_context] context manager（Python）和 @[`tracingEnabled`][tracingEnabled] 选项（TypeScript）都允许你在运行时覆盖全局 tracing 设置，无需重构代码，也不必修改环境变量。

<Note>
下面的章节分别提供了按语言区分的示例，你可以结合自己的应用逻辑和业务需求进行调整。
</Note>

<Tabs>
<Tab title="Python" icon="brand-python">

## tracing context 的工作方式

当你使用 @[`tracing_context`][tracing_context] context manager 时，它会覆盖其作用域内代码的全局 tracing 配置。这意味着你可以继续在全局打开自动 tracing，同时只针对特定函数调用有选择地控制 tracing 行为。

控制优先级分为三层：

1. **`tracing_context(enabled=...)`**：最高优先级，用于作用域内的 tracing 控制。
1. **`ls.configure(enabled=...)`**：全局配置，用于设置整体 tracing 行为。
1. **环境变量**：最低优先级，即 `LANGSMITH_TRACING`。

## 为特定调用关闭 tracing

如果你想为某个特定操作关闭 tracing，可以用 `enabled=False` 的 `tracing_context` 把它包起来：

```python
import langsmith as ls
from langsmith import traceable

# LANGSMITH_TRACING=true is set globally

@traceable
def my_function(input_text: str):
    return process(input_text)

# Default invocation - is traced
result = my_function("regular data")

# Disable tracing for sensitive data
with ls.tracing_context(enabled=False):
    result = my_function("sensitive data")  # not traced
```

这种方式适合你明确知道某些特定数据不应该被记录的场景。

## 基于业务逻辑启用条件式 tracing

你也可以根据运行时条件动态启用或关闭 tracing，例如客户配置或请求属性。

```python
import langsmith as ls
from langsmith import traceable

@traceable
def my_function(input_text: str):
    return process(input_text)

def client_requires_zero_retention(client_id: str) -> bool:
    """
    Check if a client has a zero-retention policy.

    In production, this would query a database, configuration service,
    or feature flag system. Consider caching results for performance.
    """
    # Example: Query from database or config
    zero_retention_clients = get_zero_retention_clients()  # Your implementation
    return client_id in zero_retention_clients

def handle_request(client_id: str, user_input: str):
    """
    Process a request with conditional tracing based on client requirements.
    """
    should_disable = client_requires_zero_retention(client_id)

    with ls.tracing_context(enabled=not should_disable):
        return my_function(user_input)

# Example usage
handle_request("client-a", "some input")  # Traced or not based on client settings
```

## 为每个请求自定义 tracing 配置

你还可以动态定制 tracing 配置，例如把 traces 路由到不同项目，或添加请求级别的 metadata。

```python
import langsmith as ls
from langsmith import traceable

@traceable
def my_function(input_text: str):
    return process(input_text)

def handle_request(client_id: str, user_input: str, region: str):
    """
    Route traces to client-specific projects with custom metadata.
    """
    client_tier = get_client_tier(client_id)  # e.g., "enterprise", "standard"

    with ls.tracing_context(
        enabled=True,
        project_name=f"client-{client_id}",
        tags=["production", f"tier-{client_tier}", f"region-{region}"],
        metadata={
            "client_id": client_id,
            "region": region,
            "tier": client_tier
        }
    ):
        return my_function(user_input)

# Traces go to "client-abc" project with custom tags and metadata
handle_request("abc", "some input", "us-west")
```

这种方式适合：

- **多租户应用**：把不同客户的 traces 隔离到各自项目中
- **多区域部署**：按地理区域追踪性能和行为
- **功能分支**：把实验性特性的 traces 路由到专用项目
- **用户分群**：按用户等级、cohort 或 A/B test 分组分析行为

## 配合自动 tracing 使用

@[`tracing_context`][tracing_context] context manager 可以和自动 tracing 一起使用。你可以继续在全局设置 `LANGSMITH_TRACING=true`，然后用 `tracing_context` 针对特定请求覆盖设置：

```python
import os
import langsmith as ls

# Global environment variable set
os.environ["LANGSMITH_TRACING"] = "true"

@ls.traceable
def process_data(data: str):
    return data.upper()

# Automatically traced (respects LANGSMITH_TRACING)
process_data("hello")

# Override global setting - disable for this call
with ls.tracing_context(enabled=False):
    process_data("sensitive")  # not traced

# Override global setting - enable with custom config
with ls.tracing_context(
    enabled=True,
    project_name="special-project"
):
    process_data("important")  # Traced to "special-project"
```

## 嵌套 tracing contexts

当你嵌套使用 `tracing_context` 时，最内层 context 会生效。

```python Python
import langsmith as ls

@ls.traceable
def inner_function(data: str):
    return data

@ls.traceable
def outer_function(data: str):
    # This call respects the inner context
    return inner_function(data)

# Outer context disables tracing
with ls.tracing_context(enabled=False):
    # But inner context re-enables it
    with ls.tracing_context(enabled=True):
        outer_function("data")  # is traced
```

如果你平时默认不记录 traces，但又希望在某个局部区域临时重新开启 tracing 进行调试，这种方式就很有用。

## 有条件地隐藏输入和输出

有时你希望 trace 仍然被记录下来，这样 run 的耗时、结构、错误和 metadata 仍然可用，但针对某些请求，输入和输出必须被隐藏，例如来自严格隐私要求租户的 traces。这和[完全关闭 tracing](#disable-tracing-for-specific-invocations)不同，也不同于 [`Client(hide_inputs=...)`](/langsmith/mask-inputs-outputs#hide-inputs-and-outputs) 这种对 client 发出的所有 traces 一刀切的隐藏方式。

要按请求做隐藏，可以使用带 `replicas` 参数的 @[`tracing_context`][tracing_context]，并传入一个 `updates` dict，用来覆写记录到 run 上的 `inputs` 和 `outputs`。由于 `tracing_context` 只作用于当前执行上下文，不同请求即便并发运行、使用不同隐藏策略，也不会互相干扰。

```python
import langsmith as ls
from langsmith import traceable

@traceable
def my_agent(user_input: str) -> str:
    return process(user_input)

def should_redact(tenant_id: str) -> bool:
    """Return True if traces for this tenant should have inputs/outputs masked."""
    return tenant_id in get_redacted_tenants()

def handle_request(tenant_id: str, user_input: str) -> str:
    replica: dict = {"project_name": "my-project"}
    if should_redact(tenant_id):
        # Recorded run will have empty inputs/outputs but full structure,
        # timing, metadata, and any errors.
        replica["updates"] = {"inputs": {}, "outputs": {}}

    with ls.tracing_context(replicas=[replica]):
        return my_agent(user_input)
```

你可以在 `updates` 中使用任意子集的 run fields，例如 `{"inputs": {"redacted": True}}` 用于保留一个标记，或 `{"outputs": {}}` 只隐藏 outputs。同样的方式也适用于把不同隐藏策略写入不同目标，每个 replica 都可以单独指定 `project_name`、`api_key` 和 `updates`。关于 replica 的完整说明，请参阅[使用 replicas 将 traces 写入多个目标](/langsmith/log-traces-to-project#write-traces-to-multiple-destinations-with-replicas)。

<Note>
当你用 `updates` 来隐藏 inputs 或 outputs 时，请务必在 replica 上设置 `project_name`。如果 replica 的 `project_name` 和当前会话的 active project 相同，`updates` 可能会被丢弃，导致未隐藏的 inputs / outputs 被直接发送出去。
</Note>

## 在已部署 agents 中自定义 tracing

在 LangSmith Deployment 的 [Agent Server](/langsmith/agent-server) 中，tracing 默认是开启的。当你使用 [factory function](/langsmith/graph-rebuild) 时，可以用 `tracing_context` 把 yield 出来的 graph 包起来，从而按每次执行控制 tracing。它适合用于添加自定义 metadata、完全关闭 tracing，或根据认证后的用户身份自定义 tracing。

### 为 graph 关闭 tracing

```python
import contextlib
import langsmith as ls
from langgraph_sdk.runtime import ServerRuntime


@contextlib.asynccontextmanager
async def make_graph(runtime: ServerRuntime):
    graph = build_my_graph()

    # You can use tracing_context to dynamically enable/disable tracing,
    # set metadata or tags, override the tracing project, etc.
    with ls.tracing_context(enabled=False, metadata={"foo": "bar"}):
        yield graph
```

### 按用户进行 tracing

```python
import contextlib
import langsmith as ls
from langgraph_sdk.runtime import ServerRuntime

def get_project_for_user(user_id: str) -> str | None:
    ...
    return "my-project"

graph = build_my_graph()

@contextlib.asynccontextmanager
async def make_graph(runtime: ServerRuntime):
    user = runtime.user
    # Route traces to a different project depending on user or disable tracing entirely
    project_name = get_project_for_user(user.identity)

    if project_name is None:
        with ls.tracing_context(enabled=False):
            yield graph
    else:
        with ls.tracing_context(
            enabled=True,
            project_name=project_name,
            metadata={"user_id": user.identity, "foo": "bar"},
        ):
            yield graph
```

## 可复用的 tracing wrapper

你可以创建一个 decorator，自动应用条件式 tracing 逻辑。

```python Python
import functools
import langsmith as ls
from langsmith import traceable

def conditional_trace(check_function):
    """
    Decorator that conditionally traces based on a check function.

    Args:
        check_function: Function that returns True if tracing should be enabled
    """
    def decorator(func):
        traced_func = traceable(func)

        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            should_trace = check_function(*args, **kwargs)
            with ls.tracing_context(enabled=should_trace):
                return traced_func(*args, **kwargs)
        return wrapper
    return decorator

# Usage
def should_trace_client(client_id: str, *args, **kwargs) -> bool:
    return not client_requires_zero_retention(client_id)

@conditional_trace(should_trace_client)
def process_request(client_id: str, data: str):
    return data.upper()

# Automatically applies conditional tracing based on client_id
process_request("client-a", "some data")
```

</Tab>
<Tab title="TypeScript" icon="brand-typescript">

## tracing enabled 的工作方式

在 TypeScript 中，你通过调用 @[`traceable()`][traceable] 时传入的 @[`tracingEnabled`][tracingEnabled] 参数，为每个函数单独控制 tracing。这样你就可以在函数粒度上有选择地启用或关闭 tracing。

整体上只有两层控制：

1. **`tracingEnabled` 参数**：最高优先级，通过 @[`traceable()`][traceable] 的配置传入。
1. **环境变量**：最低优先级，即 `LANGSMITH_TRACING`。

## 为特定调用关闭 tracing

如果你想为某个特定操作关闭 tracing，可以创建一个带 `tracingEnabled: false` 的 traceable 版本：

```typescript
import { traceable } from "langsmith/traceable";

const myFunction = traceable(
    (inputText: string) => {
        return process(inputText);
    },
    { name: "my_function" }
);

// Default invocation - is traced
await myFunction("regular data");

// Disable tracing for sensitive data
const myFunctionNoTrace = traceable(
    (inputText: string) => {
        return process(inputText);
    },
    { name: "my_function", tracingEnabled: false }
);

await myFunctionNoTrace("sensitive data");  // not traced
```

这种方式适合那些你明确知道不应该记录的特殊数据。

## 基于业务逻辑启用条件式 tracing

在很多应用里，你都需要根据运行时条件动态控制 tracing，比如客户隐私要求、合规规则或 feature flags。

在 TypeScript 中，最有效率的做法，是预先创建 traced 和 non-traced 两个函数版本，然后在运行时根据业务逻辑挑一个使用。这样可以避免每次请求都重新创建 traced wrapper 的性能开销，同时仍然保留足够细粒度的 tracing 控制。例如：

```typescript
import { traceable } from "langsmith/traceable";

// Define the core logic once
function processText(inputText: string): string {
    // Your actual processing logic
    return inputText.toUpperCase();
}

// Create traced and non-traced variants upfront
const myFunction = traceable(processText, { name: "my_function" });
const myFunctionNoTrace = traceable(processText, {
    name: "my_function",
    tracingEnabled: false
});

function clientRequiresZeroRetention(clientId: string): boolean {
    /**
     * Check if a client has a zero-retention policy.
     *
     * In production, this would query a database, configuration service,
     * or feature flag system. Consider caching results for performance.
     */
    const zeroRetentionClients = getZeroRetentionClients();  // Your implementation
    return zeroRetentionClients.includes(clientId);
}

async function handleRequest(clientId: string, userInput: string) {
    /**
     * Process a request with conditional tracing based on client requirements.
     * Efficiently selects pre-created traced or non-traced variant.
     */
    const shouldDisable = clientRequiresZeroRetention(clientId);

    // Select the appropriate pre-created variant
    const fn = shouldDisable ? myFunctionNoTrace : myFunction;
    return await fn(userInput);
}

// Example usage
await handleRequest("client-a", "some input");  // Traced or not based on client settings
```

## 配合自动 tracing 使用

@[`tracingEnabled`][tracingEnabled] 选项可以与自动 tracing 无缝配合。你可以继续在全局设置 `LANGSMITH_TRACING=true`，再用 `tracingEnabled` 去覆盖某些特定函数的行为。

```typescript
import { traceable } from "langsmith/traceable";

// Global tracing enabled via environment
process.env.LANGSMITH_TRACING = "true";

const processData = traceable(
    (data: string) => {
        return data.toUpperCase();
    },
    { name: "process_data" }
);

// Automatically traced (respects LANGSMITH_TRACING)
await processData("hello");

// Override global setting - disable for this call
const processDataNoTrace = traceable(
    (data: string) => {
        return data.toUpperCase();
    },
    { name: "process_data", tracingEnabled: false }
);

await processDataNoTrace("sensitive");  // not traced

// Override global setting - enable with custom config
const processDataCustom = traceable(
    (data: string) => {
        return data.toUpperCase();
    },
    {
        name: "process_data",
        project_name: "special-project",
        tracingEnabled: true
    }
);

await processDataCustom("important");  // Traced to "special-project"
```

</Tab>
</Tabs>

## 与 sampling 的区别

条件式 tracing 和 [sampling](/langsmith/sample-traces) 用途并不相同：

| Feature | Conditional tracing | Sampling |
|---------|-------------------|----------|
| **Control** | 确定性的，显式开启或关闭 | 概率性的，随机采样 |
| **Use case** | 业务逻辑、合规要求、按请求决策 | 成本优化、高流量场景下的可观测性 |
| **Predictability** | 对特定请求有保证的行为 | 只提供流量的统计性代表样本 |
| **Configuration** | 运行时代码逻辑 | 环境变量或 client 配置 |

你也可以把两者组合起来使用，以获得更细粒度的控制。

## Related

- [Trace without environment variables](/langsmith/trace-without-env-vars)：通过编程方式配置 tracing，而不是使用环境变量。
- [Set a sampling rate for traces](/langsmith/sample-traces)：通过概率采样来降低 trace 量
- [Mask inputs and outputs](/langsmith/mask-inputs-outputs)：隐藏 traces 中的敏感数据，而不是完全关闭 tracing。
- [Add metadata and tags to traces](/langsmith/add-metadata-tags)：使用自定义属性对 traces 进行分类和过滤。
