---
title: Quickstart
description: 从模板构建一个 agent
---

在这个 quickstart 中，你将使用预构建的 **Executive Assistant** [template](/langsmith/fleet/templates)，它可以帮你管理收件箱、日历和每日简报。

<Callout icon="message" color="#8B5CF6" iconType="regular">
你将通过聊天与 agent 交互，就像给一个乐于助人的助手发消息一样。
</Callout>

## 开始之前

你需要准备：
- 一个 LangSmith 账号（[在这里注册](https://smith.langchain.com/agents?skipOnboarding=true)）。
- 一个 Gmail 账号。
- 一个 Google calendar。
- 一个 OpenAI 或 Anthropic API key（第 1 步会说明如何获取）。

## 1. 获取模型 API key

你的 agent 需要一个 API key 才能连接到 AI 模型。AI 模型负责理解并响应你的请求。

<Tabs>
  <Tab title="OpenAI (ChatGPT)">
    1. 前往 [platform.openai.com/api-keys](https://platform.openai.com/api-keys)。
    1. 点击 **Create new secret key**。
    1. 给它起一个名字，例如 “Fleet”。
    1. 复制这个 key（它以 `sk-` 开头）。
    1. 把它保存在安全的地方，第 2 步会用到。
  </Tab>

  <Tab title="Anthropic (Claude)">
    1. 前往 [console.anthropic.com/settings/keys](https://console.anthropic.com/settings/keys)。
    2. 点击 **Create Key**。
    3. 给它起一个名字，例如 “Fleet”。
    4. 复制这个 key（它以 `sk-ant-` 开头）。
    5. 把它保存在安全的地方，第 2 步会用到。
  </Tab>
</Tabs>

<Warning>
这两项服务都会按用量收费。
</Warning>

## 2. 把你的 API key 添加到 LangSmith

接下来你需要把 API key 添加到 LangSmith，这样 agents 才能使用它：

<Steps>
  <Step title="Open Settings">
    1. 打开 [smith.langchain.com](https://smith.langchain.com)。
    2. 点击左下角的 <Icon icon="settings" /> **Settings** 图标。
  </Step>

  <Step title="Go to Secrets">
    点击顶部的 **Secrets** 标签页。
  </Step>

  <Step title="Add your key">
    1. 点击 **Add secret**。
    2. 在 **Key** 中填写：
       - `OPENAI_API_KEY`（如果你使用 OpenAI）
       - `ANTHROPIC_API_KEY`（如果你使用 Anthropic）
    3. 在 **Value** 中粘贴你在第 1 步复制的 API key。
    4. 点击 **Save secret**。
  </Step>
</Steps>

<Callout type="success" icon="check" color="#10B981" iconType="regular">
现在你的 agent 已经可以访问 AI 模型来理解和响应请求了。接下来，你将创建自己的 agent。
</Callout>

## 3. 创建你的 agent

<Steps>
  <Step title="Navigate to Fleet">
    1. 在 [LangSmith UI](https://smith.langchain.com) 中，点击左侧导航顶部的 <Icon icon="pointer"/> **Switch to Fleet**。
  </Step>

  <Step title="Choose a template">
    1. 在左侧导航中选择 **Templates**，或者点击 **My Agents** 中的 **+** 并选择 **From template**。
    1. 选择 **Executive Assistant** 模板来创建你的 agent。

    <Tip>
    如果你不想从模板开始，在创建 agent 时选择 **Build with AI**，并描述你想要的 agent。该 agent 会自行配置，并在关键节点暂停以获取你的输入。
    </Tip>
  </Step>

  <Step title="Authorize accounts">
    你的 agent 会请求你连接 Google 账号：

    1. 点击 **Connect**。
    2. 使用你的 Google 账号登录。
    3. 查看权限说明并点击 **Allow**。
    4. 你会被重定向回 LangSmith，此时 agent 会被创建。
  </Step>

</Steps>

<Info>
只有在执行你明确交给它的任务时，agent 才会访问你的账号。你也可以随时在 Google 账号设置中撤销访问权限。
</Info>

## 4. 查看 agent 模板

<Steps>

  <Step title="View and customize the template">

    这时你可以查看 Executive Assistant 的模板 instructions。如有需要，也可以对 instructions 做调整。

    如果你做了修改，请点击 **Save changes**。
  </Step>

  <Step title="Start a test chat">
    在 agent 聊天中，试用这个 Executive Assistant，例如：

        > _Apply a "Review" label to emails that I receive, which require some kind of review from me_
  </Step>

  <Step title="Agent starts working">
    agent 会开始工作，并在每一步需要你批准时提供 **Continue** 选项。

    <img
    className="block dark:hidden"
    src="/langsmith/images/agent-builder-response.png"
    alt="Test chat output view with response including approvals for Gmail tool."
    />

    <img
    className="hidden dark:block"
    src="/langsmith/images/agent-builder-response-dark.png"
    alt="Test chat output view with response including approvals for Gmail tool."
    />

    在测试 agent 的过程中，你可以继续修改 instructions，或添加你需要的工具。确认效果满意后，点击 **Save changes**。

  </Step>

</Steps>

## 编辑你的 agent

你可能还想更新 agent 的 instructions，或者增加更多工具。你既可以直接通过聊天让 agent 帮你更新，也可以通过 [agent sidebar](/langsmith/fleet/essentials#agent-sidebar) 来配置它：

- 在 **Connections** 抽屉中添加集成和工具，并将每个工具设为自动运行或[需要审批](/langsmith/fleet/essentials#human-in-the-loop)。请参阅 [Tools](/langsmith/fleet/tools)。
- 在 **Channels** 抽屉中连接 [Slack](/langsmith/fleet/slack-app)、[Gmail](/langsmith/fleet/channels#add-a-gmail-channel) 或 [Microsoft Teams](/langsmith/fleet/teams-app)。
- 在 **Schedule** 抽屉中按 [schedule](/langsmith/fleet/schedules) 运行你的 agent。
- 在 **Advanced settings** 抽屉中更换 [model](/langsmith/fleet/manage-agent-settings#change-the-model)。

## 下一步

现在你已经创建了第一个 agent，可以继续探索这些内容：

<CardGroup cols={2}>
  <Card title="Try more templates" icon="layout-grid" href="/langsmith/fleet/templates">
    探索适用于常见任务的预构建 agents
  </Card>

  <Card title="Add automation" icon="bolt" href="/langsmith/fleet/essentials#channels">
    通过 channels（Slack、邮件、schedules）让 agent 自动运行
  </Card>

  <Card title="Connect more tools" icon="puzzle" href="/langsmith/fleet/tools">
    添加 Slack、GitHub、Linear 等更多工具
  </Card>

  <Card title="Build complex agents" icon="sitemap" href="/langsmith/fleet/essentials#sub-agents">
    使用 sub-agents 拆解大型任务
  </Card>
</CardGroup>
