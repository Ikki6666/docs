---
title: Add a custom search tool, memory, and a schedule
sidebarTitle: 构建定时 agent
description: 把 provider 搜索替换为 Tavily tool，然后为 quickstart 中的研究助手添加持久 memory 和每日 schedule。
---

import ManagedDeepAgentsPrivateBetaNote from '/snippets/langsmith/managed-deep-agents-private-beta-note.mdx';

本教程从 [quickstart](/langsmith/managed-deep-agents-quickstart) 继续。使用你在那里创建的 `research-assistant` 项目，包括你的 model、instructions 和可用的 `mda dev` 配置。

`mda init` 可能还会生成 `identity` 和 `sandbox/` 等文件。保持它们不变即可；本教程不会改动这些文件。

本指南会把 quickstart 中内置的 provider 搜索替换为一个已编写的 [Tavily](https://tavily.com) 搜索 tool，启用持久 memory，添加每日 schedule，然后部署。

<ManagedDeepAgentsPrivateBetaNote />

## 扩展 agent

<Steps>
  <Step title="添加自定义搜索 tool" id="add-tool">

内置的 provider 搜索便于首次运行。已编写的 tools 给你更多控制权：可以选择搜索 API、调整参数，并把 tool 代码保留在你的项目中。

<Note>
如果你已经在 [Quickstart](/langsmith/managed-deep-agents-quickstart) 中按步骤使用了 Tavily，请跳到下一步。
</Note>

将 [Tavily API key](https://app.tavily.com) 添加到 `.env`：

```text .env
TAVILY_API_KEY=<TAVILY_API_KEY>
```

安装 Tavily 客户端：

:::python
```bash
uv add tavily-python
```
:::

:::js
```bash
npm install @langchain/tavily
```
:::

创建一个自定义 `internet_search` tool：

:::python
```python tools/search.py
import os
from typing import Literal

from langchain.tools import tool
from tavily import TavilyClient


tavily_client = TavilyClient(api_key=os.environ["TAVILY_API_KEY"])


@tool
def internet_search(
    query: str,
    max_results: int = 5,
    topic: Literal["general", "news", "finance"] = "general",
) -> dict:
    """Search the internet for relevant sources."""
    return tavily_client.search(
        query,
        max_results=max_results,
        topic=topic,
    )
```
:::

:::js
```ts tools/search.ts
import { TavilySearch } from "@langchain/tavily";
import { tool } from "langchain";
import { z } from "zod";

export const internetSearch = tool(
  async ({ query, maxResults = 5, topic = "general" }) => {
    const tavilySearch = new TavilySearch({
      maxResults,
      tavilyApiKey: process.env.TAVILY_API_KEY,
      topic,
    });
    return tavilySearch._call({ query });
  },
  {
    name: "internet_search",
    description: "Search the internet for relevant sources.",
    schema: z.object({
      query: z.string().describe("The search query."),
      maxResults: z.number().optional().default(5),
      topic: z.enum(["general", "news", "finance"]).optional().default("general"),
    }),
  },
);
```
:::

用你编写的 tool 替换 provider 搜索 tool dict。保留 quickstart 中的 `model` 值：

:::python
```python agent.py
from managed_deepagents import define_deep_agent

from tools.search import internet_search

agent = define_deep_agent(
    name="research-assistant",
    model="openai:gpt-5.5",
    tools=[internet_search],
)
```
:::

:::js
```ts agent.ts
import { defineDeepAgent } from "managed-deepagents";

import { internetSearch } from "./tools/search";

export const agent = defineDeepAgent({
  name: "research-assistant",
  model: "openai:gpt-5.5",
  tools: [internetSearch],
});
```
:::

如果 `mda dev` 已经在运行，请重启它。在 Studio 中提问：

```txt wrap
What were the main announcements from the latest LangChain release?
```

确认 agent 调用了 `internet_search` 并返回了带引用的答案。关于更多已编写的 tools，请参见 [Custom tools](/langsmith/managed-deep-agents-tools)。

  </Step>

  <Step title="为 memory 更新 instructions" id="instructions">

扩展 `instructions.md`，让 agent 知道需要保留哪些共享知识。保留研究行为，并添加一个 memory 策略：

```markdown instructions.md
# Research assistant

You are a careful research assistant. Use internet search to find sources,
keep notes, and return concise answers with citations.

## Memory

- Record reusable research procedures and project knowledge that can improve future work.
- For release research, check the project's official changelog before secondary sources.
- Never store personal data or secrets in memory.
```
  </Step>

  <Step title="启用并使用持久 memory" id="memory">

持久 memory 是 opt-in 的。在让 agent 记住任何内容之前，请先在项目根目录下添加一个 memory 声明：

:::python
```python memory.py
from managed_deepagents import define_memory

memory = define_memory(scope="agent")
```
:::

:::js
```ts memory.ts
import { defineMemory } from "managed-deepagents";

export const memory = defineMemory({ scope: "agent" });
```
:::

Memory 在整个 deployment 范围内共享，对所有调用方可见，因此不要存储个人数据或机密信息。

重启 `mda dev` 以发现新文件。在某个 thread 中，让 agent 研究一次发布，并记录一条可复用的项目规则，例如 "For release research, check the official changelog before secondary sources." 然后在 Studio 中创建一个 **new thread**，询问它将如何研究下一次发布。确认即使新 thread 没有任何对话历史，它也会应用这条共享规则。

详情请参见 [Memory](/langsmith/managed-deep-agents-memory)。

  </Step>

  <Step title="安排每日摘要" id="schedule">

添加一个 `schedules/` 模块，让 agent 可以在没有用户消息的情况下按 cron 节奏运行。下面这个 schedule 会在太平洋时间每个工作日早上 8 点运行：

:::python
```python schedules/daily_digest.py
from managed_deepagents import define_schedule

schedule = define_schedule(
    cron="0 8 * * 1-5",
    timezone="America/Los_Angeles",
    prompt=(
        "Review durable memory for reusable research rules. "
        "Summarize anything useful, then list open questions for today."
    ),
)
```
:::

:::js
```ts schedules/daily-digest.ts
import { defineSchedule } from "managed-deepagents";

export const schedule = defineSchedule({
  cron: "0 8 * * 1-5",
  timezone: "America/Los_Angeles",
  prompt:
    "Review durable memory for reusable research rules. " +
    "Summarize anything useful, then list open questions for today.",
});
```
:::

如果首次触发时 memory 为空，agent 仍会返回开放问题。

在 deployment 可用后，`mda deploy` 会把这个 schedule reconcile 成 LangSmith cron job。在你完成下一步部署后，你应该会看到：

- `mda deploy` 完成，没有 schedule 错误（不要传入 `--no-wait`，否则 schedules 不会被 reconcile）。
- 该 deployment 上存在一个对应此文件的托管 cron。Schedule 名称与模块名一致：`daily_digest`（Python）或 `daily-digest`（TypeScript）。
- 该 cron 不会立即运行摘要。首次触发会等到工作日太平洋时间 8:00。

关于 thread 行为和限制，请参见 [Schedules](/langsmith/managed-deep-agents-schedules)。

  </Step>

  <Step title="部署并检查" id="deploy">

将项目部署到 LangSmith：

```bash
mda deploy .
```

成功后，CLI 会打印 deployment dashboard URL。Deploy 会把 instructions 同步到 Context Hub，上传编译后的项目，并对每日 schedule 执行 reconcile。

打开该 URL 并确认：

- Deployment 处于 ready 状态。
- 存在 `daily_digest` 或 `daily-digest` cron。
- 一次测试对话 run 在 traces 中展示了模型调用、`internet_search` tool 调用，以及 memory 的读写。

关于 deploy flags 和故障排查，请参见 [Deploy an agent](/langsmith/managed-deep-agents-deploy) 与 [CLI reference](/langsmith/managed-deep-agents-cli#deploy-projects)。

  </Step>
</Steps>

## 下一步

<CardGroup cols={2}>
  <Card title="Custom middleware" icon="code" href="/langsmith/managed-deep-agents-middleware">
    添加围绕模型与工具调用的日志、重试、限制和 guardrails。
  </Card>
  <Card title="Identity" icon="fingerprint" href="/langsmith/managed-deep-agents-identity">
    认证调用方，并在 tools 和 middleware 中使用已验证的 identity。
  </Card>
  <Card title="Evals" icon="flask" href="/langsmith/managed-deep-agents-evals">
    编写 Harbor tasks，并为 Harbor 编译托管 agent。
  </Card>
</CardGroup>
