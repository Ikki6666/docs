---
title: 使用 annotation queues
sidebarTitle: 使用 annotation queues
---

_Annotation queues_ 为人工审核者提供了一套聚焦的工作流，用于给特定的[runs](/langsmith/observability-concepts#runs)或[threads](/langsmith/observability-concepts#threads)附加反馈。虽然你始终可以直接在[traces](/langsmith/observability-concepts#traces)中内联标注，但 annotation queues 允许你把 runs 和 threads 组织成组、预设 rubric，并跟踪审核者进度。审核整个 thread 可以让你评估一段完整的多轮对话，捕获单个 run 无法反映的质量信号。

<Info>
你也可以通过 SDK 以编程方式管理 annotation queues 和 feedback configs。请参阅[以编程方式管理反馈与 annotation queues](/langsmith/annotation-queues-sdk)。
</Info>

要自定义 run 输出在审核期间的显示方式，请[为 annotation queues 配置自定义输出渲染](/langsmith/custom-output-rendering#for-annotation-queues)。

LangSmith 支持两种 queue 样式：

- [**Single-run annotation queues**](#single-run-annotation-queues) 每次展示一个 queue 条目，可以是一个 run 或一个 thread，并允许审核者提交你配置好的任意 rubric 反馈。对于 **run** 条目，single-run queues 还支持使用 [assertions](/langsmith/assertions) 捕获离线评估的验收标准。
- [**Pairwise annotation queues (PAQs)**](#pairwise-annotation-queues) 并排展示两个 runs，让审核者根据你定义的 rubric 项快速判断哪个输出更好，或者两者是否等价。

<Tip>
如果想看 annotation queues 的演示，请观看[Getting started with annotation queues](#video-guide)视频指南。
</Tip>

## Single-run annotation queues

Single-run queues 每次展示一个条目，并允许审核者提交你配置好的任意 rubric 反馈。你可以直接在 [LangSmith UI](https://smith.langchain.com) 的 **Annotation queues** 部分创建它们。一个 queue 可以混合包含 run 条目和 thread 条目。_Thread 条目_ 表示一整段对话，并使用与 run 条目相同的 rubric 进行审核。

Run 条目和 thread 条目支持不同的能力：

| 能力 | Run 条目 | Thread 条目 |
| --- | --- | --- |
| Rubric feedback | 是 | 是 |
| Reviewer notes | 是 | 否 |
| Assertions | 是 | 否 |
| Add to Dataset | 是 | 否 |
| Default dataset | 是 | 否 |
| Automation rules | 是 | 否 |

### 创建 single-run queue

1. 在左侧导航中进入 **Annotation Queues**。
1. 点击左上角的 **+ Annotation Queue**，打开 **Create Annotation Queue** 面板。

#### Basic details

1. 填写 queue 的 **Name** 和 **Description**。
1. 可选择指定一个 **Application**。
1. 可选择 **Select a default dataset**，这样在把审核后的 runs 导出为数据集时会更方便，该数据集位于你的 LangSmith [workspace](/langsmith/administration-overview#workspaces) 中。Default datasets 适用于你在 run 条目上使用 **Add to Dataset** 的情况；thread 条目不支持添加到数据集。

#### Annotation rubric

1. 为审核者编写一些高层级的 **Instructions**，这些内容会显示在每个条目的侧边栏中。
1. 点击 **+ Add a feedback rubric**，为 annotation queue 添加 feedback keys。审核者在每个条目上都会看到这些 feedback keys。
1. 为每个 feedback key 添加说明；如果反馈类型是分类式的，还要为每个类别添加简短说明。

    审核者会在 UI 右侧面板中看到 **Instructions** 和 **Feedback** 详情。

#### Collaborator settings

你可以设置每个条目的审核人数，或者设置条目被保留给某个协作者的最长时间。当一个条目需要多个标注者时，你可以选择让该条目在所有审核者都标记为 **Done** 之前一直保留在队列中。在这些设置中，"run" 指的是任意 queue 条目，包括 thread 条目。具体设置如下：

- **All workspace members review each run**：启用后，一个条目会一直留在队列中，直到每位 [workspace](/langsmith/administration-overview#workspaces) 成员都将自己的审核标记为 **Done**。

- **Enable reservations on runs**：为某个条目建立保留后，它会在指定时长内被锁定给你审核。条目被保留期间，其他审核者仍可查看，但不能添加反馈或备注。如果启用了所有 workspace 成员都审核每个 run，则不会启用 reservations。

    如果审核者查看了某个条目，但离开前没有将其标记为 **Done**，那么在指定的 **Reservation length** 之后，这个 reservation 会过期。该条目会重新释放回队列，供其他审核者保留。

    <Note>
        对某个条目的 annotation 点击 **Requeue**，只会将当前条目移到当前用户自己的队列末尾，不会影响其他用户的队列顺序。同时也会释放当前用户对该条目的 reservation。
    </Note>

- **Number of reviewers per run**：这决定了一个条目需要被多少位审核者标记为 **Done** 后，才会从队列中移除。

    - 审核者不能查看其他审核者留下的反馈。
    - 条目上的评论对所有审核者可见。

    <Note>
    启用 **Use assigned reviewers** 后，此项 **Number of reviewers per run** 设置会被隐藏（见下文）。
    </Note>

- **Use assigned reviewers**：启用此开关后，不再按人数阈值计算，而是使用指定的 workspace 成员。启用后：

    - 会出现一个多选用户选择器，让你选择具体的 workspace 成员作为指定审核者。
    - 只有当每位指定审核者都提交完审核后，该条目才会被标记为 **Completed**。队列条目会依次经历三种状态：**Needs Review** → **Needs Others' Review** → **Completed**。
    - 未被指定的 workspace 成员仍然可以标注条目，但他们的提交不会计入完成条件。
    - 任意 workspace 成员都可以在 queue settings 中编辑指定审核者列表。

    <Note>
    如果你为一个已经包含已完成条目的 queue 新增指定审核者，这些已完成条目不会重新变为待处理。如果移除某位指定审核者，则其尚未审核的条目会重新计算完成状态。
    </Note>

由于这些设置，不同审核者看到的条目数量可能与队列总大小不同。

### 编辑 queue 设置

1. 打开你想编辑的 annotation queue 的 **Edit Annotation Queue** 面板。你可以通过以下两种方式访问：

    - 在 **Annotation queues** 列表中，点击队列所在行右侧的 **Actions** 图标 <Icon icon="dots-vertical"/>。然后在下拉菜单中选择 <Icon icon="pencil"/> **Edit**。
    - 在 annotation queue 视图中，点击右上角的 **Settings** 图标 <Icon icon="settings"/>。

2. 在 **Edit Annotation Queue** 面板中，修改你在创建 queue 时配置的任意设置，然后点击 **Save**。

### 将 runs 和 threads 分配到 single-run queue

有多种方式可以向 single-run queue 中添加条目：

- **From the Details view**：在某个 [tracing project](/langsmith/observability-concepts#projects) 中，点击任意行打开 [Details view](/langsmith/view-traces#details-view) 中的侧边面板。在右上角点击 **+ Add**，然后选择 **Add to Annotation Queue**。在弹出框中，在 **What to add** 下选择 **Selected run**（当前 run）或 **Entire thread**（该 run 所属的完整对话）。

    你可以将任意中间 [run](/langsmith/observability-concepts#runs) 作为 run 条目添加，但不能添加 root run。**Entire thread** 要求该 run 属于某个 thread（通过 `thread_id` / `session_id` metadata 标注）。

    <img
      className="block dark:hidden"
      src="/langsmith/images/add-to-annotation-queue-what-to-add-light.png"
      alt="Add to Annotation Queue 弹出框，包含 Selected run 和 Entire thread 的 What to add 标签页，以及一个 queue 选择器。"
    />

    <img
      className="hidden dark:block"
      src="/langsmith/images/add-to-annotation-queue-what-to-add-dark.png"
      alt="Add to Annotation Queue 弹出框，包含 Selected run 和 Entire thread 的 What to add 标签页，以及一个 queue 选择器。"
    />

    <Note>
    如果 **Entire thread** 选项不可用，或者 **Threads** 标签页为空，说明这些 runs 没有通过 `thread_id` / `session_id` metadata 标注。
    </Note>

- **From the Traces or Runs tab**：在某个 tracing project 中，选择 **Traces** 或 **Runs** 标签页。使用行复选框选中一个或多个条目。点击页面底部的 **Add to Annotation Queue**。在 **What to add** 中，将每个选中项作为 **Selected run** 或其 **Entire thread** 加入队列。

    <img
      className="block dark:hidden"
      src="/langsmith/images/multi-select-annotation-queue-light.png"
      alt="runs 表格视图，其中已选中若干 runs。页面底部有 Add to Annotation Queue 按钮。"
    />

    <img
      className="hidden dark:block"
      src="/langsmith/images/multi-select-annotation-queue-dark.png"
      alt="runs 表格视图，其中已选中若干 runs。页面底部有 Add to Annotation Queue 按钮。"
    />

- **From the Threads tab**：在某个 tracing project 中，选择 **Threads** 标签页。使用行复选框选中一个或多个条目。点击页面底部的 **Add to Annotation Queue**。选中的 threads 会作为 thread 条目添加。

    <img
      className="block dark:hidden"
      src="/langsmith/images/threads-tab-add-to-annotation-queue-light.png"
      alt="Threads 标签页，其中已选中若干 threads，以及 Add to Annotation Queue 批量操作。"
    />

    <img
      className="hidden dark:block"
      src="/langsmith/images/threads-tab-add-to-annotation-queue-dark.png"
      alt="Threads 标签页，其中已选中若干 threads，以及 Add to Annotation Queue 批量操作。"
    />

- **Automation rules**：[设置规则](/langsmith/rules)，将匹配某个过滤条件的 **runs**（例如出错或用户评分较低）自动分配到某个 queue 中。

    <Note>
    Automation rules 只会将 run 条目加入队列，不会将整个 threads 作为 thread 条目添加。
    </Note>
- **Datasets & Experiments**：在某个数据集中选择一个或多个[实验](/langsmith/evaluation-concepts#experiment)，然后点击 **<Icon icon="pencil"/> Annotate**。选择 **Add to Annotation Queue**，然后选择已有 queue 或创建新 queue。实验标注流程只会添加 run 条目。

    <img
      className="block dark:hidden"
      src="/langsmith/images/annotate-experiment-light.png"
      alt="Selected experiments with the Annotate button at the bottom of the page."
    />

    <img
      className="hidden dark:block"
      src="/langsmith/images/annotate-experiment-dark.png"
      alt="Selected experiments with the Annotate button at the bottom of the page."
    />

<Note>
单次操作最多可以向 annotation queue 中添加 **100** 个 runs 或 threads。如需添加更多，请以每批 100 或更少的方式重复执行添加流程。

手动向 annotation queue 添加 runs 或 threads 默认不会改变 trace 保留期。trace 会保留为其 project 配置的保留期，除非另有操作显式延长保留期。完整的保留模型请参阅[data retention auto-upgrades](/langsmith/usage-and-billing#data-retention-auto-upgrades)。
</Note>

### 审核 single-run queue

1. 通过左侧导航进入 **Annotation Queues**。

    队列列表中包含 **Assigned Reviewers** 列，用来显示每个 queue 指定了哪些审核者。如果只想看分配给你的 queues，请点击列表顶部的 **Assigned to me** 过滤器。

1. 点击你想审核的 queue。随后会进入一个聚焦、循环的界面，展示该 queue 中需要审核的条目。左侧面板会列出 queue 条目（runs 和 threads），并显示每个条目的状态（**Needs Review**、**Needs Others' Review**、**Completed**）。使用 **View all items** 可打开完整队列列表。

1. 审核当前条目：

    - **Run 条目**：在中间面板中检查 inputs 和 outputs。添加 **Reviewer Notes**、为 [**Feedback**](/langsmith/observability-concepts#feedback) 条件打分，或者将该条目标记为已审核。若要构建数据集，可以编辑该 run 的输入和输出，创建修正后的参考示例，然后点击 **Add to Dataset**。与其手动编写修正后的参考输出，你也可以直接在审核侧边栏中[编写 **Assertions**](/langsmith/assertions)，并将其保存为该示例的期望输出。
    - **Thread 条目**：中间面板会显示该 thread 的对话 transcript。阅读 transcript 并为相同的 rubric **Feedback** keys 打分。使用 **View item** 可在对话 peek 中打开该 thread。

    点击 **Delete** 会将该条目从所有用户的队列中删除，无论当前的 reservations 或 queue settings 如何。

    <Note>
    Thread 条目仅支持 rubric feedback。关于 run 条目和 thread 条目的差异，请参见[能力表](#single-run-annotation-queues)。
    </Note>

    <img
      className="block dark:hidden"
      src="/langsmith/images/annotation-queue-thread-review-light.png"
      alt="Annotation queue 审核一个 thread 条目，显示对话 transcript 和 rubric feedback 面板。"
    />

    <img
      className="hidden dark:block"
      src="/langsmith/images/annotation-queue-thread-review-dark.png"
      alt="Annotation queue 审核一个 thread 条目，显示对话 transcript 和 rubric feedback 面板。"
    />

    在审核 annotation queue 期间提交的 feedback 和 notes 不会改变 trace 的[保留层级](/langsmith/usage-and-billing#data-retention-auto-upgrades)。

    <Tip>
        使用每个选项旁边的键盘快捷键可以更快地审核条目。
    </Tip>

## Pairwise annotation queues

Pairwise annotation queues（PAQs）会并排显示两个 runs，让审核者根据你定义的 rubric 项快速判断哪个输出更好，或者两者是否等价。它们专为两个实验之间的快速 A/B 比较而设计，常见用法是基线模型与候选模型对比。PAQs 必须从 **Datasets & Experiments** 页面创建。Pairwise queues 仅使用 run 比较，不会将 thread 条目加入队列。

### 创建 pairwise queue

1. 前往 **Datasets & Experiments**，打开某个数据集，并选择**恰好两个实验**进行比较。
1. 点击 **Annotate**。在弹出的界面中选择 **Add to Pairwise Annotation Queue**。（只有在刚好选择了两个实验时，此按钮才可用。）

    ![Popover showing the "Add to Pairwise Annotation Queue" card highlighted after two experiments are selected.](/langsmith/images/pairwise-annotation-queue-popup.png)

1. 决定是将实验发送到一个已有的 pairwise queue，还是创建一个新的。
1. 填写 queue 详情：
    - **Basic details**（名称和说明）
    - 适用于成对评分的 **Instructions & rubrics**
    - **Collaborator settings**（审核人数、reservations、reservation length）
1. 提交表单以创建 queue。LangSmith 会立即将两个实验中的 runs 配对，并把它们填充进该 queue。

PAQs 的关键差异在于：

- **Experiments**：你必须预先提供两个 experiment sessions。LangSmith 会按时间顺序自动配对它们的 runs，并在创建期间填充队列。
- **Rubric**：Pairwise rubric 项只需要 feedback key，以及可选的描述。审核者需要为每个 rubric 项判断 Run A、Run B，或两者是否同样更好。
- **Dataset**：Pairwise queues 不使用 default dataset，因为比较横跨两个实验。
- **Reservations & reviewers**：协作者控制逻辑与 single-run queue 相同。Reservations 有助于防止两个人同时评审同一组比较。

### 向 pairwise queue 添加更多比较项

如果你之后还需要添加更多比较项，请返回 **Datasets & Experiments**，再次选中那两个实验，然后选择 **Add to Pairwise Annotation Queue**，以便向现有 queue 追加新的 pairs。

选择两个实验并创建 PAQ 时，LangSmith 会自动完成配对。向现有 PAQ 增补时，LangSmith 会保留历史比较结果，并将新 pairs 追加到队列中。

### 审核 pairwise queue

1. 在 **Annotation queues** 中，选择你要审核的 pairwise queue。
1. 每个 queue 条目会在左侧显示 Run A，在右侧显示 Run B，同时展示你的 rubric。
1. 对于每个 rubric 项：
    - 选择 **A is better**、**B is better** 或 **Equal**。UI 会在后台为两个 runs 记录二值反馈。
    - 使用热键 `A`、`B` 或 `E` 快速完成选择。
1. 完成所有 rubric 项后，按下 **Done**（或者在最后一个 rubric 项上按 `Enter`）进入下一组比较。
1. 可选操作：
    - 留下绑定到任一 run 的评论。
    - 如果之后还要回看，可以 Requeue 该比较项。
    - 打开 Details view 进行更深入的调试。

Reservations、reviewer thresholds 和评论的行为与 single-run queues 完全一致，因此团队无需更改既有工作流，就可以切换使用不同类型的 queues。

    ![Pairwise review screen showing runs side-by-side with the feedback pane containing A/B/Equal buttons and keyboard shortcuts.](/langsmith/images/pairwise-annotation-queue-review-feedback-pane.png)

<Check>
可以考虑把已经带有用户反馈（例如 thumbs-down）的 runs，同时路由到一个 single-run queue 用于分诊，再路由到一个 pairwise queue 中，与更强的基线做 head-to-head 比较。这样可以帮助你更快识别回归。要了解如何从 LLM application 中采集用户反馈，请参阅[附加用户反馈](/langsmith/attach-user-feedback)指南。
</Check>

## 视频指南

<iframe
  className="w-full aspect-video rounded-xl"
  src="https://www.youtube.com/embed/rxKYHA-2KS0?si=V4EnrUmzJaUVJh0m"
  title="YouTube video player"
  frameBorder="0"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
  allowFullScreen
></iframe>
