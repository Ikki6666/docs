---
title: 使用 annotation queues
sidebarTitle: 使用 annotation queues
---

_Annotation queues_ 为人工审核者提供了一套聚焦的工作流，用于给特定的[runs](/langsmith/observability-concepts#runs)附加反馈。虽然你始终可以直接在[traces](/langsmith/observability-concepts#traces)中内联标注，但 annotation queues 允许你把 runs 组织成组、预设 rubric，并跟踪审核者进度。

<Info>
你也可以通过 SDK 以编程方式管理 annotation queues 和 feedback configs。请参阅[以编程方式管理反馈与 annotation queues](/langsmith/annotation-queues-sdk)。
</Info>

LangSmith 支持两种 queue 样式：

- [**Single-run annotation queues**](#single-run-annotation-queues) 每次展示一个 run，并允许审核者提交你配置好的任意 rubric 反馈。Single-run queues 还支持使用 [assertions](/langsmith/assertions) 捕获离线评估的验收标准。
- [**Pairwise annotation queues (PAQs)**](#pairwise-annotation-queues) 并排展示两个 runs，让审核者根据你定义的 rubric 项快速判断哪个输出更好，或者两者是否等价。

<Tip>
如果想看 annotation queues 的演示，请观看[Getting started with annotation queues](#video-guide)视频指南。
</Tip>

## Single-run annotation queues

Single-run queues 每次展示一个 run，并允许审核者提交你配置好的任意 rubric 反馈。你可以直接在 [LangSmith UI](https://smith.langchain.com) 的 **Annotation queues** 部分创建它们。

### 创建 single-run queue

1. 在左侧导航中进入 **Annotation Queues**。
1. 点击左上角的 **+ Annotation Queue**。

    ![Create Annotation Queue form with Basic Details, Annotation Rubric, and Feedback sections.](/langsmith/images/create-annotation-queue-new.png)

#### Basic details

1. 填写 queue 的 **Name** 和 **Description**。
1. 你可以选择性指定一个 **default dataset**，这样在把审核后的 runs 导出为数据集时会更方便，该数据集位于你的 LangSmith [workspace](/langsmith/administration-overview#workspaces) 中。

#### Annotation rubric

1. 为审核者编写一些高层级的 **Instructions**，这些内容会显示在每个 run 的侧边栏中。
1. 点击 **+ Add a feedback rubric**，为 annotation queue 添加 feedback keys。审核者在每个 run 上都会看到这些 feedback keys。
1. 为每个 feedback key 添加说明；如果反馈是分类式的，还要为每个类别添加简短说明。

    ![Annotation queue rubric form with instructions and desired feedback entered.](/langsmith/images/create-annotation-rubric.png)

    例如，在上图中的说明配置下，审核者会在 UI 右侧面板中看到 **Annotation Rubric** 详情。

    ![The rendered rubric for reviewers from the example instructions.](/langsmith/images/rubric-for-annotators.png)

#### Collaborator settings

你可以设置每个条目的审核人数，或者设置条目被保留给某个协作者的最长时间。当一个 run 需要多个标注者时，你可以选择让该 run 在所有审核者都标记为 **Done** 之前一直保留在队列中。具体设置如下：

- **All workspace members review each run**：启用后，一个 run 会一直留在队列中，直到每位 [workspace](/langsmith/administration-overview#workspaces) 成员都将自己的审核标记为 **Done**。

- **Enable reservations on runs**：为某个 run 建立保留后，它会在指定时长内被锁定给你审核。run 被保留期间，其他审核者仍可查看，但不能添加反馈或备注。如果启用了所有 workspace 成员都审核每个 run，则不会启用 reservations。

    如果审核者查看了某个 run，但离开前没有将其标记为 **Done**，那么在指定的 **Reservation length** 之后，这个 reservation 会过期。该 run 会重新释放回队列，供其他审核者保留。

    <Note>
        对某个 run 的 annotation 点击 **Requeue**，只会将当前 run 移到当前用户自己的队列末尾，不会影响其他用户的队列顺序。同时也会释放当前用户对该 run 的 reservation。
    </Note>

- **Number of reviewers per run**：这决定了一个 run 需要被多少位审核者标记为 **Done** 后，才会从队列中移除。

    - 审核者不能查看其他审核者留下的反馈。
    - Runs 上的评论对所有审核者可见。

    <Note>
    启用 **Use assigned reviewers** 后，此项 **Number of reviewers per run** 设置会被隐藏（见下文）。
    </Note>

- **Use assigned reviewers**：启用此开关后，不再按人数阈值计算，而是使用指定的 workspace 成员。启用后：

    - 会出现一个多选用户选择器，让你选择具体的 workspace 成员作为指定审核者。
    - 只有当每位指定审核者都提交完审核后，该 run 才会被标记为 **Completed**。队列条目会依次经历三种状态：**Needs Review** → **Needs Others' Review** → **Completed**。
    - 未被指定的 workspace 成员仍然可以标注 runs，但他们的提交不会计入完成条件。
    - 任意 workspace 成员都可以在 queue settings 中编辑指定审核者列表。

    <Note>
    如果你为一个已经包含已完成条目的 queue 新增指定审核者，这些已完成条目不会重新变为待处理。如果移除某位指定审核者，则其尚未审核的条目会重新计算完成状态。
    </Note>

由于这些设置，不同审核者看到的 runs 数量可能与队列总大小不同。

### 编辑 queue 设置

1. 打开你想编辑的 annotation queue 的 **Edit Annotation Queue** 面板。你可以通过以下两种方式访问：

    - 在 **Annotation queues** 列表中，点击队列所在行右侧的 **Actions** 图标 <Icon icon="dots-vertical"/>。然后在下拉菜单中选择 <Icon icon="pencil"/> **Edit**。
    - 在 annotation queue 视图中，点击右上角的 **Settings** 图标 <Icon icon="settings"/>。

2. 在 **Edit Annotation Queue** 面板中，修改你在创建 queue 时配置的任意设置，然后点击 **Save**。

### 将 runs 分配到 single-run queue

有多种方式可以向 single-run queue 中添加工作项：

- **From the Details view**：在任意 trace 的右上角点击 **Add to Annotation Queue**。你可以添加任意中间 [run](/langsmith/observability-concepts#runs)，但不能添加 root span。

    ![Trace view with the Add to Annotation Queue button highlighted at the top of the screen.](/langsmith/images/add-to-annotation-queue.png)

- **From the runs table**：选中多个 runs，然后点击页面底部的 **Add to Annotation Queue**。

    ![View of the runs table with runs selected. Add to Annotation Queue button at the bottom of the page.](/langsmith/images/multi-select-annotation-queue.png)

- **Automation rules**：[设置规则](/langsmith/rules)，将匹配某个过滤条件的 runs（例如出错或用户评分较低）自动分配到某个 queue 中。
- **Datasets & experiments**：在某个数据集中选择一个或多个[实验](/langsmith/evaluation-concepts#experiment)，然后点击 **<Icon icon="pencil"/> Annotate**。选择已有 queue 或创建新 queue，然后确认使用（single-run）queue 选项。

    ![Selected experiments with the Annotate button at the bottom of the page.](/langsmith/images/annotate-experiment.png)

### 审核 single-run queue

1. 通过左侧导航进入 **Annotation Queues**。

    队列列表中包含 **Assigned Reviewers** 列，用来显示每个 queue 指定了哪些审核者。如果只想看分配给你的 queues，请点击列表顶部的 **Assigned to me** 过滤器。

1. 点击你想审核的 queue。随后会进入一个聚焦、循环的界面，展示该 queue 中需要审核的 runs。左侧面板会显示每个 run 的状态（**Needs Review**、**Needs Others' Review**、**Completed**）。

1. 添加 **Reviewer Notes**、为 [**Feedback**](/langsmith/observability-concepts#feedback) 条件打分，或者将该 run 标记为已审核。若要构建数据集，可以编辑该 run 的输入和输出，创建修正后的参考示例，然后点击 **Add to Dataset**。点击 **Delete** 会将该 run 从所有用户的队列中删除，无论当前的 reservations 或 queue settings 如何。

    与其手动编写修正后的参考输出，你也可以直接在审核侧边栏中[编写 assertions](/langsmith/assertions)，并将其保存为该示例的期望输出。

    <Tip>
        每个选项旁边的键盘快捷键可以帮助你更高效地完成审核流程。
    </Tip>

## Pairwise annotation queues

Pairwise annotation queues（PAQs）会并排显示两个 runs，让审核者根据你定义的 rubric 项快速判断哪个输出更好，或者两者是否等价。它们专为两个实验之间的快速 A/B 比较而设计，常见用法是基线模型与候选模型对比。PAQs 必须从 **Datasets & Experiments** 页面创建。

### 创建 pairwise queue

1. 前往 **Datasets & Experiments**，打开某个数据集，并选择**恰好两个实验**进行比较。
1. 点击 **Annotate**。在弹出的界面中选择 **Add to Pairwise Annotation Queue**。（只有在刚好选择了两个实验时，此按钮才可用。）

    ![Popover showing the "Add to Pairwise Annotation Queue" card highlighted after two experiments are selected.](/langsmith/images/pairwise-annotation-queue-popup.png)

1. 决定是将实验发送到一个已有的 pairwise queue，还是创建一个新的。
1. 填写 queue 详情：
    - **Basic details**（名称和说明）
    - 适用于成对评分的 **Instructions & rubrics**
    - **Collaborator settings**（审核人数、reservations、reservation length）
1. 提交表单以创建 queue。LangSmith 会立即将两个实验中的 runs 配对，并把它们填充进该 queue。

PAQs 的关键差异在于：

- **Experiments**：你必须预先提供两个 experiment sessions。LangSmith 会按时间顺序自动配对它们的 runs，并在创建期间填充队列。
- **Rubric**：Pairwise rubric 项只需要 feedback key，以及可选的描述。审核者需要为每个 rubric 项判断 Run A、Run B，或两者是否同样更好。
- **Dataset**：Pairwise queues 不使用 default dataset，因为比较横跨两个实验。
- **Reservations & reviewers**：协作者控制逻辑与 single-run queue 相同。Reservations 有助于防止两个人同时评审同一组比较。

### 向 pairwise queue 添加更多比较项

如果你之后还需要添加更多比较项，请返回 **Datasets & Experiments**，再次选中那两个实验，然后选择 **Add to Pairwise Annotation Queue**，以便向现有 queue 追加新的 pairs。

选择两个实验并创建 PAQ 时，LangSmith 会自动完成配对。向现有 PAQ 增补时，LangSmith 会保留历史比较结果，并将新 pairs 追加到队列中。

### 审核 pairwise queue

1. 在 **Annotation queues** 中，选择你要审核的 pairwise queue。
1. 每个 queue 条目会在左侧显示 Run A，在右侧显示 Run B，同时展示你的 rubric。
1. 对于每个 rubric 项：
    - 选择 **A is better**、**B is better** 或 **Equal**。UI 会在后台为两个 runs 记录二值反馈。
    - 使用热键 `A`、`B` 或 `E` 快速完成选择。
1. 完成所有 rubric 项后，按下 **Done**（或者在最后一个 rubric 项上按 `Enter`）进入下一组比较。
1. 可选操作：
    - 留下绑定到任一 run 的评论。
    - 如果之后还要回看，可以 Requeue 该比较项。
    - 打开 Details view 进行更深入的调试。

Reservations、reviewer thresholds 和评论的行为与 single-run queues 完全一致，因此团队无需更改既有工作流，就可以切换使用不同类型的 queues。

    ![Pairwise review screen showing runs side-by-side with the feedback pane containing A/B/Equal buttons and keyboard shortcuts.](/langsmith/images/pairwise-annotation-queue-review-feedback-pane.png)

<Check>
可以考虑把已经带有用户反馈（例如 thumbs-down）的 runs，同时路由到一个 single-run queue 用于分诊，再路由到一个 pairwise queue 中，与更强的基线做 head-to-head 比较。这样可以帮助你更快识别回归。要了解如何从 LLM application 中采集用户反馈，请参阅[附加用户反馈](/langsmith/attach-user-feedback)指南。
</Check>

## 视频指南

<iframe
  className="w-full aspect-video rounded-xl"
  src="https://www.youtube.com/embed/rxKYHA-2KS0?si=V4EnrUmzJaUVJh0m"
  title="YouTube video player"
  frameBorder="0"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
  allowFullScreen
></iframe>
