---
title: 设置 coding agents
description: 配置 Claude Code、Codex、Gemini CLI 和 Deep Agents Code，让它们通过 LLM Gateway 路由 LLM 调用。
---

<Note>
**Beta：**LLM Gateway 当前处于 [beta](/langsmith/release-stages) 阶段。
</Note>

配置 coding agents 使用标准 LLM Gateway endpoint，以实现集中化的成本控制、可观测性和审计轨迹。gateway 会对每个调用方进行身份验证，按 model ID 路由，执行策略，并记录 trace。

Claude Code 可以使用标准 Anthropic Messages 格式，Codex 和 Deep Agents Code 可以使用标准 OpenAI 兼容格式。Gemini CLI 使用 Google 原生 API，需要[直接模型访问](/langsmith/llm-gateway-direct-model-access)。

## 前置条件

- 你的 [Organization admin](/langsmith/rbac#organization-admin) 已启用 gateway，并完成了所有必需的[provider 设置](/langsmith/llm-gateway-admin-setup)。
- 你拥有一个工作区级别的 [LangSmith API key](/langsmith/create-account-api-key)，并具备 `gateway:invoke` 和 `workspaces:read` [permissions](/langsmith/organization-workspace-operations)。
- 对于 bring-your-own-key 模型，你的工作区需要具备对应的 provider secret。[Gateway Credits 模型](/langsmith/llm-gateway-credits)不需要 provider secret。

在配置客户端之前，请设置你的 LangSmith API key：

```bash
export LANGSMITH_API_KEY="lsv2_..._....cbed3e"
```

## Claude Code CLI

将 Claude Code 指向标准 Messages endpoint，并使用带 provider 前缀的 model ID：

```bash
export ANTHROPIC_BASE_URL="https://gateway.smith.langchain.com"
export ANTHROPIC_API_KEY="$LANGSMITH_API_KEY"
export ANTHROPIC_MODEL="anthropic/claude-opus-5"

claude
```

Claude Code 会自动在 `ANTHROPIC_BASE_URL` 后追加 `/v1/messages`。gateway 使用 `anthropic/` 前缀来解析工作区的 Anthropic provider secret。

<Warning>
一旦配置了 gateway，Claude Desktop plugins 会失效。当前也还不支持已付费套餐（Plus、Max）的 Claude 用户。
</Warning>

## Codex CLI

Codex 使用 Responses API。将以下内容添加到 `~/.codex/config.toml`，通过标准 endpoint 以 Gateway Credits 调用托管的 Kimi K3 模型：

```toml
model = "moonshotai/kimi-k3"
model_provider = "langsmith-gateway"

[model_providers.langsmith-gateway]
name = "LangSmith Gateway"
base_url = "https://gateway.smith.langchain.com/v1"
env_key = "LANGSMITH_API_KEY"
wire_api = "responses"
supports_websockets = false
```

然后运行：

```bash
codex
```

要改用 bring-your-own-key 模型，请将 `model` 替换为其带 provider 前缀的 ID，例如 `openai/gpt-5.4-mini`。

<Warning>
一旦配置了 gateway，Codex Desktop plugins 会失效。由于 TOML 配置强制通过 gateway 完成身份验证，OpenAI 将不再直接处理 plugin 的认证。
</Warning>

## Gemini CLI

Gemini CLI 发送 Google 原生的 Generate Content 请求，标准 endpoint 不暴露该请求。请按[直接模型访问](/langsmith/llm-gateway-direct-model-access#configure-provider-sdks)中的说明配置 `/gemini` 路由，然后运行：

```bash
gemini
```

## Deep Agents Code

使用 OpenAI 兼容客户端配合标准 endpoint，然后通过 `openai` integration 传入托管 model slug：

```bash
export OPENAI_BASE_URL="https://gateway.smith.langchain.com/v1"
export OPENAI_API_KEY="$LANGSMITH_API_KEY"

dcode --model openai:moonshotai/kimi-k3
```

要改用 bring-your-own-key 模型，请保持标准 base URL 不变，并在 `openai:` 之后传入带 provider 前缀的 model，例如 `openai:anthropic/claude-opus-5`。关于 provider 原生 integrations 和 model ID，请参见[直接模型访问](/langsmith/llm-gateway-direct-model-access#configure-langchain-and-deep-agents)。

## 全公司范围部署

如果你的组织希望把 gateway 推给所有开发者使用，可以通过移动设备管理或共享 shell profile 来分发配置。需要分发的内容有：

1. 每个客户端的标准 gateway base URL。
1. 每位用户或团队一个工作区级别的 [LangSmith API key](/langsmith/create-account-api-key)，具体取决于你的策略粒度。
1. 为每个 coding agent 批准的 model ID。
1. 如果你的组织使用 Codex，还需要分发 Codex 的 `config.toml`。

provider API keys 仍然会集中保存在 LangSmith 工作区密钥中。Gateway Credits 模型不需要 provider API keys。

## 验证设置

配置好 coding agent 后，执行一次测试调用，并确认：

1. 调用成功，也就是 agent 收到了响应
1. 你的 LangSmith 工作区中的 `gateway` 或 `gateway-<short_api_key>-<api_key_id>` tracing project 里出现了新的 trace

如果调用因 `403` 失败，请检查你的 API key 所属角色是否包含 `gateway:invoke` 和 `workspaces:read`。如果 bring-your-own-key 调用因为缺失 provider key 而返回 `400`，请让 organization admin 把对应 provider 的 key 添加到工作区密钥中。

## 下一步

- [Gateway Credits](/langsmith/llm-gateway-credits)：无需 provider secret 即可调用托管模型
- [直接模型访问](/langsmith/llm-gateway-direct-model-access)：为需要它的 coding agents 配置 provider 原生路由
- [支出策略](/langsmith/llm-gateway-spend-policies)：为开发者使用 LLM 设置成本限制
- [Traces、Engine 和访问控制](/langsmith/llm-gateway-access)：了解 gateway traces 会出现在哪里
