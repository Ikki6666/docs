---
title: LangGraph 概览
sidebarTitle: 概览
description: 使用 LangGraph 掌控全局，设计能够可靠处理复杂任务的智能体
---

受到塑造智能体未来的公司信赖——包括 Klarna、Uber、摩根大通等——LangGraph 是一个低层级编排框架和运行时，用于构建、管理和部署长时间运行的有状态智能体。

LangGraph 非常底层，完全专注于智能体**编排**。在使用 LangGraph 之前，建议你先熟悉构建智能体的一些组件，从[模型](/oss/langchain/models)和[工具](/oss/langchain/tools)开始。

我们会在文档中频繁使用 [LangChain](/oss/langchain/overview) 组件来集成模型和工具，但你不必使用 LangChain 即可使用 LangGraph。如果你刚开始接触智能体或想要更高层级的抽象，建议你使用 LangChain 的[智能体](/oss/langchain/agents)，它为常见的 LLM 和工具调用循环提供了预构建的架构。

LangGraph 专注于智能体编排所需的核心底层能力：持久化执行、流式传输、人机交互等。

<Expandable title="LangChain 产品如何协同工作" defaultOpen={false}>

- [Deep Agents](/oss/deepagents/overview) 是一个[智能体框架套件](/oss/concepts/products#agent-harnesses-like-the-deep-agents-sdk)：在 LangGraph 之上提供规划、子智能体、文件系统工具和上下文管理。
- [LangChain](/oss/langchain/overview) 是智能体框架：提供模型、工具和智能体循环的抽象与集成。
- [LangGraph](/oss/langgraph/overview) 是编排运行时：提供持久化执行、流式传输、人机交互和状态持久化。
- [LangSmith](/langsmith/observability) 是跨框架的追踪、评估、提示词和部署平台。
- [LangSmith Engine](/langsmith/engine) 可检测你的 LangGraph 智能体追踪记录中的问题并提出修复方案。你可以直接从 Engine 标签页发起包含修复方案的拉取请求。
- [LangSmith Fleet](/langsmith/fleet/index) 是无代码智能体构建器，用于模板、集成和例行自动化。

阅读[框架、运行时和套件](/oss/concepts/products)，了解开源技术栈的对比。
</Expandable>

## <Icon icon="download" size={20} /> 安装

:::python
<CodeGroup>
```bash pip
pip install -U langgraph
```

```bash uv
uv add langgraph
```
</CodeGroup>
:::

:::js
<CodeGroup>
```bash npm
npm install @langchain/langgraph @langchain/core
```

```bash pnpm
pnpm add @langchain/langgraph @langchain/core
```

```bash yarn
yarn add @langchain/langgraph @langchain/core
```

```bash bun
bun add @langchain/langgraph @langchain/core
```
</CodeGroup>
:::

然后，创建一个简单的 hello world 示例：

:::python
```python
from langgraph.graph import StateGraph, MessagesState, START, END

def mock_llm(state: MessagesState):
    return {"messages": [{"role": "ai", "content": "hello world"}]}

graph = StateGraph(MessagesState)
graph.add_node(mock_llm)
graph.add_edge(START, "mock_llm")
graph.add_edge("mock_llm", END)
graph = graph.compile()

graph.invoke({"messages": [{"role": "user", "content": "hi!"}]})
```
:::

:::js
```typescript
import { StateSchema, MessagesValue, type GraphNode, StateGraph, START, END } from "@langchain/langgraph";

const State = new StateSchema({
  messages: MessagesValue,
});

const mockLlm: GraphNode<typeof State> = (state) => {
  return { messages: [{ role: "ai", content: "hello world" }] };
};

const graph = new StateGraph(State)
  .addNode("mock_llm", mockLlm)
  .addEdge(START, "mock_llm")
  .addEdge("mock_llm", END)
  .compile();

await graph.invoke({ messages: [{ role: "user", content: "hi!" }] });
```
:::

<Tip>
使用 [LangSmith](/langsmith/observability) 来追踪请求、调试智能体行为并评估输出。设置 `LANGSMITH_TRACING=true` 和你的 API 密钥即可开始使用。按照[追踪快速入门](/langsmith/trace-with-langchain)进行设置。我们建议你还设置 [LangSmith Engine](/langsmith/engine)，它可以监控你的追踪记录、检测问题并提出修复方案。
</Tip>

## 核心优势

LangGraph 为*任何*长时间运行的有状态工作流或智能体提供底层支撑基础设施。LangGraph 不会抽象化提示词或架构，而是提供以下核心优势：

* [持久化](/oss/langgraph/persistence)：构建能够从容应对故障并长时间运行的智能体，支持从中断处恢复执行。
* [人机交互](/oss/langgraph/interrupts)：通过在任意节点检查和修改智能体状态，融入人工监督。
* [全面的记忆能力](/oss/concepts/memory)：创建具有短期工作记忆（用于持续推理）和跨会话长期记忆的有状态智能体。
* [使用 LangSmith 调试](/langsmith/observability)：通过可视化工具深入洞察复杂的智能体行为，追踪执行路径、捕获状态转换并提供详细的运行时指标。
* [生产就绪的部署](/langsmith/deployment)：借助专为处理有状态、长时间运行工作流的独特挑战而设计的可扩展基础设施，自信地部署复杂的智能体系统。

## LangGraph 生态系统

虽然 LangGraph 可以独立使用，但它也能与任何 LangChain 产品无缝集成，为开发者提供构建智能体的全套工具。为了改进你的 LLM 应用开发，可以将 LangGraph 与以下产品配合使用：

<Columns cols={1}>
    <Card title="LangSmith 可观测性" icon="/images/brand/observability-icon-dark.png" href="/langsmith/observability" arrow cta="了解更多">
        在一个平台中追踪请求、评估输出并监控部署。使用 LangGraph 进行本地原型开发，然后借助集成的可观测性和评估能力走向生产环境，构建更可靠的智能体系统。
    </Card>

    <Card title="LangSmith 部署" icon="/images/brand/deployment-icon-dark.png" href="/langsmith/deployment" arrow cta="了解更多">
        使用专为长时间运行的有状态工作流打造的部署平台，轻松部署和扩展智能体。跨团队发现、复用、配置和共享智能体，并利用 Studio 中的可视化原型快速迭代。
    </Card>

    <Card title="LangChain" icon="/images/brand/langchain-icon.png" href="/oss/langchain/overview" arrow cta="了解更多">
        提供集成和可组合组件，简化 LLM 应用开发。包含构建在 LangGraph 之上的智能体抽象。
    </Card>
</Columns>

## 致谢

LangGraph 的灵感来自 [Pregel](https://research.google/pubs/pub37252/) 和 [Apache Beam](https://beam.apache.org/)。公开接口的设计灵感来自 [NetworkX](https://networkx.org/documentation/latest/)。LangGraph 由 LangChain Inc（LangChain 的创建者）开发，但可以脱离 LangChain 独立使用。
