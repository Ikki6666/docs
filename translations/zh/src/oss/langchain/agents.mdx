---
title: 智能体
---

import AgentInvocationThreadAndContextJs from '/snippets/code-samples/agent-invocation-thread-and-context-js.mdx';
import AgentInvocationThreadAndContextPy from '/snippets/code-samples/agent-invocation-thread-and-context-py.mdx';
import AgentInvocationThreadIdJs from '/snippets/code-samples/agent-invocation-thread-id-js.mdx';
import AgentInvocationThreadIdPy from '/snippets/code-samples/agent-invocation-thread-id-py.mdx';
import AgentsContextManagementJs from '/snippets/code-samples/agents-context-management-js.mdx';
import AgentsContextManagementPy from '/snippets/code-samples/agents-context-management-py.mdx';
import AgentsExecutionEnvironmentJs from '/snippets/code-samples/agents-execution-environment-js.mdx';
import AgentsExecutionEnvironmentPy from '/snippets/code-samples/agents-execution-environment-py.mdx';
import AgentsFaultToleranceJs from '/snippets/code-samples/agents-fault-tolerance-js.mdx';
import AgentsFaultTolerancePy from '/snippets/code-samples/agents-fault-tolerance-py.mdx';
import AgentsGuardrailsJs from '/snippets/code-samples/agents-guardrails-js.mdx';
import AgentsGuardrailsPy from '/snippets/code-samples/agents-guardrails-py.mdx';
import AgentsIntroJs from '/snippets/code-samples/agents-intro-js.mdx';
import AgentsIntroPy from '/snippets/code-samples/agents-intro-py.mdx';
import AgentsModelJs from '/snippets/code-samples/agents-model-js.mdx';
import AgentsModelPy from '/snippets/code-samples/agents-model-py.mdx';
import AgentsNameJs from '/snippets/code-samples/agents-name-js.mdx';
import AgentsNamePy from '/snippets/code-samples/agents-name-py.mdx';
import AgentsPlanningDelegationJs from '/snippets/code-samples/agents-planning-delegation-js.mdx';
import AgentsPlanningDelegationPy from '/snippets/code-samples/agents-planning-delegation-py.mdx';
import AgentsSteeringJs from '/snippets/code-samples/agents-steering-js.mdx';
import AgentsSteeringPy from '/snippets/code-samples/agents-steering-py.mdx';
import AgentsStreamingProgressJs from '/snippets/code-samples/agents-streaming-progress-js.mdx';
import AgentsStreamingProgressPy from '/snippets/code-samples/agents-streaming-progress-py.mdx';
import AgentsStructuredOutputJs from '/snippets/code-samples/agents-structured-output-js.mdx';
import AgentsStructuredOutputPy from '/snippets/code-samples/agents-structured-output-py.mdx';
import AgentsSystemPromptJs from '/snippets/code-samples/agents-system-prompt-js.mdx';
import AgentsSystemPromptPy from '/snippets/code-samples/agents-system-prompt-py.mdx';
import AgentsToolsJs from '/snippets/code-samples/agents-tools-js.mdx';
import AgentsToolsPy from '/snippets/code-samples/agents-tools-py.mdx';

智能体是一个模型，它会循环调用工具，直到完成给定任务。

<img
    src="/oss/images/core_agent_loop.svg"
    alt="核心智能体循环图"
    style={{height: "300px", width: "auto", justifyContent: "center"}}
    className="rounded-lg block mx-auto"
/>

<Note>
**智能体 = 模型 + 框架**

框架的职责：在给定任务中，在正确的时间为模型提供正确的上下文。
</Note>

框架是循环周围的一切：模型、提示词、工具，以及任何塑造其行为的中间件。

@[`create_agent`] 是一个高度可配置的框架。最简单的情况下，你可以这样创建：

:::python

<AgentsIntroPy />

:::
:::js

<AgentsIntroJs />

:::

在此基础上，你可以直接使用 `model=`、`tools=` 和 `system_prompt=` 参数配置基础组件。对于更高级的能力，请使用[中间件](#configure-the-harness)扩展框架。

## 核心组件

<img
    src="/oss/images/agent_model_harness.svg"
    alt="智能体模型和框架组件图"
    style={{height: "280px", width: "auto", justifyContent: "center"}}
    className="rounded-lg block mx-auto"
/>

### 模型

传入模型标识符字符串（`"provider:model"`）或已初始化的模型实例，为智能体选择模型。请参阅[模型](/oss/langchain/models)，了解参数、提供商设置和动态模型选择。

:::python

<AgentsModelPy />

:::
:::js

<AgentsIntroJs />

:::

### 工具

要为智能体提供工具，请传入任意 Python callable、LangChain 工具或工具字典。请参阅[工具](/oss/langchain/tools)，了解工具定义、上下文访问和动态工具选择。

:::python

<AgentsToolsPy />

:::
:::js

<AgentsToolsJs />

:::

### 系统提示词

塑造智能体处理任务的方式。系统提示词参数接受字符串或 `SystemMessage`。对于运行时的动态提示词，请使用[中间件](/oss/langchain/middleware)。

:::python

<AgentsSystemPromptPy />

:::
:::js

<AgentsSystemPromptJs />

:::

### 结构化输出

使用 `response_format=` 从智能体返回经过验证的 schema。请参阅[结构化输出](/oss/langchain/structured-output)，了解策略和示例。

:::python

<AgentsStructuredOutputPy />

:::
:::js

<AgentsStructuredOutputJs />

:::

## 调用

<Tip>
使用 [LangSmith](https://smith.langchain.com) 追踪此循环的每一步、调试工具调用，并评估智能体输出。按照[追踪快速入门](/langsmith/trace-with-langchain)进行设置。我们还建议你设置 [LangSmith Engine](/langsmith/engine)，它会监控你的追踪记录、检测问题并提出修复建议。
</Tip>


:::python
你可以用一条消息调用智能体。在幕后，这会向智能体的 [`State`](/oss/langgraph/graph-api#state) 传入一次更新。所有智能体的状态都包含一个[消息序列](/oss/langgraph/use-graph-api#messagesstate)；要调用智能体，请传入一条新消息以及 `thread_id`，让智能体能够持久化并恢复对话历史：
:::
:::js
你可以用一条消息调用智能体。在幕后，这会向智能体的 [`State`](/oss/langgraph/graph-api#state) 传入一次更新。所有智能体的状态都包含一个[消息序列](/oss/langgraph/use-graph-api#messagesvalue)；要调用智能体，请传入一条新消息以及 `thread_id`，让智能体能够持久化并恢复对话历史：
:::

:::python

<AgentInvocationThreadIdPy />

:::
:::js

<AgentInvocationThreadIdJs />

:::

<Note>
使用 `thread_id` 持久化对话历史要求智能体配置有 [checkpointer](/oss/langchain/long-term-memory)。部署到 [LangSmith](/langsmith/deployment) 时，系统会自动预置 checkpointer。在本地，请显式传入一个 checkpointer，例如 `create_agent(..., checkpointer=InMemorySaver())`。
</Note>

:::python
如果还需要向工具和中间件传递单次运行配置（例如用户 ID、API key 或功能标志），请将其作为 `context` 与 `config` 一起传入。使用 `context_schema` 定义这些数据的形状，并通过 `runtime.context` 访问：

<AgentInvocationThreadAndContextPy />

`thread_id` 限定的是*对话*范围（消息历史、checkpoint），而 `context` 携带的是工具和中间件在调用时读取的*单次运行*数据。二者通常一起传入。请参阅[工具上下文](/oss/langchain/tools#context)和 [Runtime](/oss/langchain/runtime) 了解更多信息。
:::
:::js
如果还需要向工具和中间件传递单次运行配置（例如用户 ID、API key 或功能标志），请将其作为 `context` 与 config 一起传入。使用 `contextSchema` 定义这些数据的形状，并通过 `runtime.context` 访问：

<AgentInvocationThreadAndContextJs />

`thread_id` 限定的是*对话*范围（消息历史、checkpoint），而 `context` 携带的是工具和中间件在调用时读取的*单次运行*数据。二者通常一起传入。请参阅[工具上下文](/oss/langchain/tools#context)和 [Runtime](/oss/langchain/runtime) 了解更多信息。
:::

## 流式传输

`invoke` 会在一次运行结束时返回最终响应。如果智能体执行多个工具调用，用户通常需要在完成前看到进度更新。使用流式传输，可以在中间消息和工具活动发生时将它们展示出来。

:::python

<AgentsStreamingProgressPy />

:::
:::js

<AgentsStreamingProgressJs />

:::

<Tip>
有关流式传输模式、事件类型和 UI 模式，请参阅[流式传输](/oss/langchain/streaming)。
</Tip>

## 配置框架

`create_agent` 具有很强的扩展性。中间件是自定义的基础单元：每个中间件负责一个关注点，在正确时机接入智能体循环，并且可以与任何其他中间件自由组合。只选择你的用例需要的部分，跳过其余部分。

常见模式已作为一等中间件预构建。其他任何能力都可以作为[自定义中间件](/oss/langchain/middleware/custom)构建。

<img
    src="/oss/images/agent_harness_capabilities.svg"
    alt="按类别划分的智能体框架能力"
    style={{height: "300px", width: "auto", justifyContent: "center"}}
    className="rounded-lg block mx-auto"
/>

随着智能体承担复杂工作，它们需要在几个关键领域获得支持。中间件生态系统提供：


<CardGroup cols={2}>
  <Card title="执行环境" icon="bolt" href="#execution-environment">
    工具、文件系统、沙箱和代码执行
  </Card>
  <Card title="上下文管理" icon="database" href="#context-management">
    摘要、记忆、技能和提示词缓存
  </Card>
  <Card title="规划和委派" icon="sitemap" href="#planning-and-delegation">
    用于并行隔离工作的待办事项列表和子智能体
  </Card>
  <Card title="容错" icon="shield" href="#fault-tolerance">
    重试、回退和调用限制
  </Card>
  <Card title="护栏" icon="lock" href="#guardrails">
    PII 检测和内容控制
  </Card>
  <Card title="引导" icon="user" href="#steering">
    在高影响操作前进行 human-in-the-loop 审批
  </Card>
</CardGroup>

<Tip>
`create_deep_agent` 为长时间运行的编码和研究任务预先组装了这套栈（默认包含文件系统、摘要、子智能体和提示词缓存）。请参阅 [Deep Agents](/oss/deepagents/harness)，了解完整的预构建框架。
</Tip>

### 执行环境

当智能体可以采取行动，而不只是生成文本时，它们尤其有用。执行环境为智能体提供工作区：可调用的工具、用于跨轮次读写文件的文件系统，以及用于运行脚本或 shell 命令的代码执行能力。

:::python

<AgentsExecutionEnvironmentPy />

:::
:::js

<AgentsExecutionEnvironmentJs />

:::

请参阅 @[`FilesystemMiddleware`]、[沙箱](/oss/deepagents/sandboxes)、[解释器](/oss/deepagents/interpreters)。

### 上下文管理

每次模型调用都有固定的上下文窗口。智能体运行时，这个窗口会逐渐填入累积的历史记录、工具结果和中间步骤。摘要会在溢出之前压缩历史记录；记忆会在启动时加载持久化指令，让知识可以跨会话保留；技能则按需暴露领域知识，而不是预先加载所有内容。

:::python

<AgentsContextManagementPy />

:::
:::js

<AgentsContextManagementJs />

:::

请参阅 @[`SummarizationMiddleware`]、@[`MemoryMiddleware`]、[技能](/oss/langchain/multi-agent/skills)、[上下文工程](/oss/deepagents/context-engineering)。

### 规划和委派

复杂任务通常会超出单个上下文窗口可处理的范围。委派让主智能体把工作拆分成多个部分，交给各自在隔离上下文中运行的子智能体，并专注于协调而不是执行。工作可以并行运行；主智能体的上下文保持清爽。

:::python
<AgentsPlanningDelegationPy />
:::
:::js
<AgentsPlanningDelegationJs />
:::

请参阅[子智能体](/oss/langchain/multi-agent/subagents)。

### 命名智能体

可以选择为智能体使用一个标识符。当把智能体作为子图嵌入[多智能体](/oss/langchain/multi-agent)系统时，这尤其有用。

:::python

<AgentsNamePy />

:::
:::js

<AgentsNameJs />

:::

### 容错

生产环境中的智能体会遇到开发中很少出现的故障：速率限制、模型超时、瞬时 API 错误。容错中间件在基础设施层处理这些问题，这样你的工具和业务逻辑不需要在每次调用周围都写 try/catch。

:::python
<AgentsFaultTolerancePy />
:::
:::js
<AgentsFaultToleranceJs />
:::

:::python
请参阅 @[`ModelRetryMiddleware`]、@[`ToolRetryMiddleware`]、[预构建中间件](/oss/langchain/middleware/built-in)。
:::

:::js
请参阅 @[`modelRetryMiddleware`]、@[`toolRetryMiddleware`]、[预构建中间件](/oss/langchain/middleware/built-in)。
:::

### 护栏

有些策略不能只放在提示词里，它们需要以确定性的方式强制执行，不受模型行为影响。护栏会拦截智能体循环中的数据流，在工具结果进入模型上下文之前应用合规规则或内容策略。

:::python
<AgentsGuardrailsPy />
:::
:::js
<AgentsGuardrailsJs />
:::

:::python
请参阅 @[`PIIMiddleware`]、[预构建中间件](/oss/langchain/middleware/built-in)。
:::

:::js
请参阅 @[`piiMiddleware`]、[预构建中间件](/oss/langchain/middleware/built-in)。
:::

### 引导

完全自主并不总是合适。引导让你在特定决策点安排人类参与，例如破坏性写入、昂贵 API 调用或任何需要判断的操作之前，而不必重构智能体。智能体会暂停并等待；人类批准、编辑或拒绝；随后执行继续。

:::python
<AgentsSteeringPy />
:::
:::js
<AgentsSteeringJs />
:::

:::python
请参阅 @[`HumanInTheLoopMiddleware`]、[human-in-the-loop](/oss/langchain/human-in-the-loop)。
:::

:::js
请参阅 @[`humanInTheLoopMiddleware`]、[human-in-the-loop](/oss/langchain/human-in-the-loop)。
:::

### 中间件资源

<CardGroup cols={3}>
  <Card title="中间件概览" icon="route" href="/oss/langchain/middleware/overview" arrow>
    中间件栈如何工作以及 hook 何时触发
  </Card>
  <Card title="预构建中间件" icon="package" href="/oss/langchain/middleware/built-in" arrow>
    包含配置示例的完整参考
  </Card>
  <Card title="自定义中间件" icon="code" href="/oss/langchain/middleware/custom" arrow>
    为业务逻辑、PII 清理等编写你自己的 hook
  </Card>
</CardGroup>
