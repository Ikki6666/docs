---
title: MESSAGE_COERCION_FAILURE
---

当消息对象不符合预期格式时，就会出现此错误。

:::python
## 可接受的消息格式

LangChain 模块接受 `MessageLikeRepresentation`，其定义如下：

```python
from typing import Union

from langchain_core.prompts.chat import (
    BaseChatPromptTemplate,
    BaseMessage,
    BaseMessagePromptTemplate,
)

MessageLikeRepresentation = Union[
    Union[BaseMessagePromptTemplate, BaseMessage, BaseChatPromptTemplate],
    tuple[
        Union[str, type],
        Union[str, list[dict], list[object]],
    ],
    str,
]
```

这包括 OpenAI 风格的消息对象（`{ role: "user", content: "Hello world!" }`）、元组，以及普通字符串（它们会被转换为 @[`HumanMessage`] 对象）。

如果模块收到超出这些格式范围的值，就会报错：

```python
from langchain_anthropic import ChatAnthropic

uncoercible_message = {"role": "HumanMessage", "random_field": "random value"}

model = ChatAnthropic(model="claude-sonnet-4-6")

model.invoke([uncoercible_message])
```

```text
ValueError: Message dict must contain 'role' and 'content' keys, got {'role': 'HumanMessage', 'random_field': 'random value'}
```
:::
:::js
TODO: 添加 JS 示例
:::

## 故障排查

要解决此错误：

1. **确保格式正确**：传给聊天模型的所有输入都必须是 LangChain 消息类数组，或者是受支持的类消息格式
2. 确认消息在传递给模型之前，没有发生非预期的字符串化或转换
3. 检查错误堆栈，并添加日志语句，在消息对象传给模型之前先查看它们的内容
