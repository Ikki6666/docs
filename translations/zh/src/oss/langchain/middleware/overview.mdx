---
title: 概览
description: 在每一步控制和自定义智能体执行
---

中间件提供了一种更严格控制智能体内部行为的方式。它适用于以下场景：

- 跟踪智能体行为，做日志、分析和调试。
- 转换提示词、[工具选择](/oss/langchain/middleware/built-in#llm-tool-selector) 和输出格式。
- 添加 [重试](/oss/langchain/middleware/built-in#tool-retry)、[回退](/oss/langchain/middleware/built-in#model-fallback) 和提前终止逻辑。
- 应用 [速率限制](/oss/langchain/middleware/built-in#model-call-limit)、防护栏以及 [PII 检测](/oss/langchain/middleware/built-in#pii-detection)。

:::python
通过把中间件传给 @[`create_agent`] 来添加它们：

```python
from langchain.agents import create_agent
from langchain.agents.middleware import SummarizationMiddleware, HumanInTheLoopMiddleware

agent = create_agent(
    model="gpt-5.5",
    tools=[...],
    middleware=[
        SummarizationMiddleware(...),
        HumanInTheLoopMiddleware(...)
    ],
)
```
:::

:::js
通过把中间件传给 `createAgent` 来添加它们：

```typescript
import {
  createAgent,
  summarizationMiddleware,
  humanInTheLoopMiddleware,
} from "langchain";

const agent = createAgent({
  model: "gpt-5.5",
  tools: [...],
  middleware: [summarizationMiddleware, humanInTheLoopMiddleware],
});
```
:::

## 智能体循环

核心智能体循环包括调用模型、让它选择要执行的工具，然后在不再调用工具时结束：

<img
    src="/oss/images/core_agent_loop.png"
    alt="核心智能体循环图"
    style={{height: "200px", width: "auto", justifyContent: "center"}}
    className="rounded-lg block mx-auto"
/>

中间件会在这些步骤前后暴露钩子：

<img
    src="/oss/images/middleware_final.png"
    alt="中间件流程图"
    style={{height: "300px", width: "auto", justifyContent: "center"}}
    className="rounded-lg mx-auto"
/>

## 在 LangGraph 工作流中使用中间件

中间件不是一个独立运行时：这些钩子会在 @[`create_agent`] 返回的已编译 [LangGraph](/oss/langgraph/overview) 内运行。你可以把整个智能体连同中间件一起，作为节点或子图放进更大的 @[StateGraph] 中，所有中间件钩子都会继续运行。

当外围拓扑不只是标准的“循环直到完成”时，就适合使用这种模式：例如在路由到多个智能体之前先分类输入、并行展开工作，或者把智能体调用和确定性步骤串起来。

`HumanInTheLoopMiddleware` 会按每个工具的 `.name` 进行匹配。在 Python 中，`@tool` 装饰的函数会直接取函数名作为名字，所以下面的 key 是 `"send_email"`；在 TypeScript 中，key 则与 `tool({...}, { name })` 里传入的 `name` 一致。

:::python
```python
from langchain.agents import AgentState, create_agent
from langchain.agents.middleware import HumanInTheLoopMiddleware
from langgraph.graph import START, StateGraph

# 假设 read_email、send_email、classify_node 和 route 已在别处定义。
email_agent = create_agent(
    model="claude-sonnet-4-6",
    tools=[read_email, send_email],
    middleware=[HumanInTheLoopMiddleware(interrupt_on={"send_email": True})],
)

graph = (
    StateGraph(AgentState)
    .add_node("classify", classify_node)
    .add_node("email_agent", email_agent)
    .add_edge(START, "classify")
    .add_conditional_edges("classify", route)
    .compile()
)
```
:::

:::js
```typescript
import { AgentState, createAgent, humanInTheLoopMiddleware } from "langchain";
import { StateGraph, START } from "@langchain/langgraph";

// 假设 readEmail、sendEmail、classifyNode 和 route 已在别处定义。
// readEmail / sendEmail 注册时的 name 分别是 "read_email" / "send_email"。
const emailAgent = createAgent({
  model: "claude-sonnet-4-6",
  tools: [readEmail, sendEmail],
  middleware: [humanInTheLoopMiddleware({ interruptOn: { send_email: true } })],
});

const graph = new StateGraph(AgentState)
  .addNode("classify", classifyNode)
  .addNode("emailAgent", emailAgent)
  .addEdge(START, "classify")
  .addConditionalEdges("classify", route)
  .compile();
```
:::

HITL 中断、总结、PII 脱敏、重试以及任何自定义钩子都会随着智能体节点一起移动。有关全部组合模式，包括子图 checkpointer 的作用域划分（按单次调用或按线程），请参见 [使用子图](/oss/langgraph/use-subgraphs)。

## 其他资源

<CardGroup cols={2}>
    <Card title="内置中间件" icon="box" href="/oss/langchain/middleware/built-in">
        了解适用于常见场景的内置中间件。
    </Card>
    <Card title="自定义中间件" icon="code" href="/oss/langchain/middleware/custom">
        使用钩子和装饰器构建自己的中间件。
    </Card>
    <Card title="中间件 API 参考" icon="book" href="https://reference.langchain.com/python/langchain/middleware/">
        查看中间件的完整 API 参考。
    </Card>
    <Card title="中间件集成" icon="plug" href="/oss/integrations/middleware/">
        Anthropic、AWS、OpenAI 等提供商的专用中间件。
    </Card>
    <Card title="测试智能体" icon="scale" href="/oss/langchain/test/">
        使用 LangSmith 测试你的智能体。
    </Card>
</CardGroup>
