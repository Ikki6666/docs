---
title: 在 Deep Agents Code 中使用子代理
sidebarTitle: 子代理
description: 使用带 YAML frontmatter 的 AGENTS.md 文件定义自定义 Deep Agents Code 子代理。涵盖项目和用户路径、可选模型覆盖以及示例。
---

把自定义的同步 [subagents](/oss/deepagents/subagents) 定义成 markdown 文件，这样 Deep Agents Code 就能把专门任务委派给它们。

<Note>
    目前 Deep Agents Code 还不支持异步 subagents。
</Note>

每个 subagent 都放在自己独立的文件夹里，并包含一个 `AGENTS.md` 文件：

```text
.deepagents/agents/{subagent-name}/AGENTS.md   # 项目级
~/.deepagents/{agent}/agents/{subagent-name}/AGENTS.md  # 用户级
```

项目 subagent 会覆盖同名的用户 subagent（见 [precedence rules](/oss/deepagents/code/data-locations#subagents)）。

frontmatter 里必须包含 `name` 和 `description`（与 [`SubAgent` dictionary spec](/oss/deepagents/subagents#subagent-dictionary-based) 一致）。markdown 正文会成为 subagent 的 `system_prompt`。除了基础规范外，`AGENTS.md` 还支持一个可选的 `model` frontmatter 字段，用来覆盖主 agent 在这个 subagent 上使用的模型。格式使用 `provider:model-name`，例如 `anthropic:claude-opus-4-8`、`openai:gpt-5.5`。如果省略，就继承主 agent 的模型。

<Note>
    其他 `SubAgent` 字段（`tools`、`middleware`、`interrupt_on`、`skills`）目前不能通过 `AGENTS.md` frontmatter 配置。用这种方式定义的自定义 subagent 会继承主 agent 的 tools。若需要完整控制，请直接使用 SDK。
</Note>

## 文件格式

subagent 的 `AGENTS.md` 文件使用 YAML frontmatter，后接 markdown 正文：

```markdown
---
name: researcher
description: Research topics on the web before writing content
model: anthropic:claude-haiku-4-5-20251001
---

You are a research assistant with access to web search.

## 你的流程
1. Search for relevant information
2. Summarize findings clearly
```

## 示例：成本更低的 subagent

对于简单的委派任务，可以使用更便宜、更快的模型，同时让主 agent 继续运行在更强的模型上：

```markdown
---
name: general-purpose
description: General-purpose agent for research and multi-step tasks
model: anthropic:claude-haiku-4-5-20251001
---

You are a general-purpose assistant. Complete the task efficiently and return a concise summary.
```

这会覆盖内置的 general-purpose subagent，把所有委派任务路由到更便宜的模型。更多信息见 [Override the general-purpose subagent](/oss/deepagents/subagents#override-the-general-purpose-subagent)。
