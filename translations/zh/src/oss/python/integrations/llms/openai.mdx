---
title: "OpenAI 集成"
description: "使用 LangChain Python 与 OpenAI LLM 集成。"
---

<Warning>
**你当前所在的页面文档介绍的是 OpenAI 文本补全模型。最新、最常用的 OpenAI 模型是 [聊天补全模型](/oss/langchain/models)。**

除非你明确在使用 `gpt-3.5-turbo-instruct`，否则你大概率应该查看 [这个页面](/oss/integrations/chat/openai/)。
</Warning>

[OpenAI](https://platform.openai.com/docs/introduction) 提供了一系列能力不同、适用于不同任务的模型。

本示例介绍如何使用 LangChain 与 OpenAI [models](https://platform.openai.com/docs/models) 交互。

## 概览

### 集成详情

| Class | Package | Local | Serializable | [JS support](https://js.langchain.com/docs/integrations/chat/openai) | Downloads | Version |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: |
| [`ChatOpenAI`](https://reference.langchain.com/python/langchain-openai/chat_models/base/ChatOpenAI) | [`langchain-openai`](https://reference.langchain.com/python/langchain-openai) | ❌ | beta | ✅ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-openai?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-openai?style=flat-square&label=%20) |

## 配置

要访问 OpenAI 模型，你需要创建一个 OpenAI 账号、获取 API key，并安装 `langchain-openai` 集成包。

### 凭据

前往 [platform.openai.com](https://platform.openai.com) 注册 OpenAI 并生成 API key。完成后，设置 `OPENAI_API_KEY` 环境变量：

```python
import getpass
import os

if "OPENAI_API_KEY" not in os.environ:
    os.environ["OPENAI_API_KEY"] = getpass.getpass("Enter your OpenAI API key: ")
```

如果你的请求要通过代理或 service emulator 转发，你可以通过环境变量设置 base URL，而不是传入 `base_url`。读取优先级如下（优先匹配者生效）：

1. 显式传入的 `base_url`（或 `openai_api_base`）关键字参数。
2. `OPENAI_API_BASE`，由 LangChain 在初始化时读取。
3. `OPENAI_BASE_URL`，由底层 `openai` SDK client 读取。

要启用模型调用的自动 tracing，请设置你的 [LangSmith](/langsmith/observability) API key：

```python
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装

LangChain OpenAI 集成位于 `langchain-openai` 包中：

```python
pip install -qU langchain-openai
```

如果你需要指定 organization ID，可以使用下面这个代码单元。不过，如果你只属于单个组织，或者打算使用默认组织，就不需要它。你可以在 [OpenAI API keys page](https://platform.openai.com/account/api-keys) 查看默认 organization。

要指定 organization，可以这样做：

```python
OPENAI_ORGANIZATION = getpass()

os.environ["OPENAI_ORGANIZATION"] = OPENAI_ORGANIZATION
```

## 实例化

现在我们可以实例化模型对象并生成 chat completions：

```python
from langchain_openai import OpenAI

llm = OpenAI()
```

## 调用

```python
llm.invoke("Hello how are you?")
```

```text
'\n\nI am an AI and do not have emotions like humans do, so I am always functioning at my optimal level. Thank you for asking! How can I assist you today?'
```

## 链式调用

```python
from langchain_core.prompts import PromptTemplate

prompt = PromptTemplate.from_template("How to say {input} in {output_language}:\n")

chain = prompt | llm
chain.invoke(
    {
        "output_language": "German",
        "input": "I love programming.",
    }
)
```

```text
'\nIch liebe Programmieren.'
```

## 使用代理

如果你处在显式代理后面，可以指定 `http_client` 进行透传。

```python
pip install httpx

import httpx

openai = OpenAI(
    model_name="gpt-3.5-turbo-instruct",
    http_client=httpx.Client(proxies="http://proxy.yourcompany.com:8080"),
)
```

---

## API reference

关于所有 @[`OpenAI`] llm 功能和配置的详细文档，请前往 API reference：[reference.langchain.com/python/langchain-openai/llms/base/OpenAI](https://reference.langchain.com/python/langchain-openai/llms/base/OpenAI)
