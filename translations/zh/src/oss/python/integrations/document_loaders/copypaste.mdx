---
title: "Copy paste 集成"
description: "使用 LangChain Python 与 Copy paste document loader 集成。"
---

本 notebook 介绍如何从你想直接复制粘贴的内容中加载一个 document 对象。在这种情况下，你甚至不需要使用 DocumentLoader，而是可以直接构造一个 Document。

```python
from langchain_core.documents import Document
```

```python
text = "..... put the text you copy pasted here......"
```

```python
doc = Document(page_content=text)
```

## Metadata

如果你想为这段文本添加来源相关的元数据，可以很方便地通过 `metadata` 键来完成。

```python
metadata = {"source": "internet", "date": "Friday"}
```

```python
doc = Document(page_content=text, metadata=metadata)
```

```python

```
