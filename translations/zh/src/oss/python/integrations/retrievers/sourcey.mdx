---
title: "Sourcey 集成"
description: "使用 LangChain Python 与 Sourcey retriever 集成。"
---

[Sourcey](https://sourcey.com) 已经会产出这个 retriever 需要的文件。

使用 `SourceyRetriever` 可以从已发布的 Sourcey 文档站点中检索内容。它会读取 `search-index.json`，在可用时使用 `llms-full.txt`，并返回 `Document` 对象，同时把规范化页面 URL 放在 `metadata["source"]` 中。

## 设置

安装包：

<CodeGroup>
```bash pip
pip install -qU langchain-sourcey
```

```bash uv
uv add langchain-sourcey
```
</CodeGroup>

不需要 API key。

## 实例化

`site_url` 应该指向已发布的 Sourcey docs 构建根路径。

```python
from langchain_sourcey import SourceyRetriever

retriever = SourceyRetriever(
    site_url="https://sourcey.com/docs",
    top_k=3,
)
```

## 使用

```python
docs = retriever.invoke("mcp integration")

for doc in docs:
    print(doc.metadata["title"])
    print(doc.metadata["source"])
```

## 在 chain 中使用

安装一个 chat model 包。这个示例使用 OpenAI：

<CodeGroup>
```bash pip
pip install -qU langchain-openai
```

```bash uv
uv add langchain-openai
```
</CodeGroup>

```python
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnablePassthrough
from langchain_openai import ChatOpenAI

from langchain_sourcey import SourceyRetriever

retriever = SourceyRetriever(site_url="https://sourcey.com/docs", top_k=3)

prompt = ChatPromptTemplate.from_template(
    """Answer the question using the documentation context below.

{context}

Question: {question}"""
)

chain = (
    RunnablePassthrough.assign(context=(lambda x: x["question"]) | retriever)
    | prompt
    | ChatOpenAI(model="gpt-4.1-mini")
    | StrOutputParser()
)

chain.invoke({"question": "How does Sourcey document MCP servers?"})
```

## 返回的 metadata

每个 `Document` 都包含一些便于引用和调试的 metadata 字段：

- `source`: 用于引用的 canonical page URL
- `matched_url`: 原始匹配 URL，在适用时包含锚点
- `matched_title`: 匹配到的搜索条目标题
- `title`: 补全后的页面标题
- `path`: Sourcey 输出路径
- `anchor`: 匹配到的片段标识（如果有）
- `tab`: Sourcey tab 标签
- `category`: Sourcey 搜索分类
- `site_url`: 本次检索使用的 docs 根路径
- `score`: retriever 排名分数

## Sourcey 站点要求

为了获得更干净的检索效果，已发布的 Sourcey 站点应当：

- 发布 `search-index.json`
- 发布 `llms-full.txt`
- 在 `sourcey.config.ts` 中设置 `siteUrl`，这样返回的引用才是 canonical 的

如果 `llms-full.txt` 不可用，`SourceyRetriever` 会回退到从匹配到的 HTML 页面中提取文本。

## 更多

- Sourcey 指南：[Using Sourcey with LangChain](https://sourcey.com/docs/guides/guide-langchain-retriever)
- 包地址：[langchain-sourcey on PyPI](https://pypi.org/project/langchain-sourcey/)
