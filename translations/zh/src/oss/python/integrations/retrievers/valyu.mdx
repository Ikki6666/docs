---
title: "Valyucontext 集成"
description: "使用 LangChain Python 与 Valyucontext retriever 集成。"
---

>[Valyu](https://www.valyu.network/) 允许 AI 应用和 agent 在互联网上以及专有数据源中搜索相关的、可直接供 LLM 使用的信息。

这份 notebook 会介绍如何在 LangChain 中使用 Valyu deep search 工具。

首先，获取一个 Valyu API key，并把它添加为环境变量。你可以通过 [注册这里](https://platform.valyu.network/) 获得 10 美元的免费额度。

## 设置

这个集成位于 `langchain-valyu` 包中。

```python
pip install -qU langchain-valyu
```

要使用这个包，你还需要把 `VALYU_API_KEY` 环境变量设置为你的 Valyu API key。

```python
import os

valyu_api_key = os.environ["VALYU_API_KEY"]
```

## 实例化

现在我们可以实例化 retriever 了：
`ValyuContextRetriever` 可以通过以下参数配置：

- `k: int = 5`
  每个查询返回的 top 结果数量。

- `search_type: str = "all"`
  要执行的搜索类型：`'all'`、`'proprietary'` 或 `'web'`。默认是 `'all'`。

- `relevance_threshold: float = 0.5`
  文档被视为相关所需的最小相关性分数，范围 0 到 1。默认值为 0.5。

- `max_price: float = 20.0`
  你愿意为每次查询支付的最高价格（美元）。默认值为 20.0。

- `start_date: Optional[str] = None`
  时间过滤的开始日期，格式为 YYYY-MM-DD（可选）。

- `end_date: Optional[str] = None`
  时间过滤的结束日期，格式为 YYYY-MM-DD（可选）。

- `client: Optional[Valyu] = None`
  可选的自定义 Valyu client 实例。如果不提供，就会在内部创建一个新的 client。

- `valyu_api_key: Optional[str] = None`
  你的 Valyu API key。如果不提供，retriever 会查找 `VALYU_API_KEY` 环境变量。

```python
from langchain_valyu import ValyuRetriever

retriever = ValyuRetriever(
    k=5,
    search_type="all",
    relevance_threshold=0.5,
    max_price=20.0,
    start_date="2024-01-01",
    end_date="2024-12-31",
    client=None,
    valyu_api_key=os.environ["VALYU_API_KEY"],
)
```

## 使用

```python
query = "What are the benefits of renewable energy?"
docs = retriever.invoke(query)

for doc in docs:
    print(doc.page_content)
    print(doc.metadata)
```

## 在 chain 中使用

我们可以很容易地把这个 retriever 组合进一个 chain。

```python
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnablePassthrough
from langchain_openai import ChatOpenAI

prompt = ChatPromptTemplate.from_template(
    """Answer the question based only on the context provided.

Context: {context}

Question: {question}"""
)

llm = ChatOpenAI(model="gpt-5.4-mini")


def format_docs(docs):
    return "\n\n".join(doc.page_content for doc in docs)


chain = (
    {"context": retriever | format_docs, "question": RunnablePassthrough()}
    | prompt
    | llm
    | StrOutputParser()
)
```

---

## API reference

关于 Valyu Context API 所有功能和配置的详细文档，请前往 API reference： [docs.valyu.network/overview](https://docs.valyu.network/overview)
