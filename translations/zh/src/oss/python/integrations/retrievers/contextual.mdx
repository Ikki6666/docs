---
title: "Contextual AI reranker 集成"
description: "使用 LangChain Python 与 Contextual AI reranker retriever 集成。"
---

Contextual AI 的 Instruction-Following Reranker 是世界上第一个能够按照自定义指令来排序文档的 reranker，你可以用它根据新鲜度、来源和 metadata 等具体标准来优先处理文档。它在 BEIR benchmark 上表现出色，得分 61.2，并以显著优势超过竞争对手，为企业级 RAG 应用带来了前所未有的控制力和准确性。

## 关键能力

- **Instruction Following**：通过自然语言指令动态控制文档排序
- **Conflict Resolution**：智能处理来自多个知识源的冲突信息
- **Superior Accuracy**：在行业基准测试中达到领先性能
- **Seamless Integration**：可作为现有 reranker 在你的 RAG pipeline 中的直接替代方案

这个 reranker 擅长解决企业知识库中的现实问题，比如优先选择更新的文档而不是过时文档，或者优先内部文档而不是外部来源。

如果你想了解更多关于 instruction-following reranker 的信息并查看实际示例，请访问 [product overview](https://contextual.ai/blog/introducing-instruction-following-reranker/)。

关于 Contextual AI 产品的完整文档，请访问 [developer portal](https://docs.contextual.ai/)。

这个集成需要 `contextual-client` Python SDK。更多信息请查看 [contextual-client-python repository](https://github.com/ContextualAI/contextual-client-python)。

## 概览

这个集成调用的是 Contextual AI 的 Grounded Language Model。

### 集成详情

| Class | Package | Local | Serializable | JS support | Downloads | Version |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: |
| `ContextualRerank` | `langchain-contextual` | ❌ | beta | ❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-contextual?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-contextual?style=flat-square&label=%20) |

## 设置

要访问 Contextual 的 reranker 模型，你需要创建一个 Contextual AI 账户，获取 API key，并安装 `langchain-contextual` 集成包。

### 凭据

前往 [app.contextual.ai](https://app.contextual.ai) 注册 Contextual 并生成 API key。完成后，把 `CONTEXTUAL_AI_API_KEY` 环境变量设置好：

```python
import getpass
import os

if not os.getenv("CONTEXTUAL_AI_API_KEY"):
    os.environ["CONTEXTUAL_AI_API_KEY"] = getpass.getpass(
        "Enter your Contextual API key: "
    )
```

## 安装

LangChain Contextual 集成位于 `langchain-contextual` 包中：

```python
pip install -qU langchain-contextual
```

## 实例化

Contextual Reranker 的参数如下：

| Parameter | Type | Description |
| --- | --- | --- |
| documents | list[Document] | 一组需要 rerank 的文档。文档中的任何 metadata 也会用于 reranking。 |
| query | str | 用于 reranking 的查询。 |
| model | str | 要使用的 reranker 版本。当前只有 `"ctxl-rerank-en-v1-instruct"`。 |
| top_n | Optional[int] | 要返回的结果数量。如果是 None，则返回全部结果。默认值为 `self.top_n`。 |
| instruction | Optional[str] | 要传给 reranker 的指令。 |
| callbacks | Optional[Callbacks] | 在压缩过程中运行的 callbacks。 |

```python
from langchain_contextual import ContextualRerank

api_key = ""
model = "ctxl-rerank-en-v1-instruct"

compressor = ContextualRerank(
    model=model,
    api_key=api_key,
)
```

## 使用

先设置我们会用到的全局变量和示例，并实例化 reranker client。

```python
from langchain_core.documents import Document

query = "What is the current enterprise pricing for the RTX 5090 GPU for bulk orders?"
instruction = "Prioritize internal sales documents over market analysis reports. More recent documents should be weighted higher. Enterprise portal content supersedes distributor communications."

document_contents = [
    "Following detailed cost analysis and market research, we have implemented the following changes: AI training clusters will see a 15% uplift in raw compute performance, enterprise support packages are being restructured, and bulk procurement programs (100+ units) for the RTX 5090 Enterprise series will operate on a $2,899 baseline.",
    "Enterprise pricing for the RTX 5090 GPU bulk orders (100+ units) is currently set at $3,100-$3,300 per unit. This pricing for RTX 5090 enterprise bulk orders has been confirmed across all major distribution channels.",
    "RTX 5090 Enterprise GPU requires 450W TDP and 20% cooling overhead.",
]

metadata = [
    {
        "Date": "January 15, 2025",
        "Source": "NVIDIA Enterprise Sales Portal",
        "Classification": "Internal Use Only",
    },
    {"Date": "11/30/2023", "Source": "TechAnalytics Research Group"},
    {
        "Date": "January 25, 2025",
        "Source": "NVIDIA Enterprise Sales Portal",
        "Classification": "Internal Use Only",
    },
]

documents = [
    Document(page_content=content, metadata=metadata[i])
    for i, content in enumerate(document_contents)
]
reranked_documents = compressor.compress_documents(
    query=query,
    instruction=instruction,
    documents=documents,
)
```

## 在 chain 中使用

示例即将推出。

---

## API reference

关于 `ChatContextual` 所有功能和配置的详细文档，请前往 GitHub 页面：[github.com/ContextualAI//langchain-contextual](https://github.com/ContextualAI//langchain-contextual)
