---
title: "ParallelSearchRetriever 集成"
description: "使用 LangChain Python 与 ParallelSearchRetriever 集成。"
---

@[`ParallelSearchRetriever`] 是一个 LangChain [`BaseRetriever`](/oss/langchain/retrieval)，底层使用 [Parallel](https://platform.parallel.ai/) 的 [Search API](https://docs.parallel.ai/search/search-quickstart)。它返回 `list[Document]`，并附带丰富的 `metadata`，例如 `url`、`title`、`publish_date`、`search_id`、`excerpts` 和 `query`，可以直接接入任何 RAG pipeline。

<Note>
    如果你想找的是一个可被 LLM 调用的 tool，并且希望它返回原始搜索响应而不是 `Document`，请查看 [ParallelSearchTool](/oss/integrations/tools/parallel_search)。
</Note>

## 概览

### 集成详情

| Class | Package | JS support | Package latest |
| :--- | :--- | :---: | :---: |
| @[`ParallelSearchRetriever`] | @[`langchain-parallel`] | ❌ | <a href="https://pypi.org/project/langchain-parallel/" target="_blank"><img src="https://img.shields.io/pypi/v/langchain-parallel?style=flat-square&label=%20&color=orange" alt="PyPI - Latest version" noZoom height="100" class="rounded" /></a> |

## 设置

这个集成位于 `langchain-parallel` 包中。

<CodeGroup>
    ```bash pip
    pip install -U langchain-parallel
    ```
    ```bash uv
    uv add langchain-parallel
    ```
</CodeGroup>

### 凭据

前往 [Parallel](https://platform.parallel.ai) 注册并生成 API key。然后把 `PARALLEL_API_KEY` 设置到环境变量中：

```python
import getpass
import os

if not os.environ.get("PARALLEL_API_KEY"):
    os.environ["PARALLEL_API_KEY"] = getpass.getpass("Parallel API key:\n")
```

## 实例化

```python
from langchain_parallel import ParallelSearchRetriever

retriever = ParallelSearchRetriever(
    max_results=3,
    excerpts={"max_chars_per_result": 800},
)
```

## 使用

每个返回的 `Document` 都会把 excerpts 拼接到 `page_content` 中，并在 `metadata` 中暴露来源 URL、标题和发布日期：

```python
docs = retriever.invoke("breakthroughs in fusion energy 2025")
for d in docs:
    print(d.metadata.get("title"), "—", d.metadata.get("url"))
    print(d.page_content[:200], "...\n")
```

```text
Net energy gain in fusion: NIF results — https://www.nature.com/articles/...
The National Ignition Facility achieved net energy gain on December 5, 2022 ...

Commonwealth Fusion's SPARC milestone — https://news.mit.edu/...
SPARC is on track for first plasma in 2026 ...
```

### 带配置的搜索

你可以在关键词 `search_queries` 之外再传入一个 `objective`，用于定义更丰富的检索目标。这个 retriever 会把 source 和 fetch policy 转发给底层 Search API。

```python
configured = ParallelSearchRetriever(
    max_results=5,
    excerpts={"max_chars_per_result": 1500},
    mode="basic",  # 'basic'（更低延迟）或 'advanced'（更高质量）
    source_policy={
        "include_domains": ["nature.com", "science.org", "iter.org"],
    },
)

docs = configured.invoke(
    "What's the latest peer-reviewed result on net-energy-gain fusion?"
)
```

### 异步

```python
docs = await retriever.ainvoke("Latest GLP-1 trial results 2025")
```

## 在 chain 中使用

`ParallelSearchRetriever` 可以接入任何 LangChain chain：

```python
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnablePassthrough
from langchain.chat_models import init_chat_model

llm = init_chat_model(model="claude-opus-4-8")

prompt = ChatPromptTemplate.from_messages([
    ("system", "Answer using only the context below. Cite URLs."),
    ("human", "Context:\n{context}\n\nQuestion: {question}"),
])

def format_docs(docs):
    return "\n\n".join(
        f"[{d.metadata.get('url')}] {d.page_content[:500]}" for d in docs
    )

chain = (
    {"context": retriever | format_docs, "question": RunnablePassthrough()}
    | prompt
    | llm
    | StrOutputParser()
)

chain.invoke("What was the most recent fusion energy breakthrough?")
```

## API reference

如需详细文档，请查看 @[`ParallelSearchRetriever`] API reference 或 [Parallel Search API guides](https://docs.parallel.ai/search/search-quickstart)。
