---
title: "ModalSandbox 集成"
description: "使用 LangChain Python 集成 ModalSandbox 沙箱后端。"
---

[Modal](https://modal.com) 提供支持 GPU 的无服务器容器基础设施。关于注册、认证和平台细节，请参阅 [Modal 文档](https://modal.com/docs)。

## 安装

<CodeGroup>
```bash pip
pip install langchain-modal
```

```bash uv
uv add langchain-modal
```
</CodeGroup>

## 创建沙箱后端

在 Python 中，你先使用 provider SDK 创建沙箱，然后用 [deepagents backend](/oss/deepagents/backends) 对其进行包装。

```python
import modal

from langchain_modal import ModalSandbox

app = modal.App.lookup("your-app")
modal_sandbox = modal.Sandbox.create(app=app)
backend = ModalSandbox(sandbox=modal_sandbox)

result = backend.execute("echo hello")
print(result.output)
```

## 与 Deep Agents 搭配使用

```python
import modal
from langchain_anthropic import ChatAnthropic

from deepagents import create_deep_agent
from langchain_modal import ModalSandbox

app = modal.App.lookup("your-app")
modal_sandbox = modal.Sandbox.create(app=app)
backend = ModalSandbox(sandbox=modal_sandbox)

agent = create_deep_agent(
    model=ChatAnthropic(model="claude-sonnet-4-20250514"),
    system_prompt="You are a coding assistant with sandbox access.",
    backend=backend,
)

result = agent.invoke(
    {"messages": [{"role": "user", "content": "Install numpy and calculate pi"}]}
)
```

## 清理

你需要自行通过 Modal 管理沙箱生命周期。
完成后，请终止该沙箱。

另请参阅：[Sandboxes](/oss/deepagents/sandboxes)。
