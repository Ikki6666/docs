---
title: "Greennode 集成"
description: "使用 LangChain Python 集成 Greennode。"
---

>**GreenNode** 是全球 AI 解决方案提供商，同时也是 **NVIDIA Preferred Partner**，面向美国、中东和北非以及亚太地区企业，提供从基础设施到应用的全栈 AI 能力。
>依托**世界级基础设施**（LEED Gold、TIA‑942、Uptime Tier III），**GreenNode** 为企业、初创公司和研究人员提供全面的 AI 服务套件：
>
>- [强大的 AI 基础设施：](https://greennode.ai/) 作为亚太地区最早的超大规模 AI 集群之一，GreenNode 的基础设施由 NVIDIA H100 GPU 驱动，专为高吞吐量的机器学习和深度学习工作负载优化。
>- [GreenNode AI Platform：](https://greennode.ai/product/ai-platform) 面向技术团队设计的 GreenNode 自助式 AI 平台，可快速部署 Jupyter notebook 环境，并预配置优化后的计算实例。开发者可以通过该门户以最少准备时间启动 ML 训练、微调、超参数优化和推理工作流。该平台还提供 100 多个精选开源模型，并支持与常见 MLOps 工具和存储框架集成。
>- [GreenNode Serverless AI：](https://greennode.ai/product/model-as-a-service) GreenNode Serverless AI 提供一个预训练、可用于生产的模型库，覆盖文本生成、代码生成、文本转语音、语音转文本、embedding 和 reranking 等领域。该服务非常适合希望在不管理模型基础设施的情况下快速原型设计或部署 AI 方案的团队。
>- [AI Applications：](https://vngcloud.vn/en/solution) 从智能数据管理和文档处理（IDP）到智能视频分析，GreenNode 支持大规模真实世界 AI 用例。
>无论您是在构建下一个 LLM 工作流、扩展 AI 研究，还是部署企业级应用，**GreenNode** 都能提供加速前进所需的工具与基础设施。

## 安装和设置

GreenNode 集成可以通过 pip 安装：

```python
pip install -qU langchain-greennode
```

### API 密钥

要使用 GreenNode Serverless AI，您需要一个 API 密钥，可从 [GreenNode Serverless AI](https://aiplatform.console.greennode.ai/api-keys) 获取。该密钥可以通过初始化参数 `api_key` 传入，或设置为环境变量 `GREENNODE_API_KEY`。

```python
import getpass
import os

if not os.getenv("GREENNODE_API_KEY"):
    os.environ["GREENNODE_API_KEY"] = getpass.getpass("Enter your GreenNode API key: ")
```

## 聊天模型

```python
from langchain_greennode import ChatGreenNode

chat = ChatGreenNode(
    model="deepseek-ai/DeepSeek-R1-Distill-Qwen-32B",  # Choose from available models
    temperature=0.6,
    top_p=0.95,
)
```

GreenNode [Chat Model](https://python.langchain.com/docs/integrations/chat/greennode/) 的用法

## 嵌入模型

```python
from langchain_greennode import GreenNodeEmbeddings

# Initialize embeddings
embeddings = GreenNodeEmbeddings(
    model="BAAI/bge-m3"  # Choose from available models
)
```

GreenNode [Embedding Model](https://python.langchain.com/docs/integrations/embeddings/greennode/) 的用法

## Rerank

```python
from langchain_greennode import GreenNodeRerank

# Initialize reranker
rerank = GreenNodeRerank(
    model="BAAI/bge-reranker-v2-m3",  # Choose from available models
    top_n=-1,
)
```

GreenNode [Rerank Model](https://python.langchain.com/docs/integrations/retrievers/greennode-reranker) 的用法
