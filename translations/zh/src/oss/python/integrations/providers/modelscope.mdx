---
title: "Modelscope 集成"
description: "使用 LangChain Python 集成 Modelscope。"
---

>[ModelScope](https://www.modelscope.cn/home) 是一个大型模型与数据集仓库。

本页介绍如何在 LangChain 中使用 modelscope 生态系统。
内容分为两部分：安装与设置，以及对特定 modelscope wrappers 的引用。

## 安装

<CodeGroup>
```bash pip
pip install -U langchain-modelscope-integration
```

```bash uv
uv add langchain-modelscope-integration
```
</CodeGroup>

前往 [ModelScope](https://modelscope.cn/) 注册，并生成一个 [SDK token](https://modelscope.cn/my/myaccesstoken)。完成后，设置 `MODELSCOPE_SDK_TOKEN` 环境变量：

```bash
export MODELSCOPE_SDK_TOKEN=<your_sdk_token>
```

## 聊天模型

`ModelScopeChatEndpoint` 类暴露了来自 ModelScope 的聊天模型。可在 [ModelScope API documentation](https://www.modelscope.cn/docs/model-service/API-Inference/intro) 中查看可用模型。

```python
from langchain_modelscope import ModelScopeChatEndpoint

llm = ModelScopeChatEndpoint(model="Qwen/Qwen2.5-Coder-32B-Instruct")
llm.invoke("Sing a ballad of LangChain.")
```

## Embeddings

`ModelScopeEmbeddings` 类暴露了来自 ModelScope 的 embeddings。

```python
from langchain_modelscope import ModelScopeEmbeddings

embeddings = ModelScopeEmbeddings(model_id="damo/nlp_corom_sentence-embedding_english-base")
embeddings.embed_query("What is the meaning of life?")
```

## LLMs

`ModelScopeEndpoint` 类暴露了来自 ModelScope 的 LLMs。

```python
from langchain_modelscope import ModelScopeEndpoint

llm = ModelScopeEndpoint(model="Qwen/Qwen2.5-Coder-32B-Instruct")
llm.invoke("The meaning of life is")
```
