---
title: "Microsoft 集成"
description: "使用 LangChain Python 与 Microsoft 集成。"
sidebarTitle: "Microsoft"
---

本页介绍所有与 [Microsoft Azure](https://portal.azure.com) 以及其他 [Microsoft](https://www.microsoft.com) 产品相关的 LangChain 集成。

<Tip>
    **推荐：Azure OpenAI**

    建议在 [chat models](#chat-models)、[LLMs](#llms) 和 [embedding models](#embedding-models) 中统一使用 @[Azure OpenAI][AzureOpenAI]。随着 [v1 API](https://learn.microsoft.com/en-us/azure/ai-foundry/openai/api-version-lifecycle?tabs=python) 在 2025 年 8 月正式 GA，你可以直接使用 Azure endpoint 和 API keys，配合 @[`langchain-openai`] 包，通过单一接口调用部署在 [Microsoft Foundry](https://learn.microsoft.com/en-us/azure/ai-foundry/) 上的任意模型，包括 OpenAI、Llama、DeepSeek、Mistral 和 Phi。你还可以获得对 Microsoft Entra ID 认证的原生支持，以及包括 [Responses API](#responses-api) 和 [reasoning models](/oss/integrations/chat/azure_chat_openai) 在内的最新能力。[从这里开始](#azure-openai)。

    **示例与教程：**
    - [Azure-Samples/langchain-azure-openai-starter](https://github.com/Azure-Samples/langchain-azure-openai-starter)：一个面向生产的 LangChain + Azure OpenAI 应用模板，可通过单个 `azd` 命令直接部署到 Azure。
    - [microsoft/langchain-for-beginners](https://github.com/microsoft/langchain-for-beginners)：一个使用 Azure OpenAI 介绍 LangChain 的实战课程。
    - [Azure-Samples/langchain-agent-python](https://github.com/Azure-Samples/langchain-agent-python)：在 Azure 上构建并部署 LangChain agents。
</Tip>

<Note>
    **Claude on Azure**

    Microsoft Foundry 也提供对全部 [Anthropic Claude models](https://learn.microsoft.com/en-us/azure/foundry/foundry-models/how-to/use-foundry-models-claude) 的访问，包括 Opus、Sonnet 和 Haiku。Claude 模型通过 Anthropic 原生专用 endpoint 提供，而不是 Azure OpenAI v1 API。请使用指向 Foundry Anthropic endpoint 的 [`langchain-anthropic`](/oss/integrations/chat/anthropic)。
</Note>

## Chat models

Microsoft 通过 Azure 为 chat models 提供三种主要接入方式：

1. **[Azure OpenAI](https://learn.microsoft.com/en-us/azure/ai-services/openai/)**（推荐），通过单一接口访问部署在 Microsoft Foundry 上的任意模型，包括 OpenAI、Llama、DeepSeek、Mistral 和 Phi，并具备通过 [Microsoft Entra ID](https://learn.microsoft.com/en-us/azure/ai-foundry/openai/how-to/managed-identity) 进行无密钥认证、区域数据驻留和私有网络等企业能力。v1 API 使用 @[`ChatOpenAI`]，传统部署使用 @[`AzureChatOpenAI`]。

    Azure OpenAI 还支持 [Responses API](#responses-api)，可直接从 chat model 使用服务端工具，例如 code interpreter、image generation 和 file search。
2. **[Azure AI](https://learn.microsoft.com/en-us/azure/ai-studio/how-to/deploy-models)**，适合在使用 chat model 的同时接入更广泛 Azure 生态中的工具、存储和自定义 middleware。
3. **[Azure ML](https://learn.microsoft.com/en-us/azure/machine-learning/)**，支持部署和管理自定义或微调后的开源模型。

### Azure OpenAI

要开始使用 Azure OpenAI，请先[创建 Azure deployment](https://learn.microsoft.com/en-us/azure/ai-services/openai/how-to/create-resource)，然后安装 `langchain-openai` 包：

<CodeGroup>
    ```bash pip
    pip install -U langchain-openai
    ```

    ```bash uv
    uv add langchain-openai
    ```
</CodeGroup>

在 v1 API 下，可直接让 @[`ChatOpenAI`] 指向 Azure endpoint，无需提供 `api_version`：

<Tabs>
    <Tab title="Entra ID (recommended)">
        ```bash
        pip install azure-identity
        ```

        ```python
        from azure.identity import DefaultAzureCredential, get_bearer_token_provider
        from langchain_openai import ChatOpenAI

        token_provider = get_bearer_token_provider(
            DefaultAzureCredential(),
            "https://cognitiveservices.azure.com/.default",
        )

        llm = ChatOpenAI(
            model="gpt-5.4-mini",  # your Azure deployment name
            base_url="https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/",
            api_key=token_provider,  # callable that handles token refresh
        )
        ```
    </Tab>
    <Tab title="API key">
        ```python
        from langchain_openai import ChatOpenAI

        llm = ChatOpenAI(
            model="gpt-5.4-mini",  # your Azure deployment name
            base_url="https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/",
            api_key="your-azure-api-key",
        )
        ```
    </Tab>
</Tabs>

对于传统 Azure OpenAI API 版本，请使用 @[`AzureChatOpenAI`]：

```python
from langchain_openai import AzureChatOpenAI
```

完整的端到端设置、Entra ID 认证、tool calling 和 reasoning 示例，参见 [Azure ChatOpenAI integration page](/oss/integrations/chat/azure_chat_openai)。

#### Responses API

Azure OpenAI 支持 [Responses API](https://learn.microsoft.com/en-us/azure/ai-foundry/openai/how-to/responses)，可提供有状态对话、内置工具，例如 web search、file search、code interpreter，以及结构化推理摘要。设置 `reasoning` 参数时，@[`ChatOpenAI`] 会自动路由到 Responses API，你也可以通过 `use_responses_api=True` 显式启用：

<Tabs>
    <Tab title="Entra ID (recommended)">
        ```python
        from azure.identity import DefaultAzureCredential, get_bearer_token_provider
        from langchain_openai import ChatOpenAI

        token_provider = get_bearer_token_provider(
            DefaultAzureCredential(),
            "https://cognitiveservices.azure.com/.default",
        )

        llm = ChatOpenAI(
            model="gpt-5.4-mini",
            base_url="https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/",
            api_key=token_provider,
            use_responses_api=True,
        )

        response = llm.invoke("Summarize the bitter lesson.")
        print(response.text)
        ```
    </Tab>
    <Tab title="API key">
        ```python
        from langchain_openai import ChatOpenAI

        llm = ChatOpenAI(
            model="gpt-5.4-mini",
            base_url="https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/",
            api_key="your-azure-api-key",
            use_responses_api=True,
        )

        response = llm.invoke("Summarize the bitter lesson.")
        print(response.text)
        ```
    </Tab>
</Tabs>

关于 reasoning effort、reasoning summaries 和 Responses API streaming 的详细说明，参见 [Azure ChatOpenAI integration page](/oss/integrations/chat/azure_chat_openai)。

### Azure AI

>[Azure AI Foundry](https://learn.microsoft.com/en-us/azure/developer/python/get-started) 是更广泛的 Azure AI 平台。`langchain-azure-ai` 包可以将 Azure 原生工具、存储和自定义 middleware 接入你的 LangChain 应用，并通过 `AzureAIOpenAIApiChatModel` 类暴露部署在 Foundry 上的 chat models。

<CodeGroup>
    ```bash pip
    pip install -U langchain-azure-ai
    ```

    ```bash uv
    uv add langchain-azure-ai
    ```
</CodeGroup>

参见[使用示例](/oss/integrations/chat/azure_ai)。

## LLMs

Microsoft 通过 Azure 提供两种主要方式访问 LLM：

1. **[Azure OpenAI](https://learn.microsoft.com/en-us/azure/ai-services/openai/)**（推荐），通过 @[`AzureOpenAI`] 以 completion LLM 的方式访问部署在 Microsoft Foundry 上的任意模型，包括 OpenAI、Llama、DeepSeek、Mistral 和 Phi。
2. **[Azure ML](https://learn.microsoft.com/en-us/azure/machine-learning/)**，用于托管在 Azure Machine Learning 在线端点上的自定义或开源模型。

### Azure OpenAI

参见[使用示例](/oss/integrations/llms/azure_openai)。

<CodeGroup>
    ```bash pip
    pip install -U langchain-openai
    ```

    ```bash uv
    uv add langchain-openai
    ```
</CodeGroup>

<Tabs>
    <Tab title="Entra ID (recommended)">
        ```python
        from azure.identity import DefaultAzureCredential, get_bearer_token_provider
        from langchain_openai import AzureOpenAI

        token_provider = get_bearer_token_provider(
            DefaultAzureCredential(),
            "https://cognitiveservices.azure.com/.default",
        )

        llm = AzureOpenAI(
            azure_deployment="gpt-5.4-mini",  # your Azure deployment name
            api_version="2025-04-01-preview",
            azure_ad_token_provider=token_provider,
        )

        print(llm.invoke("Write a haiku about the ocean."))
        ```
    </Tab>
    <Tab title="API key">
        ```python
        from langchain_openai import AzureOpenAI

        llm = AzureOpenAI(
            azure_deployment="gpt-5.4-mini",  # your Azure deployment name
            api_version="2025-04-01-preview",
            azure_endpoint="https://YOUR-RESOURCE-NAME.openai.azure.com/",
            api_key="your-azure-api-key",
        )

        print(llm.invoke("Write a haiku about the ocean."))
        ```
    </Tab>
</Tabs>

## Embedding models

Microsoft 通过 Azure 提供两种主要方式访问 embedding models：

1. **[Azure OpenAI](https://learn.microsoft.com/en-us/azure/ai-services/openai/)**（推荐），通过 @[`AzureOpenAIEmbeddings`] 使用部署在 Microsoft Foundry 中的 embedding 模型，包括 OpenAI 的 `text-embedding-3-small`、`text-embedding-3-large` 以及 Cohere。
2. **[Azure AI](https://learn.microsoft.com/en-us/azure/ai-studio/how-to/deploy-models)**，适合在使用 embedding model 的同时接入更广泛 Azure 生态中的工具、存储和自定义 middleware。

### Azure OpenAI

参见[使用示例](/oss/integrations/embeddings/azure_openai)。

<CodeGroup>
    ```bash pip
    pip install -U langchain-openai
    ```

    ```bash uv
    uv add langchain-openai
    ```
</CodeGroup>

<Tabs>
    <Tab title="Entra ID (recommended)">
        ```python
        from azure.identity import DefaultAzureCredential, get_bearer_token_provider
        from langchain_openai import AzureOpenAIEmbeddings

        token_provider = get_bearer_token_provider(
            DefaultAzureCredential(),
            "https://cognitiveservices.azure.com/.default",
        )

        embeddings = AzureOpenAIEmbeddings(
            azure_deployment="text-embedding-3-small",  # your Azure deployment name
            api_version="2025-04-01-preview",
            azure_ad_token_provider=token_provider,
        )

        vector = embeddings.embed_query("LangChain makes agents easy.")
        ```
    </Tab>
    <Tab title="API key">
        ```python
        from langchain_openai import AzureOpenAIEmbeddings

        embeddings = AzureOpenAIEmbeddings(
            azure_deployment="text-embedding-3-small",  # your Azure deployment name
            api_version="2025-04-01-preview",
            azure_endpoint="https://YOUR-RESOURCE-NAME.openai.azure.com/",
            api_key="your-azure-api-key",
        )

        vector = embeddings.embed_query("LangChain makes agents easy.")
        ```
    </Tab>
</Tabs>

### Azure AI

<CodeGroup>
    ```bash pip
    pip install -U langchain-azure-ai
    ```

    ```bash uv
    uv add langchain-azure-ai
    ```
</CodeGroup>

参见[使用示例](/oss/integrations/providers/azure_ai#azure-ai-model-inference-for-embeddings)。

## Middleware

### Azure AI Content Safety middleware

>[Azure AI Content Safety](https://learn.microsoft.com/en-us/azure/ai-services/content-safety/overview) 可通过 middleware 形式为 LangChain agents 添加 guardrails。`langchain-azure-ai` 包当前导出了用于文本审核、图像审核、prompt injection 检测、受保护材料检测和 groundedness 评估的 middleware。

安装 middleware 包：

<CodeGroup>
    ```bash pip
    pip install -U langchain-azure-ai
    ```

    ```bash uv
    uv add langchain-azure-ai
    ```
</CodeGroup>

参见 [Microsoft Foundry middleware guide](/oss/integrations/middleware/azure_ai)。

```python
from langchain_azure_ai.agents.middleware import AzureContentModerationMiddleware
```

## Document loaders

### Azure Blob Storage

>[Azure Blob Storage](https://learn.microsoft.com/en-us/azure/storage/blobs/storage-blobs-introduction) 是 Microsoft 的云对象存储解决方案。Blob Storage 针对海量非结构化数据的存储进行了优化。非结构化数据是指不遵循特定数据模型或定义的数据，例如文本或二进制数据。

`Azure Blob Storage` 适用于：
- 直接向浏览器提供图像或文档
- 存储供分布式访问的文件
- 流式传输视频和音频
- 写入日志文件
- 存储备份与恢复、灾难恢复和归档数据
- 存储供本地或 Azure 托管服务进行分析的数据

<CodeGroup>
    ```bash pip
    pip install langchain-azure-storage
    ```

    ```bash uv
    uv add langchain-azure-storage
    ```
</CodeGroup>

参见 [Azure Blob Storage Loader 的使用示例](/oss/integrations/document_loaders/azure_blob_storage)。

```python
from langchain_azure_storage.document_loaders import AzureBlobStorageLoader
```

## Memory

### Azure cosmos DB chat message history

>[Azure Cosmos DB](https://learn.microsoft.com/azure/cosmos-db/) 为会话型 AI 应用提供聊天消息历史存储能力，支持低延迟、高可用地持久化和检索对话历史。

<CodeGroup>
    ```bash pip
    pip install langchain-azure-cosmosdb
    ```

    ```bash uv
    uv add langchain-azure-cosmosdb
    ```
</CodeGroup>

配置 Azure Cosmos DB 连接，可选择同步或异步，也可使用 access key 或 Microsoft Entra ID：

```python
from langchain_azure_cosmosdb import CosmosDBChatMessageHistory

history = CosmosDBChatMessageHistory(
    cosmos_endpoint="https://<your-account>.documents.azure.com:443/",
    cosmos_database="<your-database>",
    cosmos_container="<your-container>",
    session_id="<session-id>",
    user_id="<user-id>",
    credential="<your-key-or-token-credential>",
    ttl=3600,  # optional: messages expire after 1 hour
)
history.prepare_cosmos()

history.add_user_message("Hello!")
history.add_ai_message("Hi there!")
```

若要使用异步版本，请从同一包中导入 `AsyncCosmosDBChatMessageHistory`。

### Azure cosmos DB semantic cache

>[`AzureCosmosDBNoSqlSemanticCache`](https://github.com/langchain-ai/langchain-azure/tree/main/libs/azure-cosmosdb) 使用向量相似度在 Azure Cosmos DB for NoSQL 中缓存 LLM 响应，当遇到语义相似的 prompt 时返回缓存结果。

<CodeGroup>
    ```bash pip
    pip install langchain-azure-cosmosdb
    ```

    ```bash uv
    uv add langchain-azure-cosmosdb
    ```
</CodeGroup>

```python
from azure.cosmos import CosmosClient, PartitionKey
from langchain_core.globals import set_llm_cache
from langchain_azure_cosmosdb import AzureCosmosDBNoSqlSemanticCache

cosmos_client = CosmosClient("<endpoint>", "<key>")

cache = AzureCosmosDBNoSqlSemanticCache(
    cosmos_client=cosmos_client,
    embedding=embedding,
    vector_embedding_policy=vector_embedding_policy,
    indexing_policy=indexing_policy,
    cosmos_container_properties={"partition_key": PartitionKey(path="/id")},
    cosmos_database_properties={"id": "cache-db"},
    vector_search_fields={"text_field": "text", "embedding_field": "embedding"},
    database_name="cache-db",
    container_name="cache-container",
)

set_llm_cache(cache)
```

异步版本请导入 `AsyncAzureCosmosDBNoSqlSemanticCache`。

## Vector stores

### Azure cosmos DB
AI agents 可以将 Azure Cosmos DB 作为统一的[memory system](https://learn.microsoft.com/en-us/azure/cosmos-db/ai-agents#memory-can-make-or-break-agents)方案，享受其速度、规模和简洁性。该服务曾成功[支持 OpenAI 的 ChatGPT 服务](https://www.youtube.com/watch?v=6IIUtEFKJec&t)，以高可靠和低维护成本实现动态扩展。凭借原子记录序列引擎，它是全球首个同时支持分布式 [NoSQL](https://learn.microsoft.com/en-us/azure/cosmos-db/distributed-nosql)、[关系型](https://learn.microsoft.com/en-us/azure/cosmos-db/distributed-relational) 和[向量数据库](https://learn.microsoft.com/en-us/azure/cosmos-db/vector-database)能力，并提供 serverless 模式的服务。

下面是 Azure Cosmos DB 中可提供 vector store 功能的两种 API。

#### Azure cosmos DB for MongoDB (vCore)

>[Azure Cosmos DB for MongoDB vCore](https://learn.microsoft.com/en-us/azure/cosmos-db/mongodb/vcore/) 让你可以轻松创建一个具备原生 MongoDB 支持的数据库。
> 你可以复用已有的 MongoDB 经验，并继续使用喜爱的 MongoDB 驱动、SDK 和工具，只需将应用指向 API for MongoDB vCore 账号的连接字符串即可。
> 在 Azure Cosmos DB for MongoDB vCore 中使用向量搜索，可以将基于 AI 的应用无缝连接到存储在 Azure Cosmos DB 中的数据。

##### 安装与设置

参见[详细配置说明](/oss/integrations/vectorstores/azure_cosmos_db_mongo_vcore)。

需要安装 `langchain-azure-ai` 和 `pymongo` Python 包。

<CodeGroup>
    ```bash pip
    pip install langchain-azure-ai pymongo
    ```

    ```bash uv
    uv add langchain-azure-ai pymongo
    ```
</CodeGroup>

##### 在 Microsoft Azure 上部署 Azure cosmos DB

Azure Cosmos DB for MongoDB vCore 为开发者提供全托管的 MongoDB 兼容数据库服务，用于构建现代应用，并保持熟悉的架构方式。

通过 Cosmos DB for MongoDB vCore，开发者可以享受 Azure 原生集成、更低总体拥有成本，以及在迁移现有应用或构建新应用时熟悉的 vCore 架构。

[立即免费注册](https://azure.microsoft.com/en-us/free/)开始使用。

参见[使用示例](/oss/integrations/vectorstores/azure_cosmos_db_mongo_vcore)。

```python
from langchain_azure_ai.vectorstores import AzureCosmosDBMongoVCoreVectorSearch
```

#### Azure cosmos DB NoSQL

>[Azure Cosmos DB for NoSQL](https://learn.microsoft.com/en-us/azure/cosmos-db/nosql/vector-search) 现在以预览版形式提供向量索引和搜索能力。
该功能旨在处理高维向量，并在任意规模下实现高效、准确的向量搜索。现在你可以直接在文档中与常规数据一起存储向量，也就是说，数据库中的每个文档不仅可以包含传统的无 schema 数据，还可以将高维向量作为文档的其他属性。这种数据与向量的同位存储方式，使索引和搜索都更加高效，因为向量与其代表的数据存储在同一逻辑单元中。这简化了数据管理、AI 应用架构以及基于向量的操作效率。

##### 安装与设置

参见[详细配置说明](/oss/integrations/vectorstores/azure_cosmos_db_no_sql)。

需要安装 `langchain-azure-cosmosdb` 和 `azure-cosmos` Python 包。

<CodeGroup>
    ```bash pip
    pip install langchain-azure-cosmosdb azure-cosmos
    ```

    ```bash uv
    uv add langchain-azure-cosmosdb azure-cosmos
    ```
</CodeGroup>

##### 在 Microsoft Azure 上部署 Azure cosmos DB

Azure Cosmos DB 通过非常敏捷的动态弹性自动扩展，为现代应用和智能工作负载提供解决方案。它在每个 Azure 区域均可用，并能自动将数据复制到更靠近用户的位置。同时还提供 SLA 保证的低延迟和高可用性。

[立即免费注册](https://learn.microsoft.com/en-us/azure/cosmos-db/nosql/quickstart-python?pivots=devcontainer-codespace)开始使用。

参见[使用示例](/oss/integrations/vectorstores/azure_cosmos_db_no_sql)。

```python
from langchain_azure_cosmosdb import AzureCosmosDBNoSqlVectorSearch
```

### Azure Database for PostgreSQL

>[Azure Database for PostgreSQL - Flexible Server](https://learn.microsoft.com/en-us/azure/postgresql/flexible-server/service-overview) 是一个基于开源 Postgres 数据库引擎的关系型数据库服务。它是一个全托管数据库即服务平台，可以以可预测的性能、安全性、高可用性和动态扩展性支撑关键任务工作负载。

关于 Azure Database for PostgreSQL 的设置，参见 [set up instructions](https://learn.microsoft.com/en-us/azure/postgresql/flexible-server/quickstart-create-server-portal)。

只需使用 Azure Portal 中的[连接字符串](https://learn.microsoft.com/en-us/azure/postgresql/flexible-server/connect-python?tabs=cmd%2Cpassword#add-authentication-code)即可。

由于 Azure Database for PostgreSQL 基于开源 Postgres，你可以使用 [LangChain 的 Postgres 支持](/oss/integrations/vectorstores/pgvector/) 连接它。

### Azure SQL Database

>[Azure SQL Database](https://learn.microsoft.com/azure/azure-sql/database/sql-database-paas-overview?view=azuresql) 是一项稳健的服务，兼具可扩展性、安全性与高可用性，具备现代数据库解决方案的全部优势。它还提供专用的 Vector 数据类型和内置函数，可直接在关系数据库中简化向量嵌入的存储与查询。这消除了对独立向量数据库及其相关集成的需求，在降低整体复杂度的同时提升解决方案安全性。

通过在现有 SQL Server 数据库上实现向量搜索，你可以增强数据能力、减少开支，并避免迁移到新系统带来的挑战。

##### 安装与设置

参见[详细配置说明](/oss/integrations/vectorstores/sqlserver)。

需要安装 `langchain-sqlserver` Python 包。

```bash
!pip install langchain-sqlserver==0.1.1
```

##### 在 Microsoft Azure 上部署 Azure SQL DB

[立即免费注册](https://learn.microsoft.com/azure/azure-sql/database/free-offer?view=azuresql)开始使用。

参见[使用示例](/oss/integrations/vectorstores/sqlserver)。

```python
from langchain_sqlserver import SQLServer_VectorStore
```

## Vector store

### Azure Database for PostgreSQL

>[Azure Database for PostgreSQL - Flexible Server](https://learn.microsoft.com/en-us/azure/postgresql/flexible-server/service-overview) 是一个基于开源 Postgres 数据库引擎的关系型数据库服务。它是一个全托管数据库即服务平台，可以以可预测的性能、安全性、高可用性和动态扩展性支撑关键任务工作负载。

关于 Azure Database for PostgreSQL 的设置，参见 [set up instructions](https://learn.microsoft.com/en-us/azure/postgresql/flexible-server/quickstart-create-server-portal)。

要将 Postgres 用作 vector store，你需要在数据库中[启用 pgvector extension](https://learn.microsoft.com/en-us/azure/postgresql/flexible-server/how-to-use-pgvector)。启用扩展后，即可使用 [LangChain 中的 PGVector](/oss/integrations/vectorstores/pgvector/) 连接 Azure Database for PostgreSQL。

参见[使用示例](/oss/integrations/vectorstores/pgvector/)。直接使用 Azure Portal 中的[连接字符串](https://learn.microsoft.com/en-us/azure/postgresql/flexible-server/connect-python?tabs=cmd%2Cpassword#add-authentication-code)即可。

## Tools

### Microsoft Foundry tools

Microsoft Foundry 暴露了一组 LangChain service tools，用于访问 Azure AI Content Understanding、Document Intelligence、Image Analysis 和 Text Analytics for Health。

使用 `tools` extra 安装该包：

<CodeGroup>
    ```bash pip
    pip install -U "langchain-azure-ai[tools]"
    ```

    ```bash uv
    uv add "langchain-azure-ai[tools]"
    ```
</CodeGroup>

参见 [Microsoft Foundry Tools guide](/oss/integrations/tools/azure_ai_services)。

```python
from langchain_azure_ai.tools import AzureAIDocumentIntelligenceTool
```

### Image generation tool

Microsoft Foundry Models 在模型目录中提供了多个图像生成模型。

参见 [Microsoft Foundry tools guide](/oss/integrations/tools/azure_ai)。

```python
from langchain_azure_ai.tools import AzureOpenAIModelImageGenTool
```

### Transcriptions tool

Microsoft Foundry Models 在模型目录中提供了 Whisper 模型，用于 speech-to-text 转录。

参见 [Microsoft Foundry tools guide](/oss/integrations/tools/azure_ai)。

```python
from langchain_azure_ai.tools import AzureOpenAITranscriptionsTool
```

### Code interpreter tool（server-side）

在沙箱容器中服务端执行 Python 代码。

参见 [Microsoft Foundry tools guide](/oss/integrations/tools/azure_ai)。

```python
from langchain_azure_ai.tools.builtin import CodeInterpreterTool
```

### Web search tool（server-side）

搜索互联网中的最新信息和来源。

参见 [Microsoft Foundry tools guide](/oss/integrations/tools/azure_ai)。

```python
from langchain_azure_ai.tools.builtin import WebSearchTool
```

### File search tool（server-side）

在 vector stores 中搜索相关文档内容。

参见 [Microsoft Foundry tools guide](/oss/integrations/tools/azure_ai)。

```python
from langchain_azure_ai.tools.builtin import FileSearchTool
```

### Image generation tool（server-side）

在 Azure AI Foundry 中服务端使用 GPT 图像模型生成或编辑图像。

参见 [Microsoft Foundry tools guide](/oss/integrations/tools/azure_ai)。

```python
from langchain_azure_ai.tools.builtin import ImageGenerationTool
```

### MCP tool（server-side）

访问外部 Model Context Protocol (MCP) 服务器。

参见 [Microsoft Foundry tools guide](/oss/integrations/tools/azure_ai)。

```python
from langchain_azure_ai.tools.builtin import McpTool
```

### Azure Container Apps Dynamic Sessions

你需要从 Azure Container Apps 服务中获取环境变量 `POOL_MANAGEMENT_ENDPOINT`。
参见 [Azure dynamic sessions setup instructions](/oss/integrations/tools/azure_dynamic_sessions/#setup)。

需要安装一个 Python 包。

<CodeGroup>
    ```bash pip
    pip install langchain-azure-dynamic-sessions
    ```

    ```bash uv
    uv add langchain-azure-dynamic-sessions
    ```
</CodeGroup>

参见[使用示例](/oss/integrations/tools/azure_dynamic_sessions)。

```python
from langchain_azure_dynamic_sessions import SessionsPythonREPLTool
```

### Azure Logic Apps

触发 Azure Logic Apps workflows，以自动化业务流程和集成。

使用 `tools` extra 安装该包：

<CodeGroup>
    ```bash pip
    pip install -U "langchain-azure-ai[tools]"
    ```

    ```bash uv
    uv add "langchain-azure-ai[tools]"
    ```
</CodeGroup>

参见 [Azure Logic Apps integration guide](/oss/integrations/tools/azure_logic_apps)。

```python
from langchain_azure_ai.tools import AzureLogicAppTool
```

## Toolkits

### Microsoft Foundry Project Toolbox

通过 Model Context Protocol (MCP) 从 Azure AI Foundry Toolbox 动态加载 tools。

使用 `tools` extra 安装该包：

<CodeGroup>
    ```bash pip
    pip install -U "langchain-azure-ai[tools]" langchain-mcp-adapters httpx
    ```

    ```bash uv
    uv add "langchain-azure-ai[tools]" langchain-mcp-adapters httpx
    ```
</CodeGroup>

参见 [Azure AI Foundry Toolbox guide](/oss/integrations/tools/azure_ai#azureaiprojecttoolbox)。

```python
from langchain_azure_ai.tools import AzureAIProjectToolbox
```

### Microsoft Foundry tools（原 Azure AI Services）

安装集成包：

<CodeGroup>
    ```bash pip
    pip install -U "langchain-azure-ai[tools]"
    ```
    ```bash uv
    uv add "langchain-azure-ai[tools]"
    ```
</CodeGroup>

参见[使用示例](/oss/integrations/tools/azure_ai_services)。

```python
from langchain_azure_ai.tools import AzureAIServicesToolkit
```

`AzureAIServicesToolkit` 工具包包含以下工具：

- Image Analysis: [AzureAIImageAnalysisTool](/oss/integrations/tools/azure_ai_services#azureaiimageanalysistool)
- Document Intelligence: [AzureAIDocumentIntelligenceTool](/oss/integrations/tools/azure_ai_services#azureaidocumentintelligencetool)
- Speech to Text: [AzureAISpeechToTextTool](/oss/integrations/tools/azure_ai_services#azureaispeechtotexttool)
- Text to Speech: [AzureAITextToSpeechTool](/oss/integrations/tools/azure_ai_services#azureaitexttospeechtool)
- Text Analytics for Health: [AzureAITextAnalyticsHealthTool](/oss/integrations/tools/azure_ai_services#azureaitextanalyticshealthtool)
