---
title: "Linkup 集成"
description: "使用 LangChain Python 集成 Linkup。"
---

> [Linkup](https://www.linkup.so/) 提供一个 API，可将 LLM 连接到网络和 Linkup Premium Partner 数据源。

## 安装和设置

要使用 Linkup provider，您首先需要一个有效的 API key，可通过[在 Linkup 网站注册](https://app.linkup.so/sign-up)获取。
您还需要 `langchain-linkup` 包，可通过 pip 安装：

<CodeGroup>
```bash pip
pip install langchain-linkup
```

```bash uv
uv add langchain-linkup
```
</CodeGroup>

## 检索器

请参阅[用法示例](/oss/integrations/retrievers/linkup_search)。

```python
from langchain_linkup import LinkupSearchRetriever

retriever = LinkupSearchRetriever(
    depth="deep",  # "standard" or "deep"
    linkup_api_key=None,  # API key can be passed here or set as the LINKUP_API_KEY environment variable
)
```

## 工具

请参阅[用法示例](/oss/integrations/tools/linkup_search)。

```python
from langchain_linkup import LinkupSearchTool

tool = LinkupSearchTool(
    depth="deep",  # "standard" or "deep"
    output_type="searchResults",  # "searchResults", "sourcedAnswer" or "structured"
    linkup_api_key=None,  # API key can be passed here or set as the LINKUP_API_KEY environment variable
)
```
