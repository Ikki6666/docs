---
title: "Metal 集成"
description: "使用 LangChain Python 集成 Metal。"
---

本页介绍如何在 LangChain 中使用 [Metal](https://getmetal.io)。

## 什么是 metal？

Metal 是一个面向生产环境构建的托管检索与记忆平台。你可以轻松把数据索引到 `Metal` 中，并在其上运行语义搜索和检索。

## 快速开始

先[创建一个 Metal 账户](https://app.getmetal.io/signup)。

之后，你可以轻松使用 `MetalRetriever` 类来检索数据，用于语义搜索、提示上下文等场景。这个类接收一个 `Metal` 实例，以及一个要传给 Metal API 的参数字典。

```python
from langchain_classic.retrievers import MetalRetriever
from metal_sdk.metal import Metal


metal = Metal("API_KEY", "CLIENT_ID", "INDEX_ID");
retriever = MetalRetriever(metal, params={"limit": 2})

docs = retriever.invoke("search term")
```
