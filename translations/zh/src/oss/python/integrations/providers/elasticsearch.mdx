---
title: "Elasticsearch 集成"
description: "使用 LangChain Python 集成 Elasticsearch。"
---

> [Elasticsearch](https://www.elastic.co/elasticsearch/) 是一个分布式、RESTful 的搜索与分析引擎。
> 它提供了一个分布式、支持多租户的全文搜索引擎，带有 HTTP Web 接口和无模式的 JSON 文档。

## 安装和设置

### 设置 Elasticsearch

有两种方式可以开始使用 Elasticsearch：

#### 在本地机器上安装 Elasticsearch

在本地运行 Elasticsearch 进行开发和测试的最简单方式，是使用 [start-local](https://github.com/elastic/start-local) 脚本。该脚本通过一条简单的命令使用 Docker 设置 Elasticsearch。

```bash
curl -fsSL https://elastic.co/start-local | sh
```

这会创建一个 `elastic-start-local` 文件夹。要启动 Elasticsearch：

```bash
cd elastic-start-local
./start.sh
```

Elasticsearch 将可通过 `http://localhost:9200` 访问。`elastic` 用户的密码和 API 密钥会自动生成，并存储在 `elastic-start-local` 文件夹中的 `.env` 文件里。

如果您只需要 Elasticsearch 而不需要 Kibana，可以使用 `--esonly` 选项：

```bash
curl -fsSL https://elastic.co/start-local | sh -s -- --esonly
```

<Note>
start-local 设置仅用于本地测试，不应在生产环境中使用。生产安装请参阅官方 [Elasticsearch documentation](https://www.elastic.co/guide/en/elasticsearch/reference/current/install-elasticsearch.html)。
</Note>

#### 在 elastic cloud 上部署 Elasticsearch

`Elastic Cloud` 是一个托管版 Elasticsearch 服务。可以注册 [free trial](https://cloud.elastic.co/registration?utm_source=langchain&utm_content=documentation)。

### 安装客户端

<CodeGroup>
```bash pip
pip install elasticsearch
pip install langchain-elasticsearch
```

```bash uv
uv add elasticsearch
uv add langchain-elasticsearch
```
</CodeGroup>

## 嵌入模型

请参阅[用法示例](/oss/integrations/embeddings/elasticsearch)。

```python
from langchain_elasticsearch import ElasticsearchEmbeddings
```

## 向量存储

请参阅[用法示例](/oss/integrations/vectorstores/elasticsearch)。

```python
from langchain_elasticsearch import ElasticsearchStore
```

## 检索器

### ElasticsearchRetriever

`ElasticsearchRetriever` 通过 Query DSL 提供对 Elasticsearch 全部功能的灵活访问。

请参阅[用法示例](/oss/integrations/retrievers/elasticsearch_retriever)。

```python
from langchain_elasticsearch import ElasticsearchRetriever
```

## LLM cache

```python
from langchain_elasticsearch import ElasticsearchCache
```

## Byte store

请参阅[用法示例](/oss/integrations/stores/elasticsearch)。

```python
from langchain_elasticsearch import ElasticsearchEmbeddingsCache
```

## Chain

这是一个用于与 Elasticsearch Database 交互的 chain。

```python
from langchain_classic.chains.elasticsearch_database import ElasticsearchDatabaseChain
```
