---
title: "Gel 集成"
description: "使用 LangChain Python 集成 Gel。"
---

[Gel](https://www.geldata.com/) 是一个构建在 PostgreSQL 之上的强大数据平台。

- 以对象和图的方式思考，而不是表和 JOIN。
- 使用高级 Python SDK、集成 GUI、迁移引擎、Auth 和 AI 层等丰富能力。
- 可以在本地、远程或[全托管云](https://www.geldata.com/cloud)中运行。

## 安装

<CodeGroup>
```bash pip
pip install langchain-gel
```

```bash uv
uv add langchain-gel
```
</CodeGroup>

## 设置

1. 运行 `gel project init`
2. 编辑 schema。要使用 LangChain vectorstore，您需要以下类型：

```gel
using extension pgvector;

module default {
    scalar type EmbeddingVector extending ext::pgvector::vector<1536>;

    type Record {
        required collection: str;
        text: str;
        embedding: EmbeddingVector;
        external_id: str {
            constraint exclusive;
        };
        metadata: json;

        index ext::pgvector::hnsw_cosine(m := 16, ef_construction := 128)
            on (.embedding)
    }
}
```

> 注意：这只是最小化设置。您可以根据需要自由添加更多类型、属性和链接。
> 想进一步了解如何利用 Gel 的 schema，请阅读 [docs](https://docs.geldata.com/learn/schema)。

3. 运行迁移：`gel migration create && gel migrate`。

## 用法

```python
from langchain_gel import GelVectorStore

vector_store = GelVectorStore(
    embeddings=embeddings,
)
```

请参阅 [完整的 GEL vectorstore 用法示例](/oss/integrations/vectorstores/gel)。
