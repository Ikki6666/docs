---
title: "(MODULE_NAME) 集成"
description: "使用 LangChain Python 集成 (MODULE_NAME) 聊天模型。"
---
---

这个 Python 聊天模型模板的使用方式如下：

- [ ] 将 (MODULE_NAME) 替换为模块名称，例如 Anthropic、OpenAI 等（这里可以用 `command + F`）
- [ ] 更新链接，使其指向正确的模块
- [ ] 在详情表和功能表中，更新 ✅/❌ 以反映该聊天模型的实际能力
- [ ] 如有需要，更新 PyPI/registry 包名
- [ ] 如有需要，更新 API 密钥环境变量名

模板从下面开始。

本指南简要介绍如何开始使用 (MODULE_NAME) [chat model](/oss/langchain/models)。如需查看 Chat(MODULE_NAME) 的全部功能、参数和配置详情，请前往 [Chat(MODULE_NAME) API reference](https://reference.langchain.com/python/langchain-(MODULE_NAME)/chat_models/Chat(MODULE_NAME))。

## 概览

### 详情

| Class | Package | Serializable | [JS support](https://js.langchain.com/docs/integrations/chat/anthropic) | Downloads | Version |
| :--- | :--- | :---: |  :---: | :---: | :---: |
| [Chat(MODULE_NAME)](https://reference.langchain.com/python/langchain-(MODULE_NAME)/chat_models/Chat(MODULE_NAME)) | [langchain-(MODULE_NAME)](https://reference.langchain.com/python/langchain-(MODULE_NAME)) | beta/❌ | ✅/❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-(MODULE_NAME)?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-(MODULE_NAME)?style=flat-square&label=%20) |

### 功能

| [Tool calling](/oss/langchain/tools) | [Structured output](/oss/langchain/structured-output/) | [Image input](/oss/langchain/messages#multimodal) | [Audio input](/oss/langchain/messages#multimodal) | [Video input](/oss/langchain/messages#multimodal) | [Token-level streaming](/oss/langchain/streaming/) | Native async | [Token usage](/oss/langchain/models#token-usage) | [Logprobs](/oss/langchain/models#log-probabilities) |
| :---: | :---: | :---: |  :---: | :---: | :---: | :---: | :---: | :---: |
| ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅❌ |

---

## 设置

要访问 (MODULE_NAME) 模型，需要创建一个 (MODULE_NAME) 账号、获取 API 密钥，并安装 `langchain-(MODULE_NAME)` 集成包。

### 凭证

```python Set API key icon="key"
import getpass
import os

if "(MODULE_NAME)_API_KEY" not in os.environ:
    os.environ["(MODULE_NAME)_API_KEY"] = getpass.getpass("Enter your (MODULE_NAME) API key: ")
```

要为模型调用启用自动 <Tooltip tip="Log each step of a model's execution to debug and improve it">tracing</Tooltip>，请设置 [LangSmith](/langsmith/observability) API 密钥：

```python Enable tracing icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装

LangChain 的 (MODULE_NAME) 集成位于 `langchain-(MODULE_NAME)` 包中：

<CodeGroup>
    ```python pip
    pip install -U langchain-(MODULE_NAME)
    ```
    ```python uv
    uv add langchain-(MODULE_NAME)
    ```
</CodeGroup>

---

## 实例化

现在可以实例化模型对象并生成响应：

```python Initialize chat model icon="robot"
from langchain_(MODULE_NAME) import Chat(MODULE_NAME)

model = Chat(MODULE_NAME)(
    model="model-name",
    temperature=0,
    timeout=None,
    max_tokens=1024,
    max_retries=2,
    # 其他参数，完整列表见 API reference
)
```

---

## 调用

<CodeGroup>
    ```python Dictionary format icon="book"
    messages = [
        {"role": "system", "content": "You are a poetry expert"},
        {"role": "user", "content": "Write a haiku about spring"},
    ]
    response = model.invoke(messages)
    print(response)
    ```
    ```python Message objects icon="message"
    from langchain.messages import SystemMessage, HumanMessage, AIMessage

    messages = [
        SystemMessage("You are a poetry expert"),
        HumanMessage("Write a haiku about spring"),
        AIMessage("Cherry blossoms bloom...")
    ]
    response = model.invoke(messages)
    ```
</CodeGroup>

```plaintext Response object icon="terminal"
TODO - replace with response.model_dump_json(indent=2) or similar
```

```python Text content icon="cursor-text"
print(response.text)

# TODO - replace with the output of response.text
```

```python Content Blocks icon="layout-grid"
print(response.content_blocks)

# TODO - replace with the output of response.content_blocks
```

<Tip>
    完整指南可参见 [chat model invocation types](/oss/langchain/models#invocation)、[message types](/oss/langchain/messages#message-types) 和 [content blocks](/oss/langchain/messages#standard-content-blocks)。
</Tip>

## TODO: 此模型的专属功能

如果不相关，可以删除。

可以参考现有模型文档中的示例，例如：

- [ChatAnthropic](/oss/integrations/chat/anthropic)
- [ChatOpenAI](/oss/integrations/chat/openai)
- [ChatGenAI](/oss/integrations/chat/google_generative_ai)

示例：
- <Icon icon="tool" size={16}/> Tool calling
- <Icon icon="brain" size={16}/> Reasoning output / extended reasoning
- <Icon icon="speakerphone" size={16}/> Verbosity
- <Icon icon="quote" size={16}/> Citations
- <Icon icon="terminal" size={16}/> Built-in tools
    - <Icon icon="clipboard" size={16}/> Web search
    - <Icon icon="code" size={16}/> Code execution
    - <Icon icon="rss" size={16}/> Remote MCP
- <Icon icon="movie" size={16}/> Multimodal input / output
- <Icon icon="database" size={16}/> Caching
- <Icon icon="link" size={16}/> Chaining
- <Icon icon="wifi" size={16}/> Streaming usage metadata
- <Icon icon="test-pipe" size={16}/> Fine-tuning
- <Icon icon="cpu" size={16}/> Flex processing
- <Icon icon="timeline" size={16}/> Custom base URL or proxy behavior

---

## API reference

如需查看 Chat(MODULE_NAME) 的全部功能和配置详情，请前往 [API reference](<LINK>)。
