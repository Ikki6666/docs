---
title: "ChatAbso 集成"
description: "使用 LangChain Python 集成 ChatAbso 聊天模型。"
---

本文将帮助你开始使用 `ChatAbso` [聊天模型](/oss/langchain/models)。

你也可以查看完整的 [Abso router 文档](https://abso.ai)。

## 概览

### 集成详情

| Class | Package | Serializable | [JS support](https://js.langchain.com/docs/integrations/chat/abso) | Downloads | Version |
| :--- | :--- | :---: |  :---: | :---: | :---: |
| `ChatAbso` | `langchain-abso` | ❌ | ❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-abso?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-abso?style=flat-square&label=%20) |

## 设置

要访问 ChatAbso 模型，你需要创建一个 OpenAI 账户、获取 API key，并安装 `langchain-abso` 集成包。

### 凭证

- TODO: Update with relevant info.

前往（TODO: link）注册 ChatAbso 并生成 API key。完成后，设置 `ABSO_API_KEY` 环境变量：

```python
import getpass
import os

if not os.getenv("OPENAI_API_KEY"):
    os.environ["OPENAI_API_KEY"] = getpass.getpass("Enter your OpenAI API key: ")
```

### 安装

LangChain ChatAbso 集成位于 `langchain-abso` 包中：

```python
pip install -qU langchain-abso
```

## 实例化

现在可以实例化模型对象并生成聊天补全：

```python
from langchain_abso import ChatAbso

llm = ChatAbso(fast_model="gpt-5.5", slow_model="o3-mini")
```

## 调用

```python
messages = [
    (
        "system",
        "You are a helpful assistant that translates English to French. Translate the user sentence.",
    ),
    ("human", "I love programming."),
]
ai_msg = llm.invoke(messages)
ai_msg
```

```python
print(ai_msg.content)
```

---
