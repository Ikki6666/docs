---
title: "Runpod 集成"
description: "使用 LangChain Python 集成 Runpod 聊天模型。"
---

开始使用 RunPod 聊天模型。

## 概览

本指南介绍如何使用 LangChain 的 `ChatRunPod` 类，与部署在 [RunPod Serverless](https://www.runpod.io/serverless-gpu) 上的聊天模型进行交互。

## 设置

1. **安装包：**

    ```bash
    pip install -qU langchain-runpod
    ```

2. **部署聊天模型 Endpoint：** 按照 [RunPod Provider Guide](/oss/integrations/providers/runpod#setup) 中的步骤，在 RunPod Serverless 上部署兼容的聊天模型 endpoint，并获取其 Endpoint ID。
3. **设置环境变量：** 确保已经设置 `RUNPOD_API_KEY` 和 `RUNPOD_ENDPOINT_ID`（或更具体的 `RUNPOD_CHAT_ENDPOINT_ID`）。

```python
import getpass
import os

# Make sure environment variables are set (or pass them directly to ChatRunPod)
if "RUNPOD_API_KEY" not in os.environ:
    os.environ["RUNPOD_API_KEY"] = getpass.getpass("Enter your RunPod API Key: ")

if "RUNPOD_ENDPOINT_ID" not in os.environ:
    os.environ["RUNPOD_ENDPOINT_ID"] = input(
        "Enter your RunPod Endpoint ID (used if RUNPOD_CHAT_ENDPOINT_ID is not set): "
    )

# Optionally use a different endpoint ID specifically for chat models
# if "RUNPOD_CHAT_ENDPOINT_ID" not in os.environ:
#     os.environ["RUNPOD_CHAT_ENDPOINT_ID"] = input("Enter your RunPod Chat Endpoint ID (Optional): ")

chat_endpoint_id = os.environ.get(
    "RUNPOD_CHAT_ENDPOINT_ID", os.environ.get("RUNPOD_ENDPOINT_ID")
)
if not chat_endpoint_id:
    raise ValueError(
        "No RunPod Endpoint ID found. Please set RUNPOD_ENDPOINT_ID or RUNPOD_CHAT_ENDPOINT_ID."
    )
```

## 实例化

初始化 `ChatRunPod` 类。你可以通过 `model_kwargs` 传入模型特定参数，并配置轮询行为。

```python
from langchain_runpod import ChatRunPod

chat = ChatRunPod(
    runpod_endpoint_id=chat_endpoint_id,  # Specify the correct endpoint ID
    model_kwargs={
        "max_new_tokens": 512,
        "temperature": 0.7,
        "top_p": 0.9,
        # Add other parameters supported by your endpoint handler
    },
    # Optional: Adjust polling
    # poll_interval=0.2,
    # max_polling_attempts=150
)
```

## 调用

使用标准 LangChain 的 `.invoke()` 和 `.ainvoke()` 方法调用模型。也支持通过 `.stream()` 和 `.astream()` 进行流式输出，这一能力是通过轮询 RunPod 的 `/stream` endpoint 模拟实现的。

```python
from langchain.messages import HumanMessage, SystemMessage

messages = [
    SystemMessage(content="You are a helpful AI assistant."),
    HumanMessage(content="What is the RunPod Serverless API flow?"),
]

# Invoke (Sync)
try:
    response = chat.invoke(messages)
    print("--- Sync Invoke Response ---")
    print(response.content)
except Exception as e:
    print(
        f"Error invoking Chat Model: {e}. Ensure endpoint ID/API key are correct and endpoint is active/compatible."
    )

# Stream (Sync, simulated via polling /stream)
print("\n--- Sync Stream Response ---")
try:
    stream = chat.stream_events(messages, version="v3")
    for token in stream.text:
        print(token, end="", flush=True)
    print()  # Newline
except Exception as e:
    print(
        f"\nError streaming Chat Model: {e}. Ensure endpoint handler supports streaming output format."
    )

### Async Usage

# AInvoke (Async)
try:
    async_response = await chat.ainvoke(messages)
    print("--- Async Invoke Response ---")
    print(async_response.content)
except Exception as e:
    print(f"Error invoking Chat Model asynchronously: {e}.")

# AStream (Async)
print("\n--- Async Stream Response ---")
try:
    stream = await chat.astream_events(messages, version="v3")
    async for token in stream.text:
        print(token, end="", flush=True)
    print()  # Newline
except Exception as e:
    print(
        f"\nError streaming Chat Model asynchronously: {e}. Ensure endpoint handler supports streaming output format.\n"
    )
```

## 链式调用

这个聊天模型可以无缝集成到 LangChain Expression Language（LCEL）链中。

```python
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import ChatPromptTemplate

prompt = ChatPromptTemplate.from_messages(
    [
        ("system", "You are a helpful assistant."),
        ("human", "{input}"),
    ]
)

parser = StrOutputParser()

chain = prompt | chat | parser

try:
    chain_response = chain.invoke(
        {"input": "Explain the concept of serverless computing in simple terms."}
    )
    print("--- Chain Response ---")
    print(chain_response)
except Exception as e:
    print(f"Error running chain: {e}")


# Async chain
try:
    async_chain_response = await chain.ainvoke(
        {"input": "What are the benefits of using RunPod for AI/ML workloads?"}
    )
    print("--- Async Chain Response ---")
    print(async_chain_response)
except Exception as e:
    print(f"Error running async chain: {e}")
```

## 模型特性（取决于 endpoint）

高级能力是否可用，**高度依赖** 你所部署的 RunPod endpoint handler 的具体实现。`ChatRunPod` 集成只提供基础框架，而底层 handler 必须支持相应功能。

| Feature                                                    | Integration Support | Endpoint Dependent? | Notes                                                                                                                                                                      |
| :--------------------------------------------------------- | :-----------------: | :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Tool calling](/oss/langchain/tools)                | ❌                  | ✅                  | 要求 handler 能处理工具定义并返回工具调用（例如 OpenAI 格式）。集成层还需要具备相应的解析逻辑。                                                 |
| [Structured output](/oss/langchain/structured-output)        | ❌                  | ✅                  |
| ❌                  | ✅                  | 要求 handler 接受 `json_mode` 参数（或类似参数），并保证输出 JSON。                                                                               |
| ❌                  | ✅                  | 要求多模态 handler 能接收图像数据（例如 base64）。当前集成不支持多模态消息。                                                       |
| ❌                  | ✅                  | 要求 handler 能接收音频数据。当前集成不支持音频消息。                                                                                        |
| ❌                  | ✅                  | 要求 handler 能接收视频数据。当前集成不支持视频消息。                                                                                        |
| ✅ (Simulated)      | ✅                  | 通过轮询 `/stream` 实现。要求 handler 在状态响应中填充 `stream` 列表并包含 token 块（例如 `[{"output": "token"}]`）。当前不提供真正低延迟的原生流式能力。 |
| ✅                  | ✅                  | 核心的 `ainvoke`/`astream` 已实现，实际表现依赖 endpoint handler 的性能。                                                                                              |
| ❌                  | ✅                  | 要求 handler 在最终响应中返回 `prompt_tokens`、`completion_tokens`。当前集成尚未解析这些字段。                                           |
| ❌                  | ✅                  | 要求 handler 返回 log probabilities。当前集成尚未解析这些字段。                                                                                  |

**关键结论：** 如果 endpoint 遵循基本的 RunPod API 约定，那么标准聊天调用和模拟流式输出可以正常工作。高级能力则需要更具体的 handler 实现，并且可能需要扩展或自定义这个集成包。

---

## API 参考

如需查看 `ChatRunPod` 类、参数和方法的详细文档，请参考源码或生成的 API reference（如果可用）。

源码链接：[https://github.com/runpod/langchain-runpod/blob/main/langchain_runpod/chat_models.py](https://github.com/runpod/langchain-runpod/blob/main/langchain_runpod/chat_models.py)
