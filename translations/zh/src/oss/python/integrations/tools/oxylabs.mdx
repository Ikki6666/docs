---
title: "Oxylabs 集成"
description: "使用 LangChain Python 与 Oxylabs tool 集成。"
---

>[Oxylabs](https://oxylabs.io/) 是市场领先的 Web intelligence collection platform，以高标准的商业规范、伦理和合规要求驱动，帮助全球企业释放数据驱动洞察。

## 概览

该包包含了与 Oxylabs 的 LangChain 集成，可通过 LangChain 框架使用 Oxylabs Web Scraper API 抓取 Google 搜索结果。

这个包提供以下类：

- `OxylabsSearchRun`：以格式化文本形式返回抓取到的 Google 搜索结果
- `OxylabsSearchResults`：以 JSON 格式返回抓取到的 Google 搜索结果
- `OxylabsSearchAPIWrapper`：用于初始化 Oxylabs API 的封装器

|             Pricing             |
|:-------------------------------:|
| ✅ 首周免费 5,000 条结果 |

## 设置

安装所需依赖。

```python
pip install -qU langchain-oxylabs
```

### 凭据

设置正确的 API key 和环境变量。创建你的 API 用户凭据：在 [Oxylabs dashboard](https://dashboard.oxylabs.io/en/registration) 中注册试用或购买产品后，即可创建 API 用户凭据（`OXYLABS_USERNAME` 和 `OXYLABS_PASSWORD`）。

```python
import getpass
import os

os.environ["OXYLABS_USERNAME"] = getpass.getpass("Enter your Oxylabs username: ")
os.environ["OXYLABS_PASSWORD"] = getpass.getpass("Enter your Oxylabs password: ")
```

## 实例化

```python
from langchain_oxylabs import OxylabsSearchAPIWrapper, OxylabsSearchRun

oxylabs_wrapper = OxylabsSearchAPIWrapper()
tool_ = OxylabsSearchRun(wrapper=oxylabs_wrapper)
```

## 调用

### 直接带参数调用

`OxylabsSearchRun` 工具只接受一个 `query` 参数，它应该是自然语言查询，并返回组合后的字符串格式结果：

```python
tool_.invoke({"query": "Restaurants in Paris."})
```

### 使用 ToolCall 调用

```python
tool_ = OxylabsSearchRun(
    wrapper=oxylabs_wrapper,
    kwargs={
        "result_categories": [
            "local_information",
            "combined_search_result",
        ]
    },
)
```

```python
from pprint import pprint

model_generated_tool_call = {
    "args": {
        "query": "Visit restaurants in Vilnius.",
        "geo_location": "Vilnius,Lithuania",
    },
    "id": "1",
    "name": "oxylabs_search",
    "type": "tool_call",
}
tool_call_result = tool_.invoke(model_generated_tool_call)

# The content is a JSON string of results
pprint(tool_call_result.content)
```

## 在 agent 中使用

安装所需依赖。

```python
pip install -qU "langchain[openai]" langgraph
```

```python
import getpass
import os

from langchain.chat_models import init_chat_model

os.environ["OPENAI_API_KEY"] = getpass.getpass("Enter API key for OpenAI: ")
model = init_chat_model("gpt-5.4-mini", model_provider="openai")
```

```python
from langchain.agents import create_agent


# Initialize OxylabsSearchRun tool
tool_ = OxylabsSearchRun(wrapper=oxylabs_wrapper)

agent = create_agent(model, [tool_])

user_input = "What happened in the latest Burning Man floods?"

stream = agent.stream_events({"messages": user_input}, version="v3")
for snapshot in stream.values:
    snapshot["messages"][-1].pretty_print()
```

## JSON 结果

`OxylabsSearchResults` 工具可以作为 `OxylabsSearchRun` 的替代方案，以 JSON 格式获取结果：

```python
import json

from langchain_oxylabs import OxylabsSearchResults

tool_ = OxylabsSearchResults(wrapper=oxylabs_wrapper)

response_results = tool_.invoke({"query": "What are the most famous artists?"})
response_results = json.loads(response_results)

for result in response_results:
    for key, value in result.items():
        print(f"{key}: {value}")
```

---

## API reference

关于此集成包的更多信息请见：[github.com/oxylabs/langchain-oxylabs](https://github.com/oxylabs/langchain-oxylabs)

Oxylabs Web Scraper API 文档：[developers.oxylabs.io/scraper-apis/web-scraper-api](https://developers.oxylabs.io/scraper-apis/web-scraper-api)
