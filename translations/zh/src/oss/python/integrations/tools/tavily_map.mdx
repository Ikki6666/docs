---
title: "Tavily map 集成"
description: "使用 LangChain Python 与 Tavily map tool 集成。"
---

[Tavily](https://tavily.com) 是一个专为 AI agents（LLMs）打造的搜索引擎，能够快速提供实时、准确、基于事实的结果。Tavily 提供了一个 [Map](https://docs.tavily.com/documentation/api-reference/endpoint/map) endpoint，可以遍历网站并返回发现的 URL 列表，而不提取页面内容，这非常适合理解网站结构，或者在大型网站中定位特定页面。

## 概览

### 集成详情

| Class                                                          | Package                                                          | Serializable | [JS support](https://js.langchain.com/docs/integrations/tools/tavily_map) |  Version |
|:---------------------------------------------------------------|:-----------------------------------------------------------------| :---: | :---: | :---: |
| @[`TavilyMap`] | @[`langchain-tavily`] | ✅ | ✅  |  ![PyPI - Version](https://img.shields.io/pypi/v/langchain-tavily?style=flat-square&label=%20) |

### 工具特性

| [Returns artifact](/oss/langchain/tools) | Native async |                       Return data                        | Pricing |
| :---: | :---: |:--------------------------------------------------------:| :---: |
| ❌ | ✅ | 已发现 URL 的列表 | 每月 1,000 免费额度 |

## 设置

该集成位于 `langchain-tavily` 包中。

```python
pip install -qU langchain-tavily
```

### 凭据

我们还需要设置 Tavily API key。你可以访问[这个网站](https://app.tavily.com/sign-in)创建账户并获取 API key。

```python
import getpass
import os

if not os.environ.get("TAVILY_API_KEY"):
    os.environ["TAVILY_API_KEY"] = getpass.getpass("Tavily API key:\n")
```

## 实例化

该工具在实例化时接受以下参数：

- `max_depth`（可选，int）：从起始 URL 开始的最大跳数。默认是 1。
- `max_breadth`（可选，int）：每一层最多返回的 URL 数量。默认是 20。
- `limit`（可选，int）：总共最多返回的 URL 数量。默认是 50。
- `instructions`（可选，str）：用于指导 map 遍历的自然语言说明。
- `select_paths`（可选，list[str]）：仅包含 URL 中匹配这些 path regex 的页面。
- `select_domains`（可选，list[str]）：仅包含匹配这些 domain regex 的域名。
- `exclude_paths`（可选，list[str]）：跳过 URL 中匹配这些 path regex 的页面。
- `exclude_domains`（可选，list[str]）：跳过匹配这些 domain regex 的域名。
- `allow_external`（可选，bool）：允许 map 跟随外部链接。

更完整的参数说明，请参阅 [Tavily Map API documentation](https://docs.tavily.com/documentation/api-reference/endpoint/map)。

```python
from langchain_tavily import TavilyMap

tool = TavilyMap(
    max_depth=1,
    max_breadth=20,
    limit=50,
    # allow_external=False,
)
```

## 调用

### [直接带参数调用](/oss/langchain/tools)

Tavily map tool 在调用时接受以下参数：

- `url`（必填）：开始映射的基础 URL。
- 以下参数也可以在调用时传入：`instructions`、`select_paths`、`select_domains`、`exclude_paths`、`exclude_domains`、`allow_external`。

注意：这些可选参数也支持由 agent 动态设置。如果你在实例化时设置了某个参数，但在调用时又传了不同值，那么工具会使用调用时传入的值。

```python
tool.invoke({"url": "https://docs.tavily.com"})
```

```json
{
  "base_url": "https://docs.tavily.com",
  "results": [
    "https://docs.tavily.com/",
    "https://docs.tavily.com/changelog",
    "https://docs.tavily.com/welcome",
    "https://docs.tavily.com/documentation/mcp",
    "https://docs.tavily.com/documentation/about",
  ],
  "response_time": 0.37
}
```

### [使用 ToolCall 调用](/oss/langchain/tools)

我们也可以用模型生成的 ToolCall 来调用该工具，此时会返回一个 ToolMessage：

```python
model_generated_tool_call = {
    "args": {"url": "https://docs.tavily.com", "instructions": "Find API reference pages"},
    "id": "1",
    "name": "tavily_map",
    "type": "tool_call",
}
tool_msg = tool.invoke(model_generated_tool_call)

print(tool_msg.content[:400])
```

## 在 agent 中使用

我们可以把 map tool 直接绑定到模型上，由 agent 使用。这样 agent 就能动态设置说明和过滤条件，以发现它所需的 URL。

<ChatModelTabs customVarName="llm" />

```python
if not os.environ.get("OPENAI_API_KEY"):
    os.environ["OPENAI_API_KEY"] = getpass.getpass("OPENAI_API_KEY:\n")
```

```python
from langchain.chat_models import init_chat_model

model = init_chat_model(model="gpt-5.5", model_provider="openai", temperature=0)
```

```python
from langchain_tavily import TavilyMap
from langchain.agents import create_agent


tavily_map_tool = TavilyMap(max_depth=2, max_breadth=20, limit=30)

agent = create_agent(model, [tavily_map_tool])

user_input = "Map https://docs.tavily.com and list URLs that look like API reference pages."

stream = agent.stream_events({"messages": user_input}, version="v3")
for snapshot in stream.values:
    snapshot["messages"][-1].pretty_print()
```

---

## API reference

关于 Tavily Map API 全部特性和配置项的详细文档，请参阅 API reference：[docs.tavily.com/documentation/api-reference/endpoint/map](https://docs.tavily.com/documentation/api-reference/endpoint/map)
