---
title: "DaytonaDataAnalysisTool integration"
description: "使用 LangChain Python 集成 DaytonaDataAnalysisTool 工具。"
---

本指南快速概览了如何开始使用 `DaytonaDataAnalysisTool`。

<Tip>
    **Detailed Usage Example**

    该工具的详细用法示例请参阅 [Daytona documentation](https://www.daytona.io/docs/en/langchain-data-analysis)。
</Tip>

## Overview

### Details

| Class | Package | Serializable | JS support |
| :--- | :--- | :---: | :---: |
| [`DaytonaDataAnalysisTool`](https://github.com/daytonaio/langchain_daytona_data_analysis/blob/main/langchain_daytona_data_analysis/tools.py) | [`langchain-daytona-data-analysis`](https://pypi.org/project/langchain-daytona-data-analysis/) | ❌ | ❌ |

### Features

- 🔒 **Secure sandboxed execution** - 在隔离环境中运行 Python 代码
- 🐍 **Python data analysis** - 使用完整 Python 能力执行数据分析任务
- 📁 **File management** - 向沙箱上传和从沙箱下载文件
- 🔄 **Multi-step workflows** - 支持复杂的多步骤数据分析流程
- 🎯 **Custom result handling** - 使用回调处理执行结果
- 📦 **Package management** - 在沙箱中动态安装 Python 包

---

## Setup

要访问 `DaytonaDataAnalysisTool`，你需要创建一个 Daytona [account](https://app.daytona.io/)、获取 [API key](https://app.daytona.io/dashboard/keys)，并安装 `langchain-daytona-data-analysis` 集成包。

### Credentials

你必须配置 Daytona 凭证。可以通过以下三种方式之一完成：

**1. 设置 `DAYTONA_API_KEY` 环境变量：**

```bash Set API key icon="key"
export DAYTONA_API_KEY="your-daytona-api-key"
```

**2. 把它写入项目根目录下的 `.env` 文件：**

```env Set API key icon="key"
DAYTONA_API_KEY=your-daytona-api-key
```

**3. 在实例化 `DaytonaDataAnalysisTool` 时直接传入 API key：**

```python Set API key icon="key"
tool = DaytonaDataAnalysisTool(daytona_api_key="your-daytona-api-key")
```

配置 LangSmith 以获得最佳可观测性，以及对工具调用进行 <Tooltip tip="Log each step of a model's execution to debug and improve it">tracing</Tooltip> 也会很有帮助，但不是必须的。要启用自动 tracing，请设置你的 [LangSmith](/langsmith/observability) API key：

```python Enable tracing icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### Installation

`DaytonaDataAnalysisTool` 位于 `langchain-daytona-data-analysis` 包中：

### From PyPI

直接从 PyPI 安装：

<CodeGroup>
```bash pip
pip install langchain-daytona-data-analysis
```

```bash uv
uv add langchain-daytona-data-analysis
```

```bash poetry
poetry add langchain-daytona-data-analysis
```

</CodeGroup>

### From GitHub

从 GitHub 安装最新开发版本：

<CodeGroup>
```bash pip
pip install git+https://github.com/daytonaio/langchain_daytona_data_analysis
```

```bash uv
uv add git+https://github.com/daytonaio/langchain_daytona_data_analysis
```

```bash poetry
poetry add git+https://github.com/daytonaio/langchain_daytona_data_analysis
```

</CodeGroup>

---

## Instantiation

导入并实例化工具：

```python Initialize tool instance icon="robot"
from langchain_daytona_data_analysis import DaytonaDataAnalysisTool
from daytona import ExecutionArtifacts

# Optionally, you can pass an on_result callback.
# This callback lets you apply custom logic to the data analysis result.
# For example, you can save outputs, display charts, or trigger other actions.
def process_data_analysis_result(result: ExecutionArtifacts):
    print(result)

tool = DaytonaDataAnalysisTool(
    daytona_api_key="your-daytona-api-key", # Only pass if not set as DAYTONA_API_KEY environment variable
    on_result=process_data_analysis_result
)
```

---

## Invocation

### Directly

```python Call tool icon="rocket"
tool.invoke({'data_analysis_python_code': "print('Hello World')"})
```

### As a `ToolCall`

```python ToolCall icon="briefcase"
model_generated_tool_call = {
    "args": {'data_analysis_python_code': "print('Hello World')"},
    "id": "1",
    "name": tool.name,
    "type": "tool_call",
}

tool.invoke(model_generated_tool_call)
```

### Within an agent

```python Agent with tool icon="robot"
from langchain.agents import create_agent
from langchain_anthropic import ChatAnthropic

model = ChatAnthropic(
    model_name="claude-haiku-4-5-20251001",
    temperature=0,
    max_tokens_to_sample=1024,
    timeout=None,
    max_retries=2,
    stop=None
)

agent = create_agent(model, tools=[tool])
```

---

## Additional functionalities

`DaytonaDataAnalysisTool` 提供了多种方法，用于管理文件和沙箱环境：

### File management

**将文件上传到沙箱：**

```python
with open("sales_data.csv", "rb") as f:
    uploaded = tool.upload_file(
        f,
        "CSV file containing sales data with columns: id, date, product, revenue"
    )
```

**从沙箱下载文件：**

```python
file_bytes = tool.download_file("/home/daytona/results.csv")
```

**删除已上传文件：**

```python
tool.remove_uploaded_file(uploaded)
```

### Package management

**在沙箱中安装 Python 包：**

```python
# Single package
tool.install_python_packages("pandas")

# Multiple packages
tool.install_python_packages(["numpy", "matplotlib", "seaborn"])
```

<Note>
有关预装包列表，请参阅 [Daytona Default Snapshot documentation](https://www.daytona.io/docs/en/snapshots/#default-snapshot)。
</Note>

### Sandbox management

**访问沙箱实例：**

```python
sandbox = tool.get_sandbox()
```

**完成后关闭沙箱：**

```python
tool.close()  # Cleans up resources and deletes the sandbox
```

<Warning>
完成所有数据分析任务后，请调用 `tool.close()`，以正确清理资源并避免不必要的使用消耗。
</Warning>

---

## API reference

有关 `DaytonaDataAnalysisTool` 全部功能和配置的详细文档，请参阅 [API reference](https://www.daytona.io/docs/en/langchain-data-analysis#10-api-reference)。
