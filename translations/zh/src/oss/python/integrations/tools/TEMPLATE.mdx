---
title: "(MODULE_NAME) 集成"
description: "使用 LangChain Python 集成 (MODULE_NAME) 工具。"
---
---

如何使用这个 Python 工具模板：

- [ ] 将 `(MODULE_NAME)` 替换为工具名称（`command + F` 会很有帮助）
- [ ] 更新链接，使其指向正确的工具
- [ ] 在详情表和功能表中，更新 ✅/❌ 以反映该工具的实际能力
- [ ] 如有需要，更新 PyPI/registry 包名
- [ ] 如有需要，更新 API key 的环境变量名称

模板从下面这一行开始...

本指南简要介绍如何开始使用 (MODULE_NAME) [tool](/oss/langchain/tools)。如需查看 (MODULE_NAME) 全部功能、参数和配置的详细清单，请前往 [(MODULE_NAME) API reference](<LINK>)。

## 概览

### 详情

| Class | Package |  Serializable | [JS support](https://js.langchain.com/docs/integrations/chat/anthropic) | Downloads | Version |
| :--- | :--- | :---: | :---: |  :---: | :---: | :---: |
| [(MODULE_NAME)](<LINK>) | [langchain-(MODULE_NAME)](<LINK>) | beta/❌ | ✅/❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-(MODULE_NAME)?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-(MODULE_NAME)?style=flat-square&label=%20) |

### 功能

- 在这里列出功能。如有需要，也可以添加图标。

---

## 设置

要访问 (MODULE_NAME) 工具，你需要创建一个 (MODULE_NAME) 账户、获取 API key，并安装 `langchain-(MODULE_NAME)` 集成包。

### 凭证

```python Set API key icon="key"
import getpass
import os

if "(MODULE_NAME)_API_KEY" not in os.environ:
    os.environ["(MODULE_NAME)_API_KEY"] = getpass.getpass("Enter your (MODULE_NAME) API key: ")
```


另外，建议设置 LangSmith，以便对工具调用进行一流的可观测性/<Tooltip tip="Log each step of a model's execution to debug and improve it">tracing</Tooltip>。要启用自动追踪，请设置你的 [LangSmith](/langsmith/observability) API key：

```python Enable tracing icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装

`(MODULE_NAME)` 工具位于 `langchain-(MODULE_NAME)` 包中：

<CodeGroup>
    ```python pip
    pip install -U langchain-(MODULE_NAME)
    ```
    ```python uv
    uv add langchain-(MODULE_NAME)
    ```
</CodeGroup>

---

## 实例化

现在可以通过以下方式实例化一个 `(MODULE_NAME)` 工具实例：

```python Initialize tool instance icon="robot"
from langchain_(MODULE_NAME) import (MODULE_NAME)

tool = (MODULE_NAME)(...)
```

---

## 调用

### 直接调用

TODO: 描述该工具的参数是什么，并补全它们

```python Call tool icon="rocket"
tool.invoke({...})
```

### 作为 ToolCall 调用

你也可以使用模型生成的 `ToolCall` 调用该工具，在这种情况下会返回一个 @[`ToolMessage`]：

```python ToolCall icon="briefcase"
# This is usually generated by a model, but we'll create a tool call directly for demo purposes.
model_generated_tool_call = {
    "args": {...},  # TODO: FILL IN
    "id": "1",
    "name": tool.name,
    "type": "tool_call",
}
tool.invoke(model_generated_tool_call)
```

### 在 agent 中使用

你可以在 agent 中使用这个工具。为此，你需要一个支持 tool calling 的模型。

```python Agent with tool icon="robot"
# pip install -qU "langchain[anthropic]" to call the model
from langchain.agents import create_agent

tools = [tool]
agent = create_agent(
    model="claude-sonnet-4-6",
    tools=tools,
)

# Run the agent
agent.invoke(
    {"messages": [{"role": "user", "content": "INSERT QUERY"}]}
)
```

---

## TODO: 该工具的特定功能

如果不相关，请删除。

可以参考已有工具文档中的示例，例如：

- [MCP Toolbox](/oss/integrations/tools/toolbox)
- [Discord](/oss/integrations/tools/discord)
- 

---

## API reference

如需查看 (MODULE_NAME) 全部功能和配置的详细文档，请前往 [API reference](<LINK>)。
