---
title: "StripeAgentToolkit 集成"
description: "使用 LangChain Python 集成 StripeAgentToolkit 工具。"
---

本指南简要介绍如何开始使用 Stripe 的 agent toolkit。

你可以在 [Stripe 的发布博客](https://stripe.dev/blog/adding-payments-to-your-agentic-workflows) 或项目的 [PyPI 页面](https://pypi.org/project/stripe-agent-toolkit/) 中了解更多关于 `StripeAgentToolkit` 的信息。

## 概览

### 集成详情

| Class | Package | Serializable | [JS Support](https://github.com/stripe/agent-toolkit?tab=readme-ov-file#typescript) |  Version |
| :--- | :--- | :---: | :---: | :---: |
| `StripeAgentToolkit` | [`stripe-agent-toolkit`](https://pypi.org/project/stripe-agent-toolkit) | ❌ | ✅ |  ![PyPI - Version](https://img.shields.io/pypi/v/stripe-agent-toolkit?style=flat-square&label=%20) |

## 设置

这个由外部维护的包托管在 `stripe-agent-toolkit` 项目中，由 Stripe 团队维护。

你可以通过 `pip` 安装它，以及后续示例中需要的 langgraph：

```python
pip install --quiet -U langgraph stripe-agent-toolkit
```

### 凭证

除了安装该包外，你还需要使用 Stripe 账户的 secret key 来配置此集成，该 key 可在你的 [Stripe Dashboard](https://dashboard.stripe.com/account/apikeys) 中找到。

```python
import getpass
import os

if not os.environ.get("STRIPE_SECRET_KEY"):
    os.environ["STRIPE_SECRET_KEY"] = getpass.getpass("STRIPE API key:\n")
```

设置 [LangSmith](https://smith.langchain.com) 也会很有帮助，但不是必需的，可用于获得一流的可观测性：

```python
os.environ["LANGSMITH_TRACING"] = "true"
# os.environ["LANGSMITH_API_KEY"] = getpass.getpass()
```

## 实例化

这里展示如何创建 Stripe Toolkit 的一个实例。

```python
from stripe_agent_toolkit.langchain.toolkit import StripeAgentToolkit

stripe_agent_toolkit = StripeAgentToolkit(
    secret_key=os.getenv("STRIPE_SECRET_KEY"),
    configuration={
        "actions": {
            "payment_links": {
                "create": True,
            },
        }
    },
)
```

## Agent

下面展示如何在 langgraph 中使用这个 toolkit 创建一个基础 agent：

```python
from langchain_anthropic import ChatAnthropic
from langchain.agents import create_agent


model = ChatAnthropic(
    model="claude-3-5-sonnet-20240620",
)

langgraph_agent_executor = create_agent(model, stripe_agent_toolkit.get_tools())

input_state = {
    "messages": """
        Create a payment link for a new product called 'test' with a price
        of $100. Come up with a funny description about buy bots,
        maybe a haiku.
    """,
}

output_state = langgraph_agent_executor.invoke(input_state)

print(output_state["messages"][-1].content)
```
