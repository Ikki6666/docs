---
title: "Valyucontext 集成"
description: "使用 LangChain Python 与 Valyucontext tool 集成。"
---

>[Valyu](https://www.valyu.network/) 允许 AI 应用和 agents 在互联网以及私有数据源中搜索与 LLM 相关、可直接使用的信息。

本笔记本介绍如何在 LangChain 中使用 Valyu context tool。

首先，获取一个 Valyu API key，并把它加入环境变量。你可以[在这里注册](https://platform.valyu.network/)并获得 10 美元免费额度。

## 概览

### 集成详情

| Class                                                         | Package                                                        | Serializable | JS support |  Version |
|:--------------------------------------------------------------|:---------------------------------------------------------------| :---: | :---: | :---: |
| [Valyu Search](https://github.com/valyuAI/langchain-valyu) | [`langchain-valyu`](https://pypi.org/project/langchain-valyu/) | ✅ | ❌  |  ![PyPI - Version](https://img.shields.io/pypi/v/langchain-valyu?style=flat-square&label=%20) |

## 设置

该集成位于 `langchain-valyu` 包中。

```python
pip install -qU langchain-valyu
```

要使用这个包，你还需要把 `VALYU_API_KEY` 环境变量设置成自己的 Valyu API key。

```python
import getpass
import os

if not os.environ.get("VALYU_API_KEY"):
    os.environ["VALYU_API_KEY"] = getpass.getpass("Valyu API key:\n")
```

## 实例化

这里演示如何实例化 Valyu search tool。这个工具可以通过 Valyu 的 Context API endpoint 完成搜索查询。

```python
from langchain_valyu import ValyuSearchTool

tool = ValyuSearchTool()
```

## 调用

### 直接带参数调用

Valyu search tool 在调用时接受以下参数：

- `query`（必填）：自然语言搜索查询
- `search_type`（可选）：搜索类型，例如 `"all"`
- `max_num_results`（可选）：返回结果的最大数量
- `similarity_threshold`（可选）：结果的相似度阈值
- `query_rewrite`（可选）：是否重写查询
- `max_price`（可选）：搜索允许的最高价格

出于可靠性和性能考虑，某些参数可能是必填或受限制的。详情请参阅 [Valyu API documentation](https://docs.valyu.network/overview)。

```python
search_results = tool._run(
    query="What are agentic search-enhanced large reasoning models?",
    search_type="all",
    max_num_results=5,
    similarity_threshold=0.4,
    query_rewrite=False,
    max_price=20.0,
)

print("Search Results:", search_results)
```

## 在 agent 中使用

我们可以把这个工具绑定到 agent 上，并直接通过 agent executor 来使用。这会让 agent 能够动态设置 Valyu search tool 支持的可用参数。

```python
if not os.environ.get("OPENAI_API_KEY"):
    os.environ["OPENAI_API_KEY"] = getpass.getpass("OPENAI_API_KEY:\n")
```

```python
# | output: false
# | echo: false

# !pip install -qU langchain langchain-openai
from langchain.chat_models import init_chat_model

model = init_chat_model(model="gpt-5.5", model_provider="openai", temperature=0)
```

```python
from langchain_valyu import ValyuSearchTool
from langchain.agents import create_agent


valyu_search_tool = ValyuSearchTool()

agent = create_agent(model, [valyu_search_tool])

user_input = "What are the key factors driving recent stock market volatility, and how do macroeconomic indicators influence equity prices across different sectors?"

stream = agent.stream_events(
    {"messages": user_input},
    version="v3",
)
for snapshot in stream.values:
    snapshot["messages"][-1].pretty_print()
```

---

## API reference

如需查看 Valyu Context API 全部特性和配置项的详细文档，请参阅 API reference：[docs.valyu.network/overview](https://docs.valyu.network/overview)
