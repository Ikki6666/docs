---
title: "Lindorm 集成"
description: "使用 LangChain Python 与 Lindorm embedding 模型集成。"
---


这将帮助你开始使用 Lindorm embedding 模型与 LangChain。

## 概览

### 集成详情

| Provider |              Package              |
|:--------:|:---------------------------------:|
| [Lindorm](/oss/integrations/providers/lindorm/) | [`langchain-lindorm-integration`](https://pypi.org/project/langchain-lindorm-integration/) |

## 设置

要访问 Lindorm embedding 模型，你需要创建 Lindorm 账号、获取 AK&SK，并安装 `langchain-lindorm-integration` 集成包。

### 凭据

你可以在 [console](https://lindorm.console.aliyun.com/cn-hangzhou/clusterhou/cluster?spm=a2c4g.11186623.0.0.466534e93Xj6tt) 中获取凭据。

```python
import os


class Config:
    AI_LLM_ENDPOINT = os.environ.get("AI_ENDPOINT", "<AI_ENDPOINT>")
    AI_USERNAME = os.environ.get("AI_USERNAME", "root")
    AI_PWD = os.environ.get("AI_PASSWORD", "<PASSWORD>")

    AI_DEFAULT_EMBEDDING_MODEL = "bge_m3_model"  # set to your deployed model
```

### 安装

LangChain Lindorm 集成位于 `langchain-lindorm-integration` 包中：

```python
pip install -qU langchain-lindorm-integration
```

## 实例化

现在我们可以实例化模型对象并生成 chat completions：

```python
from langchain_lindorm_integration import LindormAIEmbeddings

embeddings = LindormAIEmbeddings(
    endpoint=Config.AI_LLM_ENDPOINT,
    username=Config.AI_USERNAME,
    password=Config.AI_PWD,
    model_name=Config.AI_DEFAULT_EMBEDDING_MODEL,
)
```

## 索引与检索

Embedding 模型常用于检索增强生成（RAG）流程，既可以用于索引数据，也可以用于后续检索。更详细的说明请参见我们的 [RAG tutorials](/oss/deepagents/rag)。

下面展示如何使用上面初始化的 `embeddings` 对象进行索引和检索。在这个示例中，我们会在 `InMemoryVectorStore` 中索引和检索一个示例文档。

```python
# Create a vector store with a sample text
from langchain_core.vectorstores import InMemoryVectorStore

text = "LangChain is the framework for building context-aware reasoning applications"

vectorstore = InMemoryVectorStore.from_texts(
    [text],
    embedding=embeddings,
)

# Use the vectorstore as a retriever
retriever = vectorstore.as_retriever()

# Retrieve the most similar text
retrieved_documents = retriever.invoke("What is LangChain?")

# show the retrieved document's content
retrieved_documents[0].page_content
```

```text
'LangChain is the framework for building context-aware reasoning applications'
```

## 直接使用

在底层，vectorstore 和 retriever 的实现会调用 `embeddings.embed_documents(...)` 和 `embeddings.embed_query(...)`，分别为 `from_texts` 和检索 `invoke` 中使用的文本创建 embedding。

你可以直接调用这些方法来满足自己的使用场景。

### 嵌入单个文本

你可以使用 `embed_query` 嵌入单个文本或文档：

```python
single_vector = embeddings.embed_query(text)
print(str(single_vector)[:100])  # Show the first 100 characters of the vector
```

```text
[-0.016254117712378502, -0.01154549140483141, 0.0042558759450912476, -0.011416379362344742, -0.01770
```

### 嵌入多个文本

你可以使用 `embed_documents` 嵌入多个文本：

```python
text2 = (
    "LangGraph is a library for building stateful, multi-actor applications with LLMs"
)
two_vectors = embeddings.embed_documents([text, text2])
for vector in two_vectors:
    print(str(vector)[:100])  # Show the first 100 characters of the vector
```

```text
[-0.016254086047410965, -0.011545476503670216, 0.0042558712884783745, -0.011416426859796047, -0.0177
[-0.07268096506595612, -3.236892371205613e-05, -0.0019329536007717252, -0.030644644051790237, -0.018
```

---

## API reference

关于 `LindormEmbeddings` 功能和配置选项的详细文档，请参见 [API reference](https://pypi.org/project/langchain-lindorm-integration/)。
