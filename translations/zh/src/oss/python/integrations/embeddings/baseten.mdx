---
title: "BasetenEmbeddings 集成"
description: "使用 LangChain Python 与 BasetenEmbeddings embedding 模型集成。"
---

这将帮助你开始使用 Baseten embedding 模型与 LangChain。

## 概览

Baseten 提供面向生产应用设计的推理服务。基于 Baseten Inference Stack，这些 API 为领先的开源模型或自定义模型提供企业级性能与可靠性：https://www.baseten.co/library/。

### 集成详情

<ItemTable category="embeddings" item="Baseten" />

## 设置

要访问 Baseten embedding 模型，你需要创建 Baseten 账号、获取 API key，并安装 `langchain-baseten` 集成包。

**Baseten embeddings 仅以专用模型形式提供。** 在使用此集成前，你必须先从 [Baseten model library](https://www.baseten.co/library/tag/embedding) 部署一个 embedding 模型。

embedding 功能使用 Baseten 的 [Performance Client](https://www.baseten.co/blog/introducing-baseten-embeddings-inference-bei/) 以获得优化后的性能，该客户端会作为依赖自动包含。

### 凭据

前往 [https://app.baseten.co](https://app.baseten.co) 注册 Baseten 并生成 API key。完成后，设置 `BASETEN_API_KEY` 环境变量：

```python
import getpass
import os

if not os.getenv("BASETEN_API_KEY"):
    os.environ["BASETEN_API_KEY"] = getpass.getpass("Enter your Baseten API key: ")
```

要启用模型调用的自动 tracing，请设置你的 [LangSmith](/langsmith/observability) API key：

```python
os.environ["LANGSMITH_TRACING"] = "true"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
```

### 安装

LangChain Baseten 集成位于 `langchain-baseten` 包中：

```python
pip install -qU langchain-baseten
```

## 实例化

现在我们可以使用你已部署模型的 URL 实例化 embedding 对象：

```python
from langchain_baseten import BasetenEmbeddings

embeddings = BasetenEmbeddings(
    model_url="https://model-<id>.api.baseten.co/environments/production/sync",  # Your model URL
    api_key="your-api-key",  # Or set BASETEN_API_KEY env var
)
```

## 索引与检索

Embedding 模型常用于检索增强生成（RAG）流程，既可以用于索引数据，也可以用于后续检索。更详细的说明请参见我们的 [RAG tutorials](/oss/langchain/rag)。

下面展示如何使用上面初始化的 `embeddings` 对象进行索引和检索。在这个示例中，我们会在 `InMemoryVectorStore` 中索引和检索一个示例文档。

```python
# Create a vector store with a sample text
from langchain_core.vectorstores import InMemoryVectorStore

text = "LangChain is the framework for building context-aware reasoning applications"

vectorstore = InMemoryVectorStore.from_texts(
    [text],
    embedding=embeddings,
)

# Use the vectorstore as a retriever
retriever = vectorstore.as_retriever()

# Retrieve the most similar text
retrieved_documents = retriever.invoke("What is LangChain?")

# show the retrieved document's content
retrieved_documents[0].page_content
```

```text
'LangChain is the framework for building context-aware reasoning applications'
```

## 直接使用

在底层，vectorstore 和 retriever 的实现会调用 `embeddings.embed_documents(...)` 和 `embeddings.embed_query(...)`，分别为 `from_texts` 和检索 `invoke` 中使用的文本创建 embedding。

你可以直接调用这些方法来满足自己的使用场景。

### 嵌入单个文本

你可以使用 `embed_query` 嵌入单个文本或文档：

```python
single_vector = embeddings.embed_query(text)
print(str(single_vector)[:100])  # Show the first 100 characters of the vector
```

```text
[0.013201533816754818, 0.02222288027405739, -0.036066457629203796, 0.027374643832445145, -0.01692997
```

### 嵌入多个文本

你可以使用 `embed_documents` 嵌入多个文本：

```python
text2 = (
    "LangGraph is a library for building stateful, multi-actor applications with LLMs"
)
two_vectors = embeddings.embed_documents([text, text2])
for vector in two_vectors:
    print(str(vector)[:100])  # Show the first 100 characters of the vector
```

```text
[0.013201533816754818, 0.02222288027405739, -0.036066457629203796, 0.027374643832445145, -0.01692997
[0.018247194588184357, 0.007369577884674072, -0.005529594141989946, 0.022589316591620445, -0.0699259
```

---
