---
title: "Google Firestore 集成"
description: "使用 LangChain Python 集成 Google Firestore 向量存储。"
---

> [Firestore](https://cloud.google.com/firestore) 是一个无服务器、面向文档的数据库，可按任意需求扩展。借助 Firestore 的 LangChain 集成，你可以在现有数据库应用之上构建 AI 驱动的体验。

本笔记本介绍如何使用 [Firestore](https://cloud.google.com/firestore) 通过 `FirestoreVectorStore` 类来存储向量并执行查询。

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/googleapis/langchain-google-firestore-python/blob/main/docs/vectorstores.ipynb)

## 开始之前

运行本笔记本前，你需要完成以下准备：

* [创建 Google Cloud 项目](https://developers.google.com/workspace/guides/create-project)
* [启用 Firestore API](https://console.cloud.google.com/flows/enableapi?apiid=firestore.googleapis.com)
* [创建 Firestore 数据库](https://cloud.google.com/firestore/docs/manage-databases)

确认本笔记本运行环境已具备数据库访问权限后，请填写下面的值，并在运行示例脚本前执行该单元格。

```python
# @markdown Please specify a source for demo purpose.
COLLECTION_NAME = "test"  # @param {type:"CollectionReference"|"string"}
```

### 🦜🔗 安装库

该集成位于独立的 `langchain-google-firestore` 包中，因此需要单独安装。这个笔记本还会安装 `langchain-google-vertexai` 来生成 Google embedding。

```python
pip install -upgrade --quiet langchain-google-firestore langchain-google-vertexai
```

**仅限 Colab**：取消下面单元格的注释以重启内核，或者使用按钮重启内核。对于 Vertex AI Workbench，可以使用顶部按钮重启终端。

```python
# # Automatically restart kernel after installs so that your environment can access the new packages
# import IPython

# app = IPython.Application.instance()
# app.kernel.do_shutdown(True)
```

### ☁ 设置 Google Cloud 项目

设置你的 Google Cloud 项目，以便在笔记本中使用 Google Cloud 资源。

如果你不知道项目 ID，可以尝试以下方式：

* 运行 `gcloud config list`。
* 运行 `gcloud projects list`。
* 查看帮助页面：[Locate the project ID](https://support.google.com/googleapi/answer/7014113)。

```python
# @markdown Please fill in the value below with your Google Cloud project ID and then run the cell.

PROJECT_ID = "extensions-testing"  # @param {type:"string"}

# Set the project id
!gcloud config set project {PROJECT_ID}
```

### 🔐 认证

将当前笔记本中登录的 IAM 用户认证到 Google Cloud，以访问你的 Google Cloud 项目。

* 如果你使用 Colab 运行本笔记本，请使用下面的单元格继续。
* 如果你使用 Vertex AI Workbench，请参阅 [Vertex AI Workbench setup instructions](https://github.com/GoogleCloudPlatform/generative-ai/tree/main/setup-env)。

```python
from google.colab import auth

auth.authenticate_user()
```

# 基础用法

### 初始化 FirestoreVectorStore

`FirestoreVectorStore` 允许你把新向量存入 Firestore 数据库。你可以用它存储任意模型生成的 embedding，包括 Google 的 embedding 模型。

```python
from langchain_google_firestore import FirestoreVectorStore
from langchain_google_vertexai import VertexAIEmbeddings

embedding = VertexAIEmbeddings(
    model_name="textembedding-gecko@latest",
    project=PROJECT_ID,
)

# Sample data
ids = ["apple", "banana", "orange"]
fruits_texts = ['{"name": "apple"}', '{"name": "banana"}', '{"name": "orange"}']

# Create a vector store
vector_store = FirestoreVectorStore(
    collection="fruits",
    embedding=embedding,
)

# Add the fruits to the vector store
vector_store.add_texts(fruits_texts, ids=ids)
```

作为简写方式，你也可以使用 `from_texts` 和 `from_documents` 方法，在初始化时一步完成向量写入。

```python
vector_store = FirestoreVectorStore.from_texts(
    collection="fruits",
    texts=fruits_texts,
    embedding=embedding,
)
```

```python
from langchain_core.documents import Document

fruits_docs = [Document(page_content=fruit) for fruit in fruits_texts]

vector_store = FirestoreVectorStore.from_documents(
    collection="fruits",
    documents=fruits_docs,
    embedding=embedding,
)
```

### 删除向量

你可以使用 `delete` 方法从数据库中删除带向量的文档。你需要提供想删除向量对应的文档 ID。这个操作会删除数据库中的整条文档记录，包括它可能包含的其他字段。

```python
vector_store.delete(ids)
```

### 更新向量

更新向量与添加向量的方式类似。你可以使用 `add` 方法，传入文档 ID 和新的向量内容，以更新某个文档的向量。

```python
fruit_to_update = ['{"name": "apple","price": 12}']
apple_id = "apple"

vector_store.add_texts(fruit_to_update, ids=[apple_id])
```

## 相似度搜索

你可以使用 `FirestoreVectorStore` 对已存储的向量执行相似度搜索。这对于查找相似文档或文本很有帮助。

```python
vector_store.similarity_search("I like fuji apples", k=3)
```

```python
vector_store.max_marginal_relevance_search("fuji", 5)
```

你也可以通过 `filters` 参数为搜索添加预过滤条件。这在按特定字段或值筛选时很有用。

```python
from google.cloud.firestore_v1.base_query import FieldFilter

vector_store.max_marginal_relevance_search(
    "fuji", 5, filters=FieldFilter("content", "==", "apple")
)
```

### 自定义连接与认证

```python
from google.api_core.client_options import ClientOptions
from google.cloud import firestore
from langchain_google_firestore import FirestoreVectorStore

client_options = ClientOptions()
client = firestore.Client(client_options=client_options)

# Create a vector store
vector_store = FirestoreVectorStore(
    collection="fruits",
    embedding=embedding,
    client=client,
)
```
