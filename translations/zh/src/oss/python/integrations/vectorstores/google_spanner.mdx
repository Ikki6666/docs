---
title: "Google Spanner 集成"
description: "使用 LangChain Python 集成 Google Spanner 向量存储。"
---

import LangchainCommunityUnmaintained from '/snippets/oss/langchain-community-unmaintained.mdx';

> [Spanner](https://cloud.google.com/spanner) 是一个高度可扩展的数据库，将无限扩展性与关系型语义结合在一起，例如二级索引、强一致性、Schema 和 SQL，并以一种易于使用的方式提供 99.999% 的可用性。

本笔记介绍如何通过 `SpannerVectorStore` 类将 `Spanner` 用于向量搜索。

可在 [GitHub](https://github.com/googleapis/langchain-google-spanner-python/) 上了解该软件包的更多信息。

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/googleapis/langchain-google-spanner-python/blob/main/docs/vector_store.ipynb)

## 开始之前

要运行本笔记，需要完成以下步骤：

* [创建 Google Cloud 项目](https://developers.google.com/workspace/guides/create-project)
* [启用 Cloud Spanner API](https://console.cloud.google.com/flows/enableapi?apiid=spanner.googleapis.com)
* [创建 Spanner 实例](https://cloud.google.com/spanner/docs/create-manage-instances)
* [创建 Spanner 数据库](https://cloud.google.com/spanner/docs/create-manage-databases)

### 🦜🔗 安装库

该集成位于独立的软件包 `langchain-google-spanner` 中，因此需要先安装。

```python
pip install -qU langchain-google-spanner langchain-google-vertexai
```

**仅适用于 Colab：** 取消注释下面的单元以重启内核，或使用按钮重启内核。对于 Vertex AI Workbench，可以使用顶部按钮重启终端。

```python
# # Automatically restart kernel after installs so that your environment can access the new packages
# import IPython

# app = IPython.Application.instance()
# app.kernel.do_shutdown(True)
```

### 🔐 身份验证

以当前登录到此笔记本的 IAM 用户身份对 Google Cloud 进行身份验证，以访问你的 Google Cloud 项目。

* 如果你使用 Colab 运行本笔记，请使用下面的单元并继续。
* 如果你使用 Vertex AI Workbench，请参阅 [Vertex AI Workbench 设置说明](https://github.com/GoogleCloudPlatform/generative-ai/tree/main/setup-env)。

```python
from google.colab import auth

auth.authenticate_user()
```

### ☁ 设置 Google Cloud 项目

设置你的 Google Cloud 项目，以便在本笔记中使用 Google Cloud 资源。

如果不知道项目 ID，可以尝试以下方法：

* 运行 `gcloud config list`。
* 运行 `gcloud projects list`。
* 查看支持页面：[查找项目 ID](https://support.google.com/googleapi/answer/7014113)。

```python
# @markdown Please fill in the value below with your Google Cloud project ID and then run the cell.

PROJECT_ID = "my-project-id"  # @param {type:"string"}

# Set the project id
!gcloud config set project {PROJECT_ID}
%env GOOGLE_CLOUD_PROJECT={PROJECT_ID}
```

### 💡 启用 API

`langchain-google-spanner` 软件包要求你在 Google Cloud 项目中先 [启用 Spanner API](https://console.cloud.google.com/flows/enableapi?apiid=spanner.googleapis.com)。

```python
# enable Spanner API
!gcloud services enable spanner.googleapis.com
```

## 基本用法

### 设置 Spanner 数据库参数

在 [Spanner Instances 页面](https://console.cloud.google.com/spanner?_ga=2.223735448.2062268965.1707700487-2088871159.1707257687) 中找到你的数据库参数。

```python
# @title Set Your Values Here { display-mode: "form" }
INSTANCE = "my-instance"  # @param {type: "string"}
DATABASE = "my-database"  # @param {type: "string"}
TABLE_NAME = "vectors_search_data"  # @param {type: "string"}
```

### 初始化表

`SpannerVectorStore` 类实例需要一个包含 id、content 和 embeddings 列的数据库表。

辅助方法 `init_vector_store_table()` 可用于帮你创建带有正确 schema 的表。

```python
from langchain_google_spanner import SecondaryIndex, SpannerVectorStore, TableColumn

SpannerVectorStore.init_vector_store_table(
    instance_id=INSTANCE,
    database_id=DATABASE,
    table_name=TABLE_NAME,
    # Customize the table creation
    # id_column="row_id",
    # content_column="content_column",
    # metadata_columns=[
    #     TableColumn(name="metadata", type="JSON", is_null=True),
    #     TableColumn(name="title", type="STRING(MAX)", is_null=False),
    # ],
    # secondary_indexes=[
    #     SecondaryIndex(index_name="row_id_and_title", columns=["row_id", "title"])
    # ],
)
```

### 创建嵌入模型实例

你可以使用任意 [LangChain embeddings model](/oss/integrations/embeddings/)。
如果要使用 `VertexAIEmbeddings`，可能需要启用 Vertex AI API。生产环境中建议固定嵌入模型版本。更多信息请参阅 [Text embeddings models](https://cloud.google.com/vertex-ai/docs/generative-ai/model-reference/text-embeddings)。

```python
# enable Vertex AI API
!gcloud services enable aiplatform.googleapis.com
```

```python
from langchain_google_vertexai import VertexAIEmbeddings

embeddings = VertexAIEmbeddings(
    model_name="textembedding-gecko@latest", project=PROJECT_ID
)
```

### SpannerVectorStore

初始化 `SpannerVectorStore` 类时，需要提供 4 个必需参数，其他参数仅在需要覆盖默认值时传入：

1. `instance_id` - Spanner 实例名称
1. `database_id` - Spanner 数据库名称
1. `table_name` - 用于存储文档及其嵌入的表名称
1. `embedding_service` - 用于生成嵌入的 Embeddings 实现

```python
db = SpannerVectorStore(
    instance_id=INSTANCE,
    database_id=DATABASE,
    table_name=TABLE_NAME,
    embedding_service=embeddings,
    # Connect to a custom vector store table
    # id_column="row_id",
    # content_column="content",
    # metadata_columns=["metadata", "title"],
)
```

#### 添加文档

要向向量存储中添加文档：

<LangchainCommunityUnmaintained />

```python
import uuid

from langchain_community.document_loaders import HNLoader

loader = HNLoader("https://news.ycombinator.com/item?id=34817881")

documents = loader.load()
ids = [str(uuid.uuid4()) for _ in range(len(documents))]
db.add_documents(documents, ids)
```

#### 搜索文档

使用相似度搜索在向量存储中搜索文档：

```python
db.similarity_search(query="Explain me vector store?", k=3)
```

#### 搜索文档

使用最大边际相关性搜索在向量存储中搜索文档：

```python
db.max_marginal_relevance_search("Testing the langchain integration with spanner", k=3)
```

#### 删除文档

要从向量存储中删除文档，请使用与初始化 VectorStore 时 `row_id` 列对应的 ID：

```python
db.delete(ids=["id1", "id2"])
```

#### 删除文档

你也可以直接使用文档对象来删除。VectorStore 初始化时提供的内容列和元数据列将用于定位与这些文档对应的行，所有匹配的行都会被删除。

```python
db.delete(documents=[documents[0], documents[1]])
```
