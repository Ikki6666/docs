---
title: "Google Bigtable 集成"
description: "使用 LangChain Python 集成 Google Bigtable 向量存储。"
---

# BigtableVectorStore

本指南介绍如何使用 `BigtableVectorStore`，将 Google Cloud Bigtable 作为向量存储。

[Bigtable](https://cloud.google.com/bigtable) 是一种键值和宽列存储，适合快速访问结构化、半结构化或非结构化数据。

## 概览

`BigtableVectorStore` 使用 Google Cloud Bigtable 存储文档及其向量 embedding，用于相似度搜索和检索。它还支持强大的 metadata 过滤能力，可帮助你进一步细化搜索结果。

### 集成详情
| Class | Package | Local | JS support | Package downloads | Package latest |
| :--- | :--- | :---: | :---: | :---: | :---: |
| [`BigtableVectorStore`](https://github.com/googleapis/langchain-google-bigtable-python/blob/main/src/langchain_google_bigtable/vector_store.py) | [`langchain-google-bigtable`](https://pypi.org/project/langchain-google-bigtable/) | ❌ | ❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-google-bigtable?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-google-bigtable) |

## 设置

### 前置条件

开始之前，你需要准备一个带有活跃 Bigtable 实例的 Google Cloud 项目。
* [创建 Google Cloud 项目](https://developers.google.com/workspace/guides/create-project)
* [启用 Bigtable API](https://console.cloud.google.com/flows/enableapi?apiid=bigtable.googleapis.com)
* [创建 Bigtable 实例](https://cloud.google.com/bigtable/docs/creating-instance)

### 安装

该集成位于 `langchain-google-bigtable` 包中。下面的命令还会一并安装用作 embedding 服务的 `langchain-google-vertexai`。

```python
%pip install -qU langchain-google-bigtable langchain-google-vertexai
```

**仅限 Colab**：取消下面单元格的注释以重启内核，或者使用按钮重启内核。对于 Vertex AI Workbench，可以使用顶部按钮重启终端。

```python
# Automatically restart kernel after installs so that your environment can access the new packages
# import IPython

# app = IPython.Application.instance()
# app.kernel.do_shutdown(True)
```

### 设置 Google Cloud 项目

设置你的 Google Cloud 项目，以便在本笔记本中使用 Google Cloud 资源。

如果你不知道项目 ID，可以尝试以下方式：

* 运行 `gcloud config list`。
* 运行 `gcloud projects list`。
* 查看帮助页面：[Locate the project ID](https://support.google.com/googleapi/answer/7014113)。

```python
# @markdown Please fill in your project, instance, and a new table name.
PROJECT_ID = "test-project"  # @param {type:"string"}
INSTANCE_ID = "test-instance"  # @param {type:"string"}
TABLE_ID = "your-vector-store-table-3"  # @param {type:"string"}

!gcloud config set project {PROJECT_ID}
```

### 🔐 认证

将当前笔记本中登录的 IAM 用户认证到 Google Cloud，以访问你的 Google Cloud 项目。

- 如果你使用 Colab 运行本笔记本，请使用下面的单元格继续。
- 如果你使用 Vertex AI Workbench，请参阅 [Vertex AI Workbench setup instructions](https://github.com/GoogleCloudPlatform/generative-ai/tree/main/setup-env)。

```python
from google.colab import auth

auth.authenticate_user(project_id=PROJECT_ID)
```

## 初始化

初始化 `BigtableVectorStore` 分为三步：设置 embedding 服务、确保 Bigtable 数据表已创建，以及配置向量存储参数。

### 1. 设置 embedding 服务

首先，需要一个模型为文档生成向量 embedding。这里使用 Vertex AI 模型作为示例。

```python
from langchain_google_vertexai import VertexAIEmbeddings

embeddings = VertexAIEmbeddings(project=PROJECT_ID, model_name="gemini-embedding-001")
```

### 2. 初始化数据表

在创建 `BigtableVectorStore` 之前，必须先存在具有正确列族的表。推荐使用辅助函数 `init_vector_store_table` 来创建和配置表。如果表已经存在，该函数不会执行任何操作。

```python
from langchain_google_bigtable.vector_store import init_vector_store_table

DATA_COLUMN_FAMILY = "doc_data"

try:
    init_vector_store_table(
        project_id=PROJECT_ID,
        instance_id=INSTANCE_ID,
        table_id=TABLE_ID,
        content_column_family=DATA_COLUMN_FAMILY,
        embedding_column_family=DATA_COLUMN_FAMILY,
    )
    print(f"Table '{TABLE_ID}' is ready.")
except ValueError as e:
    print(e)
```

### 3. 配置向量存储

现在来定义控制 Bigtable 连接方式和数据处理方式的参数。

#### BigtableEngine

`BigtableEngine` 对象负责管理客户端和异步操作。强烈建议初始化一个 engine 并在多个 store 之间复用，以获得更好的性能和资源管理效果。

```python
from langchain_google_bigtable import BigtableEngine

engine = await BigtableEngine.async_initialize(project_id=PROJECT_ID)
```

#### Collections

`collection` 为同一张 Bigtable 表中的文档提供一个逻辑命名空间。它会作为行键前缀使用，让多个向量存储可以在同一张表中共存而互不干扰。

```python
collection_name = "my_docs"
```

#### Metadata 配置

创建 `BigtableVectorStore` 时，有两个可选参数用于处理 metadata：

* `metadata_mappings`：这是一个 `VectorMetadataMapping` 对象列表。**任何你希望在搜索中过滤的 metadata key，都必须在这里定义映射。** 每个映射都要指定该字段的数据类型 `encoding`，这样 Bigtable 才能正确执行过滤。
* `metadata_as_json_column`：这是一个可选的 `ColumnConfig`，用于告诉 store 将**整个** metadata 字典以单个 JSON 字符串形式保存在指定列中。这对一次性高效取回全部 metadata 非常有用，包括那些未在 `metadata_mappings` 中定义的字段。**注意：** 仅存储在这个 JSON 列中的字段不能用于过滤。

```python
from langchain_google_bigtable import ColumnConfig, VectorMetadataMapping, Encoding

# Define mappings for metadata fields you want to filter on.
metadata_mappings = [
    VectorMetadataMapping(metadata_key="author", encoding=Encoding.UTF8),
    VectorMetadataMapping(metadata_key="year", encoding=Encoding.INT_BIG_ENDIAN),
    VectorMetadataMapping(metadata_key="category", encoding=Encoding.UTF8),
    VectorMetadataMapping(metadata_key="rating", encoding=Encoding.FLOAT),
]

# Define the optional column for storing all metadata as a single JSON string.
metadata_as_json_column = ColumnConfig(
    column_family=DATA_COLUMN_FAMILY, column_qualifier="metadata_json"
)
```

### 4. 创建 BigtableVectorStore 实例

```python
# Configure the columns for your store.
content_column = ColumnConfig(
    column_family=DATA_COLUMN_FAMILY, column_qualifier="content"
)
embedding_column = ColumnConfig(
    column_family=DATA_COLUMN_FAMILY, column_qualifier="embedding"
)
```

```python
from langchain_google_bigtable import BigtableVectorStore

vector_store = await BigtableVectorStore.create(
    project_id=PROJECT_ID,
    instance_id=INSTANCE_ID,
    table_id=TABLE_ID,
    engine=engine,
    embedding_service=embeddings,
    collection=collection_name,
    metadata_mappings=metadata_mappings,
    metadata_as_json_column=metadata_as_json_column,
    content_column=content_column,
    embedding_column=embedding_column,
)
```

## 管理向量存储

### 添加文档

你可以添加带预定义 ID 的文档。如果 `Document` 没有 `id` 属性，向量存储会自动为其生成一个 **`uuid4` 字符串**。

```python
from langchain_core.documents import Document

docs_to_add = [
    Document(
        page_content="A young farm boy, Luke Skywalker, is thrust into a galactic conflict.",
        id="doc_1",
        metadata={
            "author": "George Lucas",
            "year": 1977,
            "category": "sci-fi",
            "rating": 4.8,
        },
    ),
    Document(
        page_content="A hobbit named Frodo Baggins must destroy a powerful ring.",
        id="doc_2",
        metadata={
            "author": "J.R.R. Tolkien",
            "year": 1954,
            "category": "fantasy",
            "rating": 4.9,
        },
    ),
    # Document without a pre-defined ID, one will be generated.
    Document(
        page_content="A group of children confront an evil entity emerging from the sewers.",
        metadata={"author": "Stephen King", "year": 1986, "category": "horror"},
    ),
    Document(
        page_content="In a distant future, the noble House Atreides rules the desert planet Arrakis.",
        id="doc_3",
        metadata={
            "author": "Frank Herbert",
            "year": 1965,
            "category": "sci-fi",
            "rating": 4.9,
        },
    ),
]

added_ids = await vector_store.aadd_documents(docs_to_add)
print(f"Added documents with IDs: {added_ids}")
```

### 更新文档

`BigtableVectorStore` 通过覆盖实现更新。要更新文档，只需使用相同 ID 再次添加，并提供新的内容或 metadata。

```python
doc_to_update = [
    Document(
        page_content="An old hobbit, Frodo Baggins, must take a powerful ring to be destroyed.",  # Updated content
        id="doc_2",  # Same ID
        metadata={
            "author": "J.R.R. Tolkien",
            "year": 1954,
            "category": "epic-fantasy",
            "rating": 4.9,
        },  # Updated metadata
    )
]

await vector_store.aadd_documents(doc_to_update)
print("Document 'doc_2' has been updated.")
```

### 删除文档

```python
is_deleted = await vector_store.adelete(ids=["doc_2"])
```

## 查询向量存储

### 搜索

```python
results = await vector_store.asimilarity_search("a story about a powerful ring", k=1)
print(results[0].page_content)
```

### 带过滤条件的搜索

在执行向量搜索前先应用过滤条件。

#### kNN 搜索算法与过滤

默认情况下，`BigtableVectorStore` 使用 **k-Nearest Neighbors (kNN)** 搜索算法，从数据库中找出与查询向量最相似的 `k` 个向量。该向量存储支持在执行 kNN 搜索前先缩小搜索空间的过滤能力，这通常可以让查询更快，也更相关。

#### 使用 `QueryParameters` 配置查询

所有搜索设置都通过 `QueryParameters` 对象控制。这个对象不仅可以配置过滤条件，还可以指定其他重要的搜索项：
* `algorithm`：要使用的搜索算法，默认是 `"kNN"`。
* `distance_strategy`：比较使用的距离度量，例如 `COSINE`（默认）或 `EUCLIDEAN`。
* `vector_data_type`：已存储向量的数据类型，例如 `FLOAT32` 或 `DOUBLE64`。它应与 embedding 的精度保持一致。
* `filters`：用于定义过滤逻辑的字典。

#### 理解 Encodings

如果要过滤 metadata 字段，必须先在 `metadata_mappings` 中用正确的 `encoding` 定义这些字段，这样 Bigtable 才能正确解释数据。支持的编码包括：
* **String**：`UTF8`、`UTF16`、`ASCII`，用于文本类 metadata。
* **Numeric**：`INT_BIG_ENDIAN` 或 `INT_LITTLE_ENDIAN` 用于整数，`FLOAT` 或 `DOUBLE` 用于小数。
* **Boolean**：`BOOL` 用于 true/false 值。

#### 过滤支持表

| Filter Category | Key / Operator | Meaning |
|---|---|---|
| **Row Key** | `RowKeyFilter` | 将搜索范围缩小到具有特定前缀的文档 ID。 |
| **Metadata Key** | `ColumnQualifiers` | 检查一个或多个精确 metadata key 是否存在。 |
| | `ColumnQualifierPrefix` | 检查 metadata key 是否以给定前缀开头。 |
| | `ColumnQualifierRegex` | 检查 metadata key 是否匹配正则表达式。 |
| **Metadata Value** | `ColumnValueFilter` | 所有基于值的条件的容器。 |
| | `==` | 相等 |
| | `!=` | 不相等 |
| | `>` | 大于 |
| | `<` | 小于 |
| | `>=` | 大于等于 |
| | `<=` | 小于等于 |
| | `in` | 值位于列表中。 |
| | `nin` | 值不在列表中。 |
| | `contains` | 检查是否包含子串。 |
| | `like` | 对字符串执行正则匹配。 |
| **Logical**| `ColumnValueChainFilter` | 对多个值条件执行逻辑 AND。 |
| | `ColumnValueUnionFilter` | 对多个值条件执行逻辑 OR。 |

#### 复杂过滤示例

下面的示例使用多个嵌套逻辑过滤器。它会搜索满足以下任一条件的文档：(`category` 为 `sci-fi` 且 `year` 在 1970-2000 之间) 或 (`author` 为 `J.R.R. Tolkien`) 或 (`rating` > 4.5)。

```python
from langchain_google_bigtable.vector_store import QueryParameters

complex_filter = {
    "ColumnValueFilter": {
        "ColumnValueUnionFilter": {  # OR
            "ColumnValueChainFilter": {  # First AND condition
                "category": {"==": "sci-fi"},
                "year": {">": 1970, "<": 2000},
            },
            "author": {"==": "J.R.R. Tolkien"},
        }
    }
}

query_params_complex = QueryParameters(filters=complex_filter)

complex_results = await vector_store.asimilarity_search(
    "a story about a hero's journey", k=5, query_parameters=query_params_complex
)

print(f"Found {len(complex_results)} documents matching the complex filter:")
for doc in complex_results:
    print(f"- ID: {doc.id}, Metadata: {doc.metadata}")
```

### 带分数的搜索

你也可以在返回文档的同时获取距离分数。

```python
results_with_scores = await vector_store.asimilarity_search_with_score(
    query="an evil entity", k=1
)
for doc, score in results_with_scores:
    print(f"* [SCORE={score:.4f}] {doc.page_content} [{doc.metadata}]")
```

### 作为 Retriever 使用

在 RAG 应用中，你可以很方便地把向量存储当作 retriever 使用。你可以指定搜索类型，例如 `similarity` 或 `mmr`，并传入 `k`、`query_parameters` 等搜索期参数。

```python
# Define a filter to use with the retriever
retriever_filter = {"ColumnValueFilter": {"category": {"==": "horror"}}}
retriever_query_params = QueryParameters(filters=retriever_filter)

retriever = vector_store.as_retriever(
    search_type="mmr",  # Specify MMR for retrieval
    search_kwargs={
        "k": 1,
        "lambda_mult": 0.8,
        "query_parameters": retriever_query_params,  # Pass filter parameters
    },
)
retrieved_docs = await retriever.ainvoke("a story about a hobbit")
print(retrieved_docs[0].page_content)
```

## 用于检索增强生成

如需了解如何在检索增强生成（RAG）中使用该向量存储，请参阅以下内容：

- [Retrieval docs](/oss/langchain/retrieval)
- [Build a RAG app with LangChain](/oss/deepagents/rag)
- [Agentic RAG](/oss/langgraph/agentic-rag)

## API reference

如需查看 `BigtableVectorStore` 类的完整细节，请参阅其在 [GitHub](https://github.com/googleapis/langchain-google-bigtable-python/blob/main/src/langchain_google_bigtable/vector_store.py) 上的源码。
