---
title: "ClickSend 集成"
description: 使用 ClickSend 通信平台，通过 LangChain agents 发送 SMS、Email 和 Voice 消息。
---

[ClickSend](https://www.clicksend.com/) 是一个基于云的通信平台，允许开发者通过统一 API 发送 SMS、Email、Voice、Fax 和 Post 消息。

## 概览

### 集成详情

| Class | Package | Serializable | [Python support](/oss/integrations/tools/clicksend) | Version |
|:---|:---|:---:|:---:|:---:|
| `ClickSendTools` | [@clicksend/langchain-clicksend-mcp](https://www.npmjs.com/package/@clicksend/langchain-clicksend-mcp) | ❌ | ❌ | ![npm version](https://img.shields.io/npm/v/@clicksend/langchain-clicksend-mcp?style=flat-square&label=%20) |

### 工具特性

- **SMS Messaging**：向全球手机号码发送短信
- **Email**：发送事务型和营销型邮件
- **Voice**：发送自动语音消息和呼叫
- **MCP Integration**：通过 Model Context Protocol 连接，实现无缝 agent 集成

## 设置

该集成位于 `@clicksend/langchain-clicksend-mcp` 包中。

```bash
npm install @clicksend/langchain-clicksend-mcp
```

### 凭证

你需要一个带 API credentials 的 ClickSend 账号。前往 [clicksend.com](https://www.clicksend.com/) 注册，以获取用户名和 API key。

```typescript Set credentials
import dotenv from 'dotenv';
dotenv.config();
```

## 实例化

通过连接到 ClickSend MCP server 来初始化 ClickSend tools：

```typescript Initialize ClickSend
import { ClickSendTools } from "@clicksend/langchain-clicksend-mcp";

const tools = await ClickSendTools.fromHTTP({
  url: "https://mcp.clicksend.com",
  username: process.env.CLICKSEND_USERNAME,
  apiKey: process.env.CLICKSEND_API_KEY,
});

console.log(`Loaded ${tools.length} ClickSend tools`);
```

## 调用

在 LangChain agent 中使用这些 tools 来发送通信内容：

```typescript Use with an agent
import { ChatOpenAI } from "@langchain/openai";
import { createAgent } from "langchain";

// Create agent with tools
const agent = await createAgent({
  model: new ChatOpenAI({ 
    model: "gpt-4o-mini",
    temperature: 0 
  }),
  tools,
});

// Use the agent
const result = await agent.invoke({
  messages: [
    { role: "user", content: "Send an SMS to +1234567890 saying 'Hello from LangChain!'" },
  ],
});

console.log(result);
```

## API reference

有关全部功能和配置的详细文档，请参阅 [API reference](https://www.npmjs.com/package/@clicksend/langchain-clicksend-mcp)。

有关 ClickSend 的服务和定价信息，请访问 [clicksend.com](https://www.clicksend.com/)。
