---
title: "PerplexitySearchResults 集成"
description: "使用 LangChain JavaScript 集成 PerplexitySearchResults tool。"
---

[Perplexity Search API](https://docs.perplexity.ai/docs/search/quickstart) 返回实时且有依据的 Web 搜索结果。`PerplexitySearchResults` 是一个 LangChain [tool](/oss/integrations/tools/) 包装器，可让 agents 查询该 API 并接收 JSON 数组形式的搜索结果。

## 概览

### 集成详情

| Class | Package | [PY support](https://python.langchain.com/docs/integrations/tools/perplexity_search/) | Version |
| :--- | :--- | :---: | :---: |
| [`PerplexitySearchResults`](https://reference.langchain.com/javascript/langchain-perplexity/PerplexitySearchResults) | [`@langchain/perplexity`](https://www.npmjs.com/package/@langchain/perplexity) | ✅ | ![NPM - Version](https://img.shields.io/npm/v/@langchain/perplexity?style=flat-square&label=%20&) |

## 设置

这个集成位于 `@langchain/perplexity` 包中：

<CodeGroup>
```bash npm
npm install @langchain/perplexity @langchain/core
```
```bash yarn
yarn add @langchain/perplexity @langchain/core
```
```bash pnpm
pnpm add @langchain/perplexity @langchain/core
```
</CodeGroup>

### 凭据

配置一个 [Perplexity API key](https://www.perplexity.ai/account/api/keys)，并将其设置为名为 `PERPLEXITY_API_KEY` 的环境变量：

```typescript
process.env.PERPLEXITY_API_KEY = "your-api-key";
```

你也可以配置 [LangSmith](https://smith.langchain.com/) 以获得更好的可观测性，不过这不是必需的：

```typescript
process.env.LANGSMITH_TRACING = "true";
process.env.LANGSMITH_API_KEY = "your-api-key";
```

## 实例化

```typescript
import { PerplexitySearchResults } from "@langchain/perplexity";

const tool = new PerplexitySearchResults({
  maxResults: 5,
  // searchDomainFilter: ["wikipedia.org"],
  // searchRecencyFilter: "week",
});
```

构造函数选项包括 `maxResults`、`country`、`searchDomainFilter`、`searchRecencyFilter`、`searchAfterDate` 和 `searchBeforeDate`。详细信息请参阅 [Perplexity Search API parameters](https://docs.perplexity.ai/docs/search/quickstart)。

## 调用

### 直接调用

```typescript
const result = await tool.invoke("Who won the most recent FIFA World Cup?");
console.log(result);
```

这个 tool 会返回一个 JSON 编码的搜索结果数组，每一项都包含 `title`、`url`、`snippet`、`date` 和 `last_updated`。

### 与 agent 一起使用

```typescript
import { ChatAnthropic } from "@langchain/anthropic";
import { createReactAgent } from "@langchain/langgraph/prebuilt";

const llm = new ChatAnthropic({ model: "claude-3-5-haiku-latest" });

const agent = createReactAgent({
  llm,
  tools: [tool],
});

const response = await agent.invoke({
  messages: [
    { role: "user", content: "What's the latest news on the Mars Sample Return mission?" },
  ],
});
```

## API reference

关于 `PerplexitySearchResults` 全部功能和配置的详细文档，请参阅 [API reference](https://reference.langchain.com/javascript/langchain-perplexity/PerplexitySearchResults)。
