---
title: "Nia Toolkit 集成"
description: "使用 LangChain JavaScript 集成 Nia search and index API。"
---

[Nia](https://trynia.ai) 是一个搜索和索引 API，可持续从文档、研究论文、数据集、代码库等来源提供上下文，因此 agents 不必依赖过期数据。它具备可扩展性、成本降低 5 倍，并且可靠。

## 概览

### 集成详情

| Class | Package | [PY support](/oss/integrations/tools/nia) | Version |
| :--- | :--- | :---: | :---: |
| [`NiaToolkit`](https://github.com/nozomio-labs/nia-langchain) | [`@nozomioai/langchain-nia`](https://www.npmjs.com/package/@nozomioai/langchain-nia) | ✅ | ![NPM - Version](https://img.shields.io/npm/v/@nozomioai/langchain-nia?style=flat-square&label=%20&) |

## 设置

这个集成位于 `@nozomioai/langchain-nia` 包中：

<CodeGroup>
```bash npm
npm install @nozomioai/langchain-nia @langchain/core
```
```bash yarn
yarn add @nozomioai/langchain-nia @langchain/core
```
```bash pnpm
pnpm add @nozomioai/langchain-nia @langchain/core
```
</CodeGroup>

### 凭据

在 [trynia.ai](https://trynia.ai) 注册以获取 API key。

```typescript
process.env.NIA_API_KEY = "nk_...";
```

你也可以配置 [LangSmith](https://smith.langchain.com) 以获得更好的可观测性，不过这不是必需的：

```typescript
process.env.LANGSMITH_TRACING = "true";
process.env.LANGSMITH_API_KEY = "your-api-key";
```

## 实例化

### 使用 toolkit

```typescript
import { NiaToolkit } from "@nozomioai/langchain-nia";

const toolkit = new NiaToolkit({
  includeSearch: true,        // 5 search tools
  includeSources: true,       // 7 source management tools
  includeGithub: true,        // 4 GitHub tools
  includeContexts: true,      // 2 context/memory tools
  includeDependencies: true,  // 2 dependency tools
});

const tools = toolkit.getTools(); // 20 tools
```

### 使用单个工具

```typescript
import { NiaSearch } from "@nozomioai/langchain-nia";

const tool = new NiaSearch();
```

## 调用

### 跨已索引来源搜索

```typescript
import { NiaSearch } from "@nozomioai/langchain-nia";

const tool = new NiaSearch();
const result = await tool.invoke({ query: "how to use React hooks" });
```

### 搜索 Web

```typescript
import { NiaWebSearch } from "@nozomioai/langchain-nia";

const tool = new NiaWebSearch();
const result = await tool.invoke({ query: "latest Node.js release", numResults: 5 });
```

### 在 agent 中使用

```typescript
import { NiaToolkit } from "@nozomioai/langchain-nia";
import { ChatOpenAI } from "@langchain/openai";

const toolkit = new NiaToolkit({ includeSearch: true });
const tools = toolkit.getTools();

const llm = new ChatOpenAI({ model: "gpt-4o" });
const llmWithTools = llm.bindTools(tools);

const result = await llmWithTools.invoke("Search for React hooks best practices");
```

## 可用工具

| Tool | Description |
|------|-------------|
| `NiaSearch` | 对已索引的仓库、文档和数据源执行语义搜索 |
| `NiaWebSearch` | 支持分类过滤和日期范围的 Web 搜索 |
| `NiaDeepResearch` | 多步骤综合研究 |
| `NiaUniversalSearch` | 同时搜索所有来源 |
| `NiaAdvisor` | 对照已索引文档分析代码 |
| `NiaIndex` | 索引新的来源，仓库、文档、论文、数据集 |
| `NiaSourceList` | 按条件列出已索引来源 |
| `NiaSourceSubscribe` | 订阅预先索引的公开来源 |
| `NiaSourceSync` | 重新同步来源以拉取最新变更 |
| `NiaRead` | 读取已索引来源中的文件或页面 |
| `NiaGrep` | 在已索引来源内执行正则搜索 |
| `NiaExplore` | 浏览已索引来源的文件树 |
| `NiaGitHubSearch` | 在 GitHub 仓库中搜索代码 |
| `NiaGitHubRead` | 读取 GitHub 仓库中的文件 |
| `NiaGitHubGlob` | 查找匹配 glob 模式的文件 |
| `NiaGitHubTree` | 浏览仓库文件树 |
| `NiaContextSave` | 保存上下文以供跨 agent 共享 |
| `NiaContextSearch` | 对已保存上下文执行语义搜索 |
| `NiaDependencySubscribe` | 为项目依赖自动订阅文档 |
| `NiaDependencyAnalyze` | 从 manifest 预览可索引的依赖 |

## API reference

详细文档请参阅 [langchain-nia GitHub repository](https://github.com/nozomio-labs/nia-langchain)。
