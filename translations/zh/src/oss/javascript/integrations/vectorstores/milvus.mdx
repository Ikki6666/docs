---
title: "Milvus 集成"
description: "使用 LangChain JavaScript 集成 Milvus vector store。"
---

[Milvus](https://milvus.io/) 是一个面向 embeddings 相似度搜索和 AI 应用的向量数据库。

<Tip>
**兼容性**

仅支持 Node.js。
</Tip>

## 设置

1. 按照[文档](https://milvus.io/docs/v2.1.x/install_standalone-docker.md)在本机用 Docker 运行 Milvus。
2. 安装 Milvus Node.js SDK：

```bash npm
npm install -S @zilliz/milvus2-sdk-node
```

3. 在运行代码前设置环境变量，例如：

```bash
export OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE
export MILVUS_URL=YOUR_MILVUS_URL_HERE
```

## 索引并查询文档

<Tip>
请参阅[这一节](/oss/langchain/install)，了解 LangChain 包的通用安装说明。
</Tip>

```bash npm
npm install @langchain/openai @langchain/core
```

```typescript
import { Milvus } from "@langchain/classic/vectorstores/milvus";
import { OpenAIEmbeddings } from "@langchain/openai";

const vectorStore = await Milvus.fromTexts(
  ["hello", "world"],
  [{ id: 1 }, { id: 2 }],
  new OpenAIEmbeddings(),
  { collectionName: "demo_collection" }
);

const response = await vectorStore.similaritySearch("hello", 1);
```

## 从已有 collection 查询

```typescript
import { Milvus } from "@langchain/classic/vectorstores/milvus";
import { OpenAIEmbeddings } from "@langchain/openai";

const vectorStore = await Milvus.fromExistingCollection(
  new OpenAIEmbeddings(),
  { collectionName: "demo_collection" }
);

const response = await vectorStore.similaritySearch("hello", 1);
```

## 相关内容

- Vector store [conceptual guide](/oss/integrations/vectorstores)
- Vector store [how-to guides](/oss/integrations/vectorstores)
