---
title: "YDB 集成"
description: "使用 LangChain JavaScript 集成 YDBVectorStore vector store。"
---

> [YDB](https://ydb.tech/) 是一个多功能的开源分布式 SQL 数据库，兼具高可用、可扩展、强一致性和 ACID 事务能力。它可以同时承载事务型，OLTP、分析型，OLAP，和流式 workload。

本指南快速介绍如何开始使用 `YDBVectorStore` [vector store](/oss/integrations/vectorstores)。关于全部功能和配置的详细文档，请参阅 [YDB LangChain.js guide](https://ydb.js.org/guide/langchain/)。

## 设置

使用 Docker 启动本地 YDB 实例：

```bash
docker run -d -p 2136:2136 --name ydb-langchain -e YDB_USE_IN_MEMORY_PDISKS=true -h localhost ydbplatform/local-ydb:trunk
```

安装 `@ydbjs/langchain` 和 `@langchain/core`。

本指南以 [OpenAI embeddings](/oss/integrations/embeddings/openai) 为例。你也可以改用[其他受支持的 embeddings models](/oss/integrations/embeddings)。

<CodeGroup>
```bash npm
npm install @ydbjs/langchain @langchain/openai @langchain/core
```
```bash yarn
yarn add @ydbjs/langchain @langchain/openai @langchain/core
```
```bash pnpm
pnpm add @ydbjs/langchain @langchain/openai @langchain/core
```
</CodeGroup>

### 凭据

本地 YDB 实例不需要额外凭据。

如果你在本指南中使用 OpenAI embeddings，还需要设置：

```typescript
process.env.OPENAI_API_KEY = "YOUR_API_KEY";
```

## 实例化

要实例化 vector store，请传入 embeddings model 和 connection string：

```typescript
import { YDBVectorStore } from "@ydbjs/langchain";
import { OpenAIEmbeddings } from "@langchain/openai";

const embeddings = new OpenAIEmbeddings({
  model: "text-embedding-3-small",
});

await using vectorStore = new YDBVectorStore(embeddings, {
  connectionString: "grpc://localhost:2136/local",
});
```

如果你不使用 `await using`，请在结束时手动调用 `close()`：

```typescript
const vectorStore = new YDBVectorStore(embeddings, {
  connectionString: "grpc://localhost:2136/local",
});
vectorStore.close();
```

## 管理 vector store

### 添加内容

```typescript
import { Document } from "@langchain/core/documents";

const document1 = new Document({
  pageContent: "The powerhouse of the cell is the mitochondria",
  metadata: { source: "https://example.com" },
});

const document2 = new Document({
  pageContent: "Buildings are made out of brick",
  metadata: { source: "https://example.com" },
});

const document3 = new Document({
  pageContent: "Mitochondria are made out of lipids",
  metadata: { source: "https://example.com" },
});

const documents = [document1, document2, document3];

const ids = await vectorStore.addDocuments(documents);
```

### 删除内容

```typescript
await vectorStore.delete({ ids: [ids[ids.length - 1]] });
```

## 查询 vector store

### 直接查询

```typescript
const results = await vectorStore.similaritySearch("biology", 2);
```

返回分数：

```typescript
const resultsWithScore = await vectorStore.similaritySearchWithScore("biology", 2);
```

按 metadata 过滤：

```typescript
const filteredResults = await vectorStore.similaritySearch("biology", 2, {
  source: "https://example.com",
});
```

### 转成 retriever 后查询

```typescript
const retriever = vectorStore.asRetriever({
  k: 2,
});
await retriever.invoke("biology");
```

## 用于检索增强生成

- [Build a RAG app with LangChain](/oss/deepagents/rag)
- [Agentic RAG](/oss/langgraph/agentic-rag)
- [Retrieval docs](/oss/langchain/retrieval)

## API reference

关于 `YDBVectorStore` 全部功能和配置的详细文档，包括搜索策略、近似最近邻索引和列定制，请参阅 [YDB LangChain.js guide](https://ydb.js.org/guide/langchain/)。
