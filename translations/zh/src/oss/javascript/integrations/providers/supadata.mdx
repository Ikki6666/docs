# Supadata

本页介绍如何在 LangChain 中使用 [Supadata](https://supadata.ai) 生态。它可以帮助你从 YouTube、TikTok 和其他平台加载数据。

## 安装与设置

要使用 Supadata，你需要安装独立的集成包。

```bash
npm install @supadata/langchain-js
```

## Document Loader

Supadata 集成允许你把各类视频平台内容加载为 LangChain documents。它支持获取转录内容和元数据。

### 用法

```typescript
import { SupadataLoader } from "@supadata/langchain-js";

const loader = new SupadataLoader({
  apiKey: "YOUR_SUPADATA_API_KEY", // or process.env.SUPADATA_API_KEY
});

const docs = await loader.load({
  url: "https://www.youtube.com/watch?v=dQw4w9WgXcQ",
  operation: "transcript",
  lang: "en",
  text: true,
  mode: "auto",
});

console.log(docs[0].pageContent);
console.log(docs[0].metadata);
```

### 元数据示例

你也可以获取视频的结构化元数据。

```typescript
import { SupadataLoader } from "@supadata/langchain-js";

const loader = new SupadataLoader({
  apiKey: "YOUR_SUPADATA_API_KEY",
});

const docs = await loader.load({
  url: "https://www.youtube.com/watch?v=dQw4w9WgXcQ",
  operation: "metadata",
});

console.log(docs[0].pageContent);
```

## 资源

* **NPM Package:** [`@supadata/langchain-js`](https://www.npmjs.com/package/@supadata/langchain-js)
* **GitHub Repository:** [`supadata-ai/langchain-js`](https://github.com/supadata-ai/langchain-js)

有关详细用法示例和 API 文档，请参阅[官方 GitHub 仓库](https://github.com/supadata-ai/langchain-js)。
