---
title: "Anthropic 集成"
sidebarTitle: "Overview"
description: "使用 LangChain JavaScript 集成 Anthropic。"
---

与 Anthropic 模型相关的全部功能都汇总在这里。

[Anthropic](https://www.anthropic.com/) 是一家 AI 安全与研究公司，也是 Claude 的创建者。
本页涵盖 Anthropic 模型与 LangChain 之间的全部集成。

## Prompting 最佳实践

与 OpenAI 模型相比，Anthropic 模型在 prompting 方面有一些特别的最佳实践。

**System Messages 只能是第一条消息**

Anthropic 模型要求任何 system messages 都必须是 prompt 中的第一条消息。

## `ChatAnthropic`

`ChatAnthropic` 是 LangChain `ChatModel` 的一个子类，因此它最适合与 `ChatPromptTemplate` 一起使用。
你可以使用下面的代码导入这个封装：

<Tip>
LangChain 包的通用安装说明，请参阅[这一节](/oss/langchain/install)。
</Tip>

```bash npm
npm install @langchain/anthropic @langchain/core
```
```typescript
import { ChatAnthropic } from "@langchain/anthropic";
const model = new ChatAnthropic({});
```

在使用 ChatModels 时，推荐将 prompts 设计为 `ChatPromptTemplate`。
下面是一个示例：

```typescript
import { ChatPromptTemplate } from "@langchain/classic/prompts";

const prompt = ChatPromptTemplate.fromMessages([
  ["system", "You are a helpful chatbot"],
  ["human", "Tell me a joke about {topic}"],
]);
```

然后你可以像下面这样把它接入一个 chain：

```typescript
const chain = prompt.pipe(model);
await chain.invoke({ topic: "bears" });
```

更多示例，包括多模态输入，请参阅 [chat model integration page](/oss/integrations/chat/anthropic/)。
