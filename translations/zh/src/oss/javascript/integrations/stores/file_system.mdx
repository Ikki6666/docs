---
title: "LocalFileStore 集成"
description: "使用 LangChain JavaScript 集成 LocalFileStore store。"
---

<Tip>
**兼容性**：仅适用于 Node.js。
</Tip>


本文将帮助你开始使用 [`LocalFileStore`](/oss/integrations/stores)。有关 `LocalFileStore` 全部功能和配置的详细文档，请参阅 [API reference](https://reference.langchain.com/javascript/langchain/storage/file_system/LocalFileStore)。

## 概览

`LocalFileStore` 是对 `fs` 模块的封装，用于以键值对形式存储数据。
每个键值对都会拥有自己的文件，并嵌套在传给 `.fromPath` 方法的目录中。
文件名就是键，文件内容则是该键对应的值。


<Info>
**传给 `.fromPath` 的路径必须是目录，而不是文件。**
</Info>

<Warning>
**这个文件存储会修改所给目录及其所有子目录中的任意文本文件。**

请确保你在初始化 store 时指定的路径中没有其他文件。

</Warning>


### 集成详情

| Class | Package | Local | [PY support](https://python.langchain.com/docs/integrations/stores/file_system/) | Downloads | Version |
| :--- | :--- | :---: | :---: |  :---: | :---: |
| [`LocalFileStore`](https://reference.langchain.com/javascript/langchain/storage/file_system/LocalFileStore) | [langchain](https://www.npmjs.com/package/langchain) | ✅ | ✅ | ![NPM - Downloads](https://img.shields.io/npm/dm/langchain?style=flat-square&label=%20&) | ![NPM - Version](https://img.shields.io/npm/v/langchain?style=flat-square&label=%20&) |

## 设置

### 安装

LangChain 的 `LocalFileStore` 集成位于 `langchain` 包中：

<CodeGroup>
```bash npm
npm install langchain @langchain/core
```
```bash yarn
yarn add langchain @langchain/core
```
```bash pnpm
pnpm add langchain @langchain/core
```
</CodeGroup>

## 实例化

现在可以实例化 byte store：

```typescript
import { LocalFileStore } from "@langchain/classic/storage/file_system"

const kvStore = await LocalFileStore.fromPath("./messages");
```

定义一个 encoder 和 decoder，用于在数据与 `Uint8Array` 之间来回转换：

```typescript
const encoder = new TextEncoder();
const decoder = new TextDecoder();
```

## 用法

你可以像下面这样用 `mset` 方法按键写入数据：

```typescript
await kvStore.mset(
  [
    ["key1", encoder.encode("value1")],
    ["key2", encoder.encode("value2")],
  ]
)

const results = await kvStore.mget(
  [
    "key1",
    "key2",
  ]
)
console.log(results.map((v) => decoder.decode(v)));
```

```python
[ 'value1', 'value2' ]
```

你也可以用 `mdelete` 方法删除数据：

```typescript
await kvStore.mdelete(
  [
    "key1",
    "key2",
  ]
)

await kvStore.mget(
  [
    "key1",
    "key2",
  ]
)
```

```text
[ undefined, undefined ]
```

## 产出 values

如果你想获取所有键，可以调用 `yieldKeys` 方法。你也可以传入一个 key prefix，这样只返回匹配该前缀的键。

```typescript
import { LocalFileStore } from "@langchain/classic/storage/file_system"

const kvStoreForYield = await LocalFileStore.fromPath("./messages");

const encoderForYield = new TextEncoder();

// Add some data to the store
await kvStoreForYield.mset(
  [
    ["message:id:key1", encoderForYield.encode("value1")],
    ["message:id:key2", encoderForYield.encode("value2")],
  ]
)

const yieldedKeys = [];
for await (const key of kvStoreForYield.yieldKeys("message:id:")) {
  yieldedKeys.push(key);
}

console.log(yieldedKeys);
```

```python
[ 'message:id:key1', 'message:id:key2' ]
```

```typescript
import fs from "fs";

// Cleanup
await fs.promises.rm("./messages", { recursive: true, force: true });
```

---

## API reference

有关 `LocalFileStore` 全部功能和配置的详细文档，请参阅 [API reference](https://reference.langchain.com/javascript/langchain/storage/file_system/LocalFileStore)
