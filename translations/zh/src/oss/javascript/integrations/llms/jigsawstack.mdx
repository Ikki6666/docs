---
title: "Jigsawstack prompt engine 集成"
description: "使用 LangChain JavaScript 集成 Jigsawstack prompt engine LLM。"
---

LangChain.js 支持调用 JigsawStack 的 [Prompt Engine](https://docs.jigsawstack.com/api-reference/prompt-engine/run-direct) LLM。

## 设置

- 创建一个[账号](https://jigsawstack.com/dashboard)，可免费开始使用
- 创建并获取你的 [API key](https://jigsawstack.com/dashboard)

## 凭证

```bash
export JIGSAWSTACK_API_KEY="your-api-key"
```

## 用法

<Tip>
LangChain 包的通用安装说明，请参阅[这一节](/oss/langchain/install)。
</Tip>

```bash npm
npm install @langchain/jigsawstack
```
```ts
import { JigsawStackPromptEngine } from "@langchain/jigsawstack";

export const run = async () => {
  const model = new JigsawStackPromptEngine();
  const res = await model.invoke(
    "Tell me about the leaning tower of pisa?\nAnswer:"
  );
  console.log({ res });
};
```

## 相关内容


- [Models guide](/oss/langchain/models)
