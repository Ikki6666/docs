---
title: 以编程方式使用文档
description: 将 LangChain 文档连接到你的 AI 工具和工作流
---

LangChain 致力于让文档尽可能易于访问。你可以通过 AI 助手、代码编辑器以及 Model Context Protocol (MCP) 等直接集成方式，以编程方式使用这些文档。

## 快速访问选项

在文档的任意页面右上角，你都可以找到一个上下文菜单下拉框：

<img
    className="block dark:hidden"
    src="/images/copy-page-light.png"
    alt="复制页面，浅色模式"
/>

<img
    className="hidden dark:block"
    src="/images/copy-page-dark.png"
    alt="复制页面，深色模式"
/>

其中包含 `llms.txt`、MCP 服务器连接，以及 ChatGPT 和 Claude 等其他快速访问选项。

## 使用 MCP 服务器

这些文档内置了 **Model Context Protocol (MCP) 服务器**，让 AI 应用能够实时查询最新文档。

LangChain 文档的 MCP 服务器地址为：

```txt
https://docs.langchain.com/mcp
```

连接后，你可以向 AI 助手提问有关 LangChain、LangGraph 和 LangSmith 的问题，它会搜索文档并提供准确、最新的答案。

### 连接到 Claude Code

如果你在使用 Claude Code，请在终端中运行以下命令，将该服务器添加到当前项目：

```bash
claude mcp add --transport http docs-langchain https://docs.langchain.com/mcp
```

<Note>
    **项目（本地）作用域**

    上面的命令只会将 MCP 服务器添加到当前项目或工作目录。若要全局添加 MCP 服务器并在所有项目中访问，请在命令中加入 `--scope user`：

    ```bash
    claude mcp add --transport http docs-langchain --scope user https://docs.langchain.com/mcp
    ```
</Note>

### 连接到 Claude Desktop

1. 打开 Claude Desktop
2. 前往 Settings > Connectors
3. 添加 MCP 服务器 URL：`https://docs.langchain.com/mcp`

### 连接到 Codex CLI

如果你在使用 OpenAI Codex CLI，请在终端中运行以下命令，全局添加该服务器：

```sh
codex mcp add langchain-docs --url https://docs.langchain.com/mcp
```

### 连接到 Cursor

将以下内容添加到 MCP 设置配置文件中：

```json
{
  "mcpServers": {
    "docs-langchain": {
      "url": "https://docs.langchain.com/mcp"
    }
  }
}
```

### 连接到 VS Code

将以下内容添加到 MCP 设置配置文件中：

```json
{
  "servers": {
    "docs-langchain": {
      "url": "https://docs.langchain.com/mcp"
    }
  }
}
```

### 连接到 Antigravity

将以下内容添加到 MCP 设置配置文件中：

```json
{
  "mcpServers": {
    "docs-langchain": {
      "serverUrl": "https://docs.langchain.com/mcp"
    }
  }
}
```

## 了解更多

有关如何使用 Mintlify 的 MCP 服务器的更多信息，请参见 [Mintlify 官方文档](https://www.mintlify.com/docs/ai/model-context-protocol)。

如果你有问题或反馈，请前往我们的 [社区论坛](https://forum.langchain.com/)。
